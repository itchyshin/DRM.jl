*.jl.cov
*.jl.*.cov
*.jl.mem
/docs/build/
.DS_Store
*.log
deps/deps.jl

# Julia env Manifests. Root is a library (never pin). docs/ + bench/ dev DRM via
# a machine-specific path, so their Manifests aren't portable; Phase 1.0 (Grace)
# pins portable docs/+bench/ Manifests via a relative dev path.
/Manifest.toml
/docs/Manifest.toml
/bench/Manifest.toml

# DocumenterVitepress: npm deps for the VitePress build (Node from NodeJS_jll).
/docs/node_modules/
/docs/package-lock.json

# Local preview helper (Claude_Preview launch config) — not part of the package.
/.claude/launch.json

# Subagent worktrees (created by the Agent tool; never commit).
/.claude/worktrees/

# Generated benchmark fixtures/results. Regenerate with bench/*.jl scripts.
/bench/fixtures/crossed_poisson/
/bench/results/
