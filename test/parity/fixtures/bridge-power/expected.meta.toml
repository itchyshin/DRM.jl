drmtmb_version = "0.7.0"
generated_on = "2026-08-24"
r_call = "drmTMB(drm_formula(y ~ (x + z)^2, sigma ~ 1), family = gaussian(), data = dat)"
seed = 20260824
note = "Generated outputs only; no drmTMB source vendored (MIT-clean). drm_bridge #467: (...)^k crossing expanded to main effects + interactions before @formula; `x:z` is StatsModels' `x & z`."
