# Reserved for the R-parity fixture suite (Workflow G, Phase 1.1+).
# drmTMB *reference outputs* (data + fitted numbers; meta records 0.6.0) under
# test/parity/fixtures/ here so the parity gate runs on CI with no live R+drmTMB
# install. Generated outputs only — never drmTMB GPL source (see AGENTS.md).
