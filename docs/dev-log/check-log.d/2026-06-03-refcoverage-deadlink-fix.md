| 2026-06-03 | **Ref-coverage dead-link fix** (#7) | CI docs build (no local Julia) | ✅ removed `DrmFit` from the `model-fitting-and-postfit.md` `@docs` block — its docstring `@ref`s `coef`/`vcov`/`nobs`, which are exported-but-undocumented, so VitePress flagged 3 dead `./@ref` links and failed the build. Re-add `DrmFit` once those three gain docstrings (parity follow-up). Other added entries (`DrmFormula`/`BivariateDrmFormula`/`bootstrap_summary`/`bootstrap_result`) kept. | Shannon |
