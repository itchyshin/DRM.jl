| 2026-06-02 | **Collision-free check-log** (process fix) | structure review (no local Julia this session) | ✅ split the single appended `check-log.md` table — root of the recurring parallel-PR merge conflicts — into per-slice files under `check-log.d/` (mirrors `after-task/`); frozen the historical table through 2026-06-02 with a banner; added `tools/build_check_log.jl` to render the combined view (and `--check` to validate rows) without rewriting a committed file; updated DoD (AGENTS.md), the PR template, and `drm-checkpoint.jl`. New entries are now collision-free across concurrent PRs. | Shannon |
