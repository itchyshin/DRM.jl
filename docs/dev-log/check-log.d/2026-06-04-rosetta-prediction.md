| 2026-06-04 | **Rosetta prediction surface** (R↔Julia parity docs) | docs CI build (no local Julia) | ✅ expected green; `docs/src/rosetta.md` only — in **Post-fit accessors** promoted the four prediction rows out of "planned (parity gap)" to live verbs: `predict(fit, newdata; type = :response)`, `predict_parameters(fit, newdata; type = :response)`, `marginal_parameters(fit)`, `prediction_grid(reference; predictor = values, …)`. Added a **Prediction** subsection (intent table + prose) documenting response vs `:link` scale, the `Dict(:mu=>…,:sigma=>…)` return with family extras (`:nu/:zi/:hu/:zoi/:coi`) and bivariate `:mu1/:mu2/:sigma1/:sigma2/:rho12`, and `prediction_grid` Cartesian-sweep/hold-others composition. Mapping checked against docstrings in `src/gaussian_core.jl` (`predict`/`predict_parameters`/`marginal_parameters`/`prediction_grid`). **Plain code spans, not `@ref`** (page uses zero `@ref`s — convention match), even though all three symbols are in the reference `@docs` (per #176). Honesty: only the response-mean `predict` row asserts a drmTMB R spelling; per-parameter/grid rows describe the *capability* (no fabricated drmTMB API). STRICT SCOPE held — no `src/`, `test/`, or other docs touched. | Shannon |
