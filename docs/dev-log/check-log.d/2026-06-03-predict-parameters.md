| 2026-06-03 | **predict_parameters + marginal_parameters** (univariate) | CI `Pkg.test()` (no local Julia) | ✅ green (expected); added `predict_parameters(fit, newdata; type)` — population-level (RE integrated out) prediction of every distributional parameter (`:mu`, `:sigma`, family extras `:nu`/`:zi`/`:hu`/`:zoi`/`:coi`), `:response` (per-parameter inverse link) vs `:link` (Xβ); iterates `fit.blocks` ∩ keys(`formula.forms`) so RE-SD/cutpoint blocks (`:resd`/`:recov`/`:cutpoints`) are skipped. `_param_response` helper holds the per-parameter inverse link reused from the fitters (`:mu`→`_mean_response`, `:sigma`→exp, `:nu`→exp/`_logit12` Tweedie, `:zi`/`:hu`/`:zoi`/`:coi`→`_logistic`). Cheap accessor `marginal_parameters(fit)` reads `fit.means[:mu]`+`fit.scales`. +`test_predict_parameters.jl`: in-sample anchor reproduces `:mu`/`:sigma` (rtol 1e-8), `:link`==`predict` link, Poisson params-set==blocks (`:mu` only), bivariate throws. Bivariate surface + `prediction_grid` deferred (follow-up). No engine touch. | Shannon |
