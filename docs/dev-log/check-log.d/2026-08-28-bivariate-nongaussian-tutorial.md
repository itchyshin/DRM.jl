| 2026-08-28 | **Bivariate non-Gaussian tutorial** (docs-parity gap vs drmTMB's `articles/bivariate-nongaussian.html`) | every `@example` block pre-run in one shared scope against the docs env; Documenter stage executes the page clean (the vitepress npm stage fails identically on clean main — environmental, pre-existing) | ✅ new `docs/src/tutorials/bivariate-nongaussian.md`: LogNormal joint (ρ12 = 0.599 recovered, vs raw Pearson 0.683 — the scale contrast taught explicitly), Student joint (ν = 5.94 via the **logm2** link; scale-vs-marginal-SD; ρ12=0 ≠ independence), staged `associate_pairs` (η = 0.599; conditional-uncertainty caveat). Student structured markers NOT demonstrated — deliberate D-180 #3 / #471 boundary, stated with its reason. **Bipartite two-tree interactions** (drmTMB's `bipartite-phylogenetic-interactions` article): RUNNABLE in DRM.jl — `fit_phylo_interaction(y, X, C_A, C_B)` verified (8×4-cell smoke, σ̂ = 0.774 vs true 0.7, converged) — but it is an **Engine-tier matrix-level API** (test/test_api_stability.jl:82), no `bf()`/`drm()` front end, so a user-facing tutorial would document a surface the freeze deliberately keeps unstable. No tutorial now; revisit if/when a `drm()` front end lands. Found + filed en route: #552 (three pages taught `exp(coef(fit,:nu))` for the logm2 ν link — fixed in PR #553). | Shannon |
