| 2026-06-03 | **r-julia-bridge dead-link hotfix** (#7) | CI docs build (no local Julia) | ✅ main docs build was broken: VitePress flagged 1 dead `./@ref` in `r-julia-bridge.md` (a `[…](@ref)` Documenter could not resolve), which slipped in via #135 (merged red on the wrong assumption it was the npm flake). Replaced the 3 `@ref` links with relative `.md` links (`rosetta.md`, `developer-notes/formula-grammar.md`) — the proven-reliable form. Restores the main docs deploy and unblocks all docs PRs. | Shannon |
