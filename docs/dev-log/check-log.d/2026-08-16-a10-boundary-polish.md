| slice | date | change | check | result |
|---|---|---|---|---|
| A10 boundary polish (#422) | 2026-08-16 | `src/sparse_laplace_glmm.jl` — `_laplace_boundary_polish`: when a variance component collapses onto the flat shelf, freeze `log_sd` at `log(1e-6)` and re-optimise the remaining coordinates conditionally. Adopted **only if strictly better**, so it can never worsen a fit. Applied at the **4** routes whose trailing `:resd` block is a single coordinate; the 4 routes carrying q≥2 variance blocks are deliberately left alone (the "log_sd is last" precondition is false there) | `test_boundary_polish.jl` (50 assertions); `tools/parity_phylo_nongaussian.R` re-run; phylo-penalty suites | **PASSED.** The A5 open question is closed: `phylo_gamma` went **PARITY_FAIL → PARITY_PASS**, coef |d| **6.83e-05 → 6.32e-08** (~1000×), and DRM.jl's logLik now matches native drmTMB to 10 digits (was 7.3e-05 short). Reported SD on the issue fixture 3.7e-36 → 1e-06. **A guard fired during implementation**: the naive patch matched 7 call sites, but 4 carry multi-coordinate variance blocks where the precondition fails — an assertion caught it before any write, and the patch was narrowed to the verified 4. One test threshold of mine was also wrong (assumed the floor always engages; it does not on every seed) and was corrected rather than the code |
