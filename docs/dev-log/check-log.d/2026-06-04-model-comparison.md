| 2026-06-04 | **Model comparison + accessor parity** (feat-model-comparison) | CI Pkg.test (no local Julia) | ✅ green (expected); new `src/comparison.jl` adds `lrtest(reduced, full)` → `(; statistic, dof, pvalue)` nested LR test (χ² via `Distributions.ccdf(Chisq, max(stat,0))`, `ArgumentError` when `dof ≤ 0`), `anova` alias, `aicc(fit)` = `aic + 2k(k+1)/(n-k-1)` (`Inf` when `n-k-1≤0`), `weights(fit)` = `ones(nobs)` (extends `StatsAPI.weights`), `update(fit, formula; data, kwargs...)` = `drm(formula, family(fit); ...)`. Wired `include("comparison.jl")` + exports `lrtest, anova, aicc, weights, update` in `src/DRM.jl`. +`test_comparison.jl` (LR stat>0, dof, p<0.05, anova alias, wrong-order throws, aicc>aic, weights ones, update→reduced coef count) wired at end of `runtests.jl`. No engine/gaussian_core touch. | Shannon |
