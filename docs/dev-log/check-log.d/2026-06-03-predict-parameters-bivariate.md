| 2026-06-03 | **predict_parameters + marginal_parameters** (bivariate) | CI `Pkg.test()` (no local Julia) | ✅ green (expected); extended `predict_parameters(fit, newdata; type)` to `BivariateDrmFormula` — predicts `:mu1`/`:mu2`/`:sigma1`/`:sigma2`/`:rho12` each from its own fixed-effects RHS (`_split_ranef(rhs)[1]`, `ηp = Xp·β`), mirroring `predict`'s two-response newdata handling (dummy LHS = response1 for σ/ρ placeholders, response2 for μ2). `_param_response` gained `:mu1/:mu2`→`_mean_response` (identity for Gaussian), `:sigma1/:sigma2`→`exp`, `:rho12`→`tanh` — the exact links the bivariate fitter stores (gaussian_bivariate.jl: `:rho12 = tanh.(Xr·β̂)`, `:sigma{1,2} = exp.(Xs{1,2}·β̂)`). `marginal_parameters` now reads mean params (`:mu1`/`:mu2`) from `fit.means` and scale/corr params (`:sigma1`/`:sigma2`/`:rho12`) from `fit.scales`. Univariate behaviour unchanged. +`test_predict_parameters.jl`: bivariate testset — keys==`formula.forms` params, in-sample anchor reproduces mu1/mu2/sigma1/sigma2/**rho12** (rtol 1e-8), `marginal_parameters` equality, `:link`==`predict` link for means; old `@test_throws ArgumentError` bivariate test removed (bivariate now works). `prediction_grid` still deferred (follow-up). No engine touch. | Shannon |
