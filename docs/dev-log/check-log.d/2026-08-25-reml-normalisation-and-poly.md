| 2026-08-25 | **REML normalisation unified (#477) + `poly()` landed (#492)** | full suite `runtests.jl` **325 testsets, 0 failures, 0 errors** · `DRM_PARITY_TESTS=1` bridge-formula fixtures **7/7** · q4 REML fixture **33/33** at the tightened tolerance · `check_test_deps` · `check_doc_test_citations` (108) · `parity_ledger` CLOSURE: PASS | ✅ green. **#477:** DRM.jl reported TWO restricted-log-likelihood scales under one name — univariate REML routes added `(n_β/2)·log(2π)`, bivariate q=2/q=4 did not. Unified to the normalised form (lme4/glmmTMB/TMB/drmTMB). Evidence is a **185× tightening**: the q=4 parity gate's `atol_loglik` fell from **5.5436 → 0.03**, because 5.513631 of that tolerance *was* the constant. Confirmed independently by `test_q4_reml_warm_restart.jl`, whose #484 same-optimum check previously **allowed** a 5.5136 offset and now allows none — it failed with `isapprox(0.001938, 5.513631)` the moment the change landed, which is a second oracle agreeing with the fixture. I had deferred this citing an in-code "maintainer call" note that turned out to be **my own commit from the same night**. Honest limit: `reml_q2.jl` shares the derivation but has **no parity fixture of its own**. **#492:** `poly()` landed, closing the bridge-formula group — R's orthogonal basis transcribes to 9.99e-16; `*`, `:`, `+`, `- term` all match R's `model.matrix()` width exactly, and `(...)^k` is **rejected on measured evidence** (R 6 columns vs 7, because R treats `poly(x, k)` as one term). Two new R-parity fixtures on byte-identical CSV. | Shannon |
