| 2026-09-04 | **Bridge option passthrough — `sparse` forwarded, unknown keys refused, gradient exposed** (#632, refs #527, OPEN, head `80d42caa8`) | `test_bridge_option_passthrough.jl` (new) + bridge-family neighbour suites (primitive boundary, formula labels, coef_labels echo, direct-export, `objective_at`, profile, S6 route matrix, #624 REML contract) | ✅ green; 16/16 new (unknown key errors + self-names; tolerance changes iterations; `sparse=true/false` match native coef+loglik; non-Bool `sparse` errors; gradient present/finite/named/matches native to 1e-10 when the fit carries one, absent otherwise). One item requested (`robust` optimizer preset) reported **ill-posed** — no such preset exists natively, word appears only in internal comments — rather than invented. Real gap found instead: `sparse::Bool` was silently dropped by `_bridge_fit`, and there was no fail-closed check on unknown option keys at all. One regression (`coef_labels` wrongly rejected) caught and fixed during neighbour verification. Closes no part of #527. PR not yet merged. | Rose (after-task pass; implementation by an earlier session) |
