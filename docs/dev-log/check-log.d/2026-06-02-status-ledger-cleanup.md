| 2026-06-02 | **Status / ledger cleanup** | `git diff --check` + `julia --project=docs docs/make.jl` + `julia tools/build_check_log.jl --check` | ✅ green; aligned `README.md`, `ROADMAP.md`, and the coordination board with the current v0.1.1 + #80/#136 state. Kept `src/DRM.jl` and the R-bridge stub untouched because open PRs own those paths; used `check-log.d/` rather than appending to the frozen `check-log.md`. Docs build passed with existing local-link/docstring/logo warnings. | Codex |
