| 2026-06-07 | **exact implicit-logdet gradient gate — Poisson phylo Laplace** (#165) | CI: GitHub Actions `test (1)`, `test (1.10)`, `docs` (no local Julia — package servers blocked) | ⏳ pending CI; hoisted the Poisson `phylo(1 \| grp)` Laplace f/g out of the fit closure into module-level `_poisson_phylo_laplace_fg` (warm-startable, tight-Newton-tolerance), so the exact implicit-function / implicit-logdet outer gradient (reusing `takahashi_selinv`, mirroring the verified q4 `marginal_and_exact_grad`) is testable in isolation. Added standing FD-vs-analytic gate `test/test_poisson_phylo_grad_gate.jl` (≤1e-6, evaluated OFF the optimum so the db̂/dθ correction is exercised) to `runtests.jl`. No objective/logLik change — pure refactor of the gradient evaluation path; the fit closure now delegates to the new function. | Shannon |
