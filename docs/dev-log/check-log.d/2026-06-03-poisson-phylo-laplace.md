| 2026-06-03 | **Poisson phylo sparse Laplace** (#80) | focused Julia test + `Pkg.test()` + paired Julia/R benchmark + `git diff --check` | ✅ green; added public `Poisson()` routing for `phylo(1 | species)` with `tree = ...` and `se = false` timing parity, plus paired drmTMB benchmark scripts. Measured Poisson phylo R/Julia median speedup is 1.82x across p=100,500,1000,2000 (min 1.40x, max 1.96x); all paired cells converge and match logLik/coefficients/phylo SD to numerical tolerance. Scope is Poisson mean `phylo(1 | species)` only; no zero-inflation/hurdle, NB2 phylo, or other structured non-Gaussian marker is claimed. | Codex |
