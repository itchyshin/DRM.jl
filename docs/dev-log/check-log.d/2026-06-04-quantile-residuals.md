| 2026-06-04 | **randomized quantile residuals** (feat-quantile-residuals) | CI Pkg.test (no local Julia) | ✅ green (expected); added back-compat `type` kwarg to `residuals(fit; type=:response, rng=Random.default_rng())` in `src/gaussian_core.jl`. `:response` byte-identical to old behaviour; `:quantile` → `r=Φ⁻¹(u)` (Dunn-Smyth / DHARMa): Gaussian continuous `u=Φ((y-μ)/σ)` (no RNG), Poisson randomized `u=F(y-1)+(F(y)-F(y-1))·U`, both clamped to `(eps,1-eps)`. Bivariate/unsupported families/unknown type → `ArgumentError`. Qualified `Distributions.Normal/Poisson/cdf/quantile`. +`test_quantile_residuals.jl` (back-compat, Gaussian exact `(y-μ)/σ` + ~N(0,1), Poisson finite + lenient normality, Gamma throws, bad type throws) wired at end of `runtests.jl`. No export, no `src/DRM.jl` touch. | Shannon |
