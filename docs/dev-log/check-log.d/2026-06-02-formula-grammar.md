| 2026-06-02 | **Formula-grammar article** (#7 / Workflow D) | CI docs build (no local Julia this session) | ✅ green (expected); filled the `developer-notes/formula-grammar.md` Phase-0 stub from the source (authoritative, no R needed): both `bf` shapes (univariate positional + bivariate keyword), the secondary-parameter/link table (`sigma`/`nu`/`zi`/`hu`/`zoi`/`coi`), `cbind` two-column response, the implicit-intercept rule (`y ~ x` ⇒ `1 + x`), RE spellings (`(1\|g)`/`(1+x\|g)`/`(0+x\|g)`/crossed) + structured markers (`phylo`/`spatial`/`animal`/`relmat`/`meta_V`), and the reserved-syntax rejection table (incl. the new bivariate placeholder-LHS / `cbind`-on-`mu` rejections). `@ref` targets verified against header text. Docs-only; no `src/` touch. | Shannon |
