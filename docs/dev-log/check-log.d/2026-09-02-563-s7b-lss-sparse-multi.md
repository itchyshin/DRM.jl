| 2026-09-02 | **O(p) sparse exact marginal LSS engine — multi-component** (#563 S7(b), Phase 2 / D-206) | `test/test_lss_sparse_multi.jl` (7/7) + `_gradient.jl` (8/8, FD rel ≤1.37e-7) + `_reml.jl` (25/25, REML FD rel ≤1.09e-7) + `_public.jl` (35/35 + S7b.6 tripwire RED 25.79→GREEN 1.83) + ledger (18/18 gates, leaf-s7b1..s7b5) + Totoro suite (D-205) | ✅ green; extends #551's single-phylo O(p) sparse engine to one phylo `sd()` component plus nested/small iid components (objective, exact gradient incl. cross-component `Hinv[g,t]` terms, REML, public `algorithm = :sparse` router) — D-206 GO for this class, NO-GO for general crossed (dense route, `n ≤ 5000`, unchanged); S7b.5 Totoro pilot found fill O(p) but wall time NOT (exponent ≈2.6–2.9 to p=10,000); S7b.6 traced this to the public router (`src/gaussian_lss.jl`) unconditionally densifying the phylo correlation before the route decision — **not** the sparse engine itself — fixed with a lazy marker, RED-first tripwire pinned, post-fix Totoro re-measurement: p=10,000 ML 1.62s / REML 2.86s, exponent ≈1.1/0.8, RSS 1.35 GB (was 8.0 GB), loglik bit-identical to pre-fix. Not covered: crossed layouts, many-component sparse, p > 10,000, a dedicated R same-target receipt for this multi-component sparse route. | Shannon (Noether, Curie, Karpinski) |
