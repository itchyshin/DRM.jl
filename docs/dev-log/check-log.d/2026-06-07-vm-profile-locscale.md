| 2026-06-07 | **Venzon–Moolgavkar guarded-Newton profile endpoints for the location–scale fit** (#202 / #227 item C15 / #209) | extended `test/test_locscale_profile.jl`: VM endpoints vs reference bracket+bisection (rtol 1e-3) on the mean slope, χ²₁-threshold endpoint property, FULL-vector `confint(:profile)` (covariance block included) with endpoint/unbounded-flag agreement + CI matrix (Julia 1.10 + 1) | ⏳ pending CI — replaced `_ls_profile_ci`'s bracket-EXPANSION + 18 fixed BISECTIONS per endpoint with a VM-style guarded-Newton root-find that uses the profile deviance AND its envelope-theorem slope (`∂nll/∂θ[idx]` = the idx-component of the exact `_ls_marginal_grad` at the constrained optimum), warm-started across steps, with a bracket/bisection safeguard for guaranteed correctness. `_ls_profile_nll` now returns `(minval, minimizer, ok)`; an infeasible trial (Λ near-singular at a variance/correlation boundary) is treated as a hard boundary (unbounded → ±Inf) instead of crashing, so the FULL location–scale vector profiles — the `parm=:mu` scoping #223 needed is removed. No change to the generic Gaussian profile path. Verification CI-only this session (no local Julia/R; package servers blocked by the environment network policy). | Claude |
