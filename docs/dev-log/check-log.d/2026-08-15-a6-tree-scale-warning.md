| slice | date | change | check | result |
|---|---|---|---|---|
| A6 tree-scale warning | 2026-08-15 | `phylo_tree_height(phy)` — O(p) BFS over the sparse topology (edge length recovered as `-1/Q[i,j]`) — plus a once-per-tree-per-session `@warn` at the two `drm()` phylo-resolution sites when height ≠ 1, naming the exact `sqrt(h)` factor and the rescaling fix. Exported; docstring cross-links `drm_phylo_penalty` | `test_phylo_tree_height.jl` (14 assertions); end-to-end `@test_logs` through `drm()`; phylo-penalty suites re-run (73 pass) | **PASSED**. Height verified against `sigma_phy_dense`'s tip variance to 1e-8 on 4 tree shapes. Warning fires off-unit only, once per tree, never throws. Why owner-priority-1: the convention difference vs drmTMB (`vcv(corr=TRUE)` vs raw branch lengths) bit this lane **twice in one day** — the A4c parity baseline and the A5b recovery study — despite being documented after the first time; a doc alone demonstrably does not protect users |
