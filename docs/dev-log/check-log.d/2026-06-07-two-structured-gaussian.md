| 2026-06-07 | **Gaussian with TWO structured variance components** (phylo + animal in one fit; new capability, no issue yet) | CI-only (no local Julia this session): GitHub Actions `test (1)`, `test (1.10)`, `docs` | ⏳ pending CI; new `_fit_two_structured_gaussian` (dense marginal `V = σ²I + σ₁²Z₁C₁Z₁ᵀ + σ₂²Z₂C₂Z₂ᵀ`) routed by `drm(::DrmFormula,::Gaussian)` when `_collect_structured` finds two markers; both σ₁/σ₂ reported via `re_sd`/`vc` (1×1 variance summaries) + BLUPs via `ranef`. `test_two_structured_gaussian.jl` (wired): seeded recovery of σ₁(phylo)+σ₂(animal)+resid, collapse check (σ₂→0 ⇒ single-phylo logLik/SD), error paths. **NEW capability — no drmTMB parity.** Dense O(n³) first cut; sparse/Woodbury + `sigma~x` residual = follow-up. | Shannon (Claude) |
