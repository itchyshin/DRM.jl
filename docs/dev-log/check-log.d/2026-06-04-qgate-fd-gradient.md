| 2026-06-04 | **Q-gate FD-vs-exact gradient ≤1e-6** (#14 / Workflow Q) | CI `Pkg.test()` (no local Julia this session) | ✅ green (expected). #14 was NOT wired as a standing test — only existed as manual poc scripts (`test/grad_check_p100.jl`, `grad_check_diag.jl`, `check_grad_tmbgrad.jl`) using old POC script-include paths, none in `runtests.jl`. Added `test/test_qgate_fd_gradient.jl`: self-contained p=8 q4 PLSM via `random_balanced_tree`+`make_problem` (same generator as `test_analytic_grad.jl`), θ=`pack_theta(β0, Λ0)` with non-diagonal Λ0 (grad_check_diag "case B"), analytic grad via `marginal_and_exact_grad(prob,Q_cond,θ)[2]` vs central FD (h=1e-6) of `marginal_nll(prob,Q_cond,θ)[1]`, `@test maximum(abs, g_analytic .- g_fd) ≤ 1e-6`. Wired `include` at END of `runtests.jl` after `test_parity_harness.jl`. | Shannon |
