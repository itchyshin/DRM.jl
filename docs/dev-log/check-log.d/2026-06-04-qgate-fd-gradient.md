| 2026-06-04 | **Q-gate FD-vs-exact gradient ≤1e-6** (#14 / Workflow Q) | `julia --project=. test/test_qgate_fd_gradient.jl` | ✅ green after local Julia repair. First local run reproduced the draft-PR failure (`max |analytic - FD| = 0.0028194448`) from cold-started FD at `h=1e-6`; warm-starting perturbed `marginal_nll` solves from the base θ-mode and using `h=1e-4` gives a stable reference (`max |analytic - FD| ≈ 9.7e-9`, below the `1e-6` gate). The standing test remains self-contained p=8 q4 PLSM via `random_balanced_tree` + `make_problem`, θ=`pack_theta(β0, Λ0)` with non-diagonal Λ0, analytic grad via `marginal_and_exact_grad(prob,Q_cond,θ)[2]`, and is wired at the end of `test/runtests.jl`. | Shannon / Codex |
