| 2026-06-04 | **Post-fit article** (Diagnostics & Validation: prediction, residuals, model comparison) | docs CI build with executed `@example` blocks (no local Julia) | ✅ expected green. New `docs/src/diagnostics-and-validation/prediction-and-postfit.md` — one executed `@example postfit` thread on a Gaussian location–scale fit walking the whole post-fit surface: `predict` (response + `type=:link` + `se=true` → `(; prediction, se)`), `predict_parameters` (per-parameter Dict; `se=true` → `(; value, se)`), `prediction_grid` sweep composing into `predict_parameters`, `marginal_parameters` (in-sample), `residuals(; type=:quantile)` (≈N(0,1) moment check), `lrtest`/`anova` nested LR test, `aic`/`bic`/`aicc`, and `update`. Every signature/return-shape verified against `src/gaussian_core.jl` (predict@426, predict_parameters@611, prediction_grid@693, marginal_parameters@746) and `src/comparison.jl` before writing. Registered in `docs/make.jl` nav (Diagnostics & Validation). Added a **Model comparison** `@docs` block (`lrtest`/`anova`/`weights`/`update`) + `aicc` to Information criteria in `reference/model-fitting-and-postfit.md` so all `@ref`s resolve (and the #180 API is documented). Refreshed the stale "AIC/LRT are a later convenience" line in `tutorials/location-scale.md` to point at the new article. `warnonly=true`, so any stray `@ref` only warns. Closes a real #7 article gap; documents the #180/#181/#184 surface. | Shannon |
