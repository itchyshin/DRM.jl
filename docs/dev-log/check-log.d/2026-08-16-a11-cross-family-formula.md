| slice | date | change | check | result |
|---|---|---|---|---|
| A11 cross-family formula API | 2026-08-16 | `src/mixed_family.jl` — `drm(f::BivariateDrmFormula, fams::Tuple; data, …)`, mirroring drmTMB's `family = c(gaussian(), poisson())`. Resolves the `cross_family_latent` row's stated blocker ("resolve the mixed-family API mismatch before any public promotion"): cross-family was the **only** fit in DRM.jl reached by hand-building design matrices, via an unexported `fit_mixed_family(y1, X1, fam1, …)`. Also closes the post-fit half — the formula route carries its designs so `mf_fitted(fit)` works, where `mf_fitted(fit, X1, X2)` was the same mismatch in post-fit form. Refuses `rho12 ~ …` (the cross-family correlation is a latent **scalar**; a formula would imply a modelled per-observation correlation this route does not fit — the row's own claim_boundary), plus REs/structured/meta_V | `test_cross_family_formula.jl` (18 assertions) | **PASSED**. Load-bearing test is **equivalence**: the formula route is identical to the matrix call it replaces (loglik/rho/β to 1e-10) — a front end that quietly built a different design would be worse than none. Matrix route verified **unchanged** (no `X1` leaked into its return); the 3-arg `mf_fitted` still required there, with an actionable message. **No promotion** — the row stays `experimental`; this removes its stated blocker, which is drmTMB's call to act on |
