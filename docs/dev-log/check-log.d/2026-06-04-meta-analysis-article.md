| 2026-06-04 | **meta-analysis tutorial fill** (docs-meta-analysis-fill) | CI docs build (no local Julia) | ✅ expected green; filled `docs/src/tutorials/meta-analysis.md` with random-effects (intercept-only `y ~ 1 + meta_V(v)`) + meta-regression (`y ~ x + meta_V(v)`) worked examples mirroring `test/test_meta.jl`, using `@example` executed blocks (matches `location-scale.md`). Noted `meta_known_V` deprecated (parity stub). Docs-only; no `src/`, `test/`, or `make.jl` touch. | Shannon |
