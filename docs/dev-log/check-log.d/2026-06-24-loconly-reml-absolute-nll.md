| 2026-06-24 | **Location-only REML absolute nll review correction** (PR #297) | `julia --project=. test/test_location_only_reml_mme.jl`; `julia --project=. test/test_bridge.jl`; `git diff --check`; overclaim scan | ✅ green; 602/602 focused assertions and 46/46 bridge-boundary assertions passed after review fixed the exact-Gaussian location-only REML helper to report the conventional restricted negative log-likelihood by subtracting the fixed-effect constant offset while preserving the optimizer-equivalent score/curvature path. Tests now recompute the dense GLS restricted nll with the same constant and keep coverage, bridge, q4, and AI-REML flags unpromoted. | Codex |
