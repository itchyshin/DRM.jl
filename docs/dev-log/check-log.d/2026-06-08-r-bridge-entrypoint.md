| 2026-06-08 | **R bridge primitive entry point** (#5 / Phase 1.5) | `julia --project=. test/test_bridge.jl` + direct bivariate smoke | green: `drm_bridge()` now builds formula bundles from strings or keyed dictionaries, dispatches family strings, returns a flat R-reconstructable dictionary, and matches native DRM.jl Gaussian one-response and two-response fits in the focused bridge test. Broader non-Gaussian admission still waits on coefficient-scale parity. | Codex |
