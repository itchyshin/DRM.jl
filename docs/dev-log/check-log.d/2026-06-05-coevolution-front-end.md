| 2026-06-05 | **Coevolution front end** (#187 / #190 implementation) | focused q4/residual/diagnostic tests + `Pkg.test()` + q4 benchmark gate + docs build + `git diff --check` | ✅ green; wired bivariate `bf()` with shared `phylo(1 \| species)` markers on `mu1`/`mu2`/`sigma1`/`sigma2` to the verified q=4 sparse engine, preserved the residual-only `rho12` path, stored `Σ_a` in the ranef payload with a `:phylocov` coefficient block skipped by prediction, and documented the public route. Verification: q4 front-end 17/17, residual bivariate 6/6, `check_drm` 14/14, full `Pkg.test()` passed, qgate FD ≤1e-6 passed, q4 baseline logLik `-256.5177` vs drmTMB `-256.5200`. Docs build completed with existing warnings outside this slice (`animal-models.md` ranef examples, index links, undocumented internals). | Codex |
