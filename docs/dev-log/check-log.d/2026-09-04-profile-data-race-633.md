| 2026-09-04 | **Sparse-LSS profile data race — never return an infinite bound from a failed endpoint** (#633, closes #631, OPEN, head `c2b81421d`) | new: `test_profile_infinite_bound.jl`, `test_profile_nuisance_status.jl`, `test_locscale_profile_status.jl` + 35 neighbour files (profiles, bridge, sparse routes, bootstrap, API freeze gate) | ✅ RED→GREEN; RED on `origin/main` was 180 passed/38 failed incl. `Evaluated: -Inf == -0.477...`; GREEN 218/218 + 3/3. Root cause: a `Core.Box` shared between a destructured `â` and `_fit_phylo_gaussian_lss_sparse`'s own `eval_core` closure made the stored objective/gradient thread-unsafe — **second instance of this defect class** (#549 was the first, variable `V`); same-class sweep in progress. Measured 122/300 concurrent objective pairs disagreed at 600 tips (up to 2.0e-2 obj / 7.4e-1 grad) before, 0/300 after; trigger is sparse+threading, not tip count (400-tip sparse reproduces, 500-tip dense and 600-tip serial do not). `confint(:profile)`/bridge inference now RAISE on a failed arm rather than returning ±Inf; `profile_result` keeps the auditable ±Inf. Post-fix agreement with `tmbprofile` oracle 2.8e-6/2.0e-6; threaded bit-identical to serial at 600/1000 tips. Not covered: `test_profile_sigma_a.jl`/`test_bridge_bivariate_inference.jl` aborted ~25min, runtime unconfirmed vs `origin/main`; no R end-to-end re-run (installed drmTMB 0.7.0 refuses the reporter's formula on the Julia engine). PR not yet merged. | Rose (after-task pass; implementation by an earlier session) |
