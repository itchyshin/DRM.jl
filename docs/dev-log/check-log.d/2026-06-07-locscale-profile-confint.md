| 2026-06-07 | **Public `confint(:profile)` for the location–scale fit** (#202 / #209 item 2) | new public-API testset in `test/test_locscale_profile.jl` (profile vs Wald, variance-param CI, `parm=`, `profile_result` backend marker, `check_drm` gradient) + CI matrix (Julia 1.10 + 1) | ✅ green on CI — routed `confint(fit; method=:profile)` / `profile_result(fit)` on a `drm()`-fitted coupled `(1\|tag\|group)` location–scale model to the robust trust-region profiler `_ls_profile_ci`; the fit carries its structured design in the `DrmFit.nll` slot as a callable `LocScaleObjective` (zero struct changes), and the router permutes between `:recov` coef order and the engine packing. `check_drm` now uses the exact analytic outer gradient for these fits (ForwardDiff can't pierce the Float64 inner solve), upgrading `max_abs_grad` from `NaN` to a real norm. No change to the generic Gaussian profile path or other families. Verification CI-only this session (no local Julia/R; package servers blocked by the environment network policy). | Claude |
