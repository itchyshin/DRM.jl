| slice | date | change | check | result |
|---|---|---|---|---|
| A4d-2 introspection | 2026-08-15 | `src/introspection.jl` — `profile_targets(fit; ready_only)` and `structured_effects(fit)`, drmTMB's post-fit inventories. `profile_targets` mirrors `profile_result`'s dispatch exactly so `profile_ready` tracks what the profiler will actually do; `structured_effects` reconstructs markers from the retained formula via `_collect_structured` | `test_introspection.jl` (21 assertions) | **PASSED**. Readiness pinned against real dispatch on three routes: fixed-effect locscale (ready, count matches `profile_result(fit).ci`), σ-phylo (not ready, note names `profile_ci`, and `profile_result` does throw), σ-phylo + `profile_ci=true` (only the SD blocks flip to ready). **`meta_vcov_bivariate` NOT built** — DRM.jl's `meta_V` is diagonal-only and the bivariate route ignores it, so the port would export a constructor whose output nothing can consume; recorded as blocked. **A4d-1 `corpair` BLOCKED** — StatsModels' `@formula` rejects both keyword args and string literals at macro-expansion, so drmTMB's syntax is not expressible and DRM.jl cannot even intercept the paste |
