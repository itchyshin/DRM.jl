| 2026-09-02 | **S7(b) design note: sparse multi-component LSS** (#563 S7b, Phase 1 — no code) | symbolic-alignment note + measured fill probes (nnz(H)/nnz(L) nested vs crossed, scaling to p=4000) + fresh adversarial review (APPROVE WITH REQUIRED EDITS → applied) | ✅ design gate met; GO for one phylo + nested/small iid components (nnz(L)/p flat ≈3.0), NO-GO for general crossed (two 200×200 crossed factors: nnz(L)/dim² ≈ 0.35, near-dense); review caught a load-bearing gradient error (diagonal-only selected inverse: 91% error vs 3.4e-10 with cross terms) before any code; Phase 2 estimate 20–28 agent-hours, owner decision pending | Shannon (Noether, Rose) |
