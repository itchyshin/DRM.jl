| 2026-06-04 | **delta-method prediction SE** (feat-predict-se) | CI Pkg.test (no local Julia) | ✅ green (expected); added `se::Bool=false` kwarg to `predict` / `predict_parameters` in `src/gaussian_core.jl`. `se=false` unchanged; `se=true` → `predict` returns `(; prediction, se)` (univariate) / `(; prediction::Dict, se::Dict)` (bivariate), `predict_parameters` returns `Dict{Symbol,NamedTuple}` of `p => (; value, se)`. Delta method: link-scale `se_i = sqrt(xᵢ' Vp xᵢ)` from `vcov(fit)[r,r]` (r = block range), response-scale × `_link_deriv(fam,p,η)` (identity→1, exp→exp η, logistic→μ(1−μ), tanh→1−ρ²). Helpers `_link_deriv`, `_block_range`, `_delta_se`; point values reuse `_param_response`. +`test_predict_se.jl` (back-compat, independent link-SE recompute, identity response==link, per-param value/se) wired at end of `runtests.jl`. No new exports, no `src/DRM.jl` touch. | Shannon |
