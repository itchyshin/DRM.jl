| slice | date | change | check | result |
|---|---|---|---|---|
| A9 general-covariance audit | 2026-08-16 | `docs/dev-log/evidence/2026-08-16-a9-general-covariance-audit.md` — the family comparison `general_covariance_structured`'s `next_action` asks for ("compare current DRM.jl accepted families with the R gate before widening"). Audit only; nothing widened or promoted | `relmat(1\|g, K=K)` + `sigma ~ 1` across 6 families, both engines; drmTMB baseline taken from the **unmodified system-installed 0.7.0** so the local binomial-phylo branch cannot contaminate it | **PASSED**. 5 of 6 agree. **The row understates DRM.jl by one family** — it credits Gaussian/Poisson/NB2/Gamma, but **Beta also fits** (ll 110.122). The one genuine asymmetry is `beta() + relmat()`: DRM.jl fits it, drmTMB refuses with the same structured-effect gate that refuses binomial — and drmTMB *does* admit `beta() + phylo()`, so it is a **provider**-coverage gap, not a family one. Also found: DRM.jl refuses `Binomial() + relmat()` with a bare `MethodError` (no `K` in the method signature) instead of an explanation — usability gap, filed not fixed. First test run was invalid (unnamed `K`; drmTMB requires dimnames) and was corrected before any conclusion |
