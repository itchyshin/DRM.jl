| slice | date | change | check | result |
|---|---|---|---|---|
| A4e ledger honesty | 2026-08-15 | `tools/parity_ledger.py` — new `DELIBERATELY_NOT_PORTED` class, each entry carrying a **written reason**, and a countdown that reports "genuinely owed" separately from "accounted for in writing" | `python3 tools/parity_ledger.py --drmtmb ../drmTMB --ref origin/main` | **PASSED**. Countdown now **0 export gaps (18 raw, 18 accounted for)**, down from a headline of 22. Correction recorded: an earlier claim that 9 names were "already implemented in `src/`" was right in count but wrong in reason — a text grep had matched **string literals and comments**, not definitions. The true decomposition is 3 delivered under a different spelling (family + bivariate formula), 5 R post-fit functions fed by the bridge payload (A2a's one-contract finding), 1 delivered as a field, 5 PARKED (#49), 2 R-side prep, 2 blocked with structural reasons, and 4 built by A4c/A4d. **This is a measurement fix, not a capability claim** — 11 capability rows remain un-`supported`, and that bar still requires a native-vs-Julia comparison per GOAL.md |
