| 2026-06-04 | **Prediction reference docs** (discoverability) | docs CI build (no local Julia; strict `@docs` resolves every symbol) | ✅ expected green; reference page `docs/src/reference/model-fitting-and-postfit.md` — added `predict_parameters`/`marginal_parameters` to the **Prediction and simulation** `@docs` block, `rho12` to **Fitted scale and correlation**, and `family`/`is_converged`/`deviance`/`dof_residual` to a renamed **Diagnostics and accessors** block (all four have docstrings in `src/summary.jl`; verified + exported in `src/DRM.jl`). Added a "Predicting distributional parameters" prose subsection with ONE non-executed ```julia``` worked example (page uses no `@example` blocks; non-executed chosen to avoid any build execution failure) — Gaussian loc–scale fit then `predict_parameters` over a covariate sweep + `marginal_parameters`. **Skipped `prediction_grid`**: it does not exist in `src/` (only referenced in dev-log/rosetta; deferred per the predict_parameters check-log) so a strict `@docs` entry would error. Skipped `predict`/`sigma`/`corpairs`: already present on the page. No changelog edit (no Unreleased/dev section — it only points to NEWS.md). `warnonly` untouched; no `src/`/`test/` changes. | Shannon |
