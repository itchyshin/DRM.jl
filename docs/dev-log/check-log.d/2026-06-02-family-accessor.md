| 2026-06-02 | **family(fit) accessor + Rosetta fix** (#7) | CI `Pkg.test()` + docs (no local Julia) | ✅ green (expected); added `family(fit::DrmFit)` post-fit accessor (exported; clash-checked vs StatsAPI/GLM) + `test_family_accessor.jl`; corrected the Rosetta `summary(fit)` row to the real API (`coeftable`/`show`, plus `family`). Isolated post-fit + doc; no engine/family-file touch. | Shannon |
