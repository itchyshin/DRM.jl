| 2026-06-07 | **Heritability / repeatability accessors with CIs** on structured-Gaussian fits (`heritability`, `repeatability`, `icc`; refs #225/#228/#230/#231) | CI-only (no local Julia this session): GitHub Actions `test (1)`, `test (1.10)`, `docs` | ⏳ pending CI (PR #233). New `src/heritability.jl`: ratios `h² = σ²_a/(Σσ²_k+σ²_resid)` and `R = σ²_g/(σ²_g+σ²_resid)` as `(estimate, corrected, bias, se, ci, level, method)`. `method=:delta` reuses `bias_correct` (#228) — exact ∇/H through the log-σ→variance map; `method=:profile` inverts an LRT on the ratio via the stored NLL. Variance-component θ indices pulled from the `:resd` + `:resid`/`:sigma` blocks (same as `re_sd`/`sigma`). CI clamped to [0,1]. `test_heritability.jl` (wired): closed-form anchor `==` hand-computed ratio (atol 1e-8, single + two-component), partition sums to 1, ICC ≥ heritability, degenerate anchors (component→0 ⇒ h²<0.1; residual→0 ⇒ h²>0.95), delta-vs-profile endpoint agreement (atol 0.12), error paths. Scoped to clean Gaussian structured models (no non-Gaussian / q4 Λ). | Shannon (Claude) |
