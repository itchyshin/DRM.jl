| 2026-06-03 | **q4 benchmark gate repair** | `julia --project=bench bench/run_sparse_tmb_nd.jl` + `git diff --check` | ✅ green; repaired the PR-template q4 sparse-engine regression gate by routing `bench/run_sparse_tmb_nd.jl` through the packaged `DRM` module and `bench/fixtures/` instead of stale poc-era `bench/*.jl` includes and external fixture paths. Gate result: logLik Julia `-256.5177` vs drmTMB `-256.5200` (`|Δ| = 0.0023`), converged, best wall `1.099s` vs drmTMB `2.48s` (`2.26x`). Scope is this template gate only; adjacent historical q4 bench runners may still need the same audit. | Codex |
