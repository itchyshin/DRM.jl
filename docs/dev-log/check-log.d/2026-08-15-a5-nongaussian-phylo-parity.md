| slice | date | change | check | result |
|---|---|---|---|---|
| A5 non-Gaussian phylo parity | 2026-08-15 | `tools/parity_phylo_nongaussian.R` — the native comparator the `phylo_gamma_beta_binomial` capability row asks for ("no native TMB parity … add comparator or parity evidence"). Gamma / beta / binomial × `phylo(1\|species)`, native drmTMB 0.7.0 vs DRM.jl | `Rscript tools/parity_phylo_nongaussian.R`; 4-seed characterisation; `g_tol` sweep 1e-8/1e-10/1e-12 | **EVIDENCE PRODUCED, no promotion.** beta **PARITY_PASS** (coef 5.0e-07, ll 3.9e-10). gamma marginal **FAIL** at 1.016e-04 vs tol 1e-4 — *not* optimiser tolerance (identical at all three `g_tol`) and *not* family-specific (3 of 4 seeds agree to ~1e-8 in both families; seed 411 is the outlier for **both**). **Tolerance deliberately NOT loosened to make it pass.** binomial → **NO_NATIVE_COMPARATOR**: native drmTMB refuses `binomial() + phylo()` ("Structured-effect syntax is planned, not implemented") though the row's `syntax` field lists it — so a third of the row cannot be parity-verified against 0.7.0 at all. Root cause of the seed-411 1e-4 **not** chased; recorded as an open question |
