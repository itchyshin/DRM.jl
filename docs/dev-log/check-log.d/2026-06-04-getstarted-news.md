| 2026-06-04 | **Surface recent capabilities on front page + changelog** (docs-getstarted-news) | docs CI build (no local Julia) | ✅ expected green; added a "Recent additions" prose bullet list to `docs/src/get-started.md` (no new executable code — page uses `@example gs` blocks, kept build-safe) and an Unreleased entry to `NEWS.md` (the canonical changelog; `docs/src/changelog.md` just mirrors it). Each item verified against `src/DRM.jl` exports before listing: `predict_parameters`/`marginal_parameters`/`prediction_grid`, `profile_result`, `summary`/`family`/`is_converged`/`deviance`/`dof_residual`/`rho12`, non-Gaussian `phylo(1|species,tree)` (Poisson/NB2/Gamma/Beta/Binomial, constant σ) + crossed `(1|g)+(1|h)`. No src/test/tutorial/make.jl touch. | Shannon |
