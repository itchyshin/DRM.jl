| 2026-06-21 | **mu multi-coef profile batching — bridge** (drmTMB#179) | drmTMB `test-julia-inference.R` Stage A multi-coef (live bridge, seeds {42,7,13,101} × n_tip {40,80}) + `using DRM` load | ✅ green; new `drm_bridge_inference` profile branch — `profile_param` set with `profile_coef` absent → `profile_result(fit; parm=block)` once → every coefficient row via `_bridge_inference_flatten_multi_profile` (rows adapted with `bounded = isfinite(lower) && isfinite(upper)`, since profile rows carry no `bounded` field). Single-coef + SD-row paths byte-unchanged. R side joins fixed-effect rows by coef NAME. Batched endpoints vs native tmbprofile ≤ 2.43e-5 on every well-fit cell (10/16 grid cells; 6 excluded = tracked Route A garbage-logLik phylo-mean fit, not a batching defect). Rose (claim-boundary) + Fisher (inference) GO after test-robustness + disclosure fixes. | Claude (Ada) |
