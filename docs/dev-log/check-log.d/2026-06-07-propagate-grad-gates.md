| 2026-06-07 | **propagate full-Newton-in-basin fix + FD gradient gates** (#165) | CI: GitHub Actions `test (1)`, `test (1.10)`, `docs` (no local Julia — package servers blocked) | ⏳ pending CI; (1) fixed the same line-search-exhaustion-at-the-minimum bug in `_poisson_crossed_mode` (crossed Poisson inner joint is strictly convex) by taking the FULL Newton step inside the quadratic basin and keeping the safeguarded line search only far from the mode — mirroring the merged `_poisson_phylo_mode` fix; (2) applied the SAME fix to `_phylo_mean_mode` and `_crossed_mean_mode`, gated on nonnegative data-Hessian weights so it activates for convex-data families (binomial/nb2/gamma) and falls back to the line search where the weight can go negative (beta); (3) hoisted the crossed-Poisson f/g into module-level `_poisson_crossed_laplace_fg` (warm-startable, tight-tol) and added `newton_tol`/`newton_maxiter` kwargs to `_phylo_mean_laplace_fg` / `_phylo_mean_laplace_nuisance_fg`. Added standing FD-vs-analytic gates: `test/test_poisson_crossed_grad_gate.jl` (≤1e-6) and `test/test_nongaussian_phylo_grad_gate.jl` (NB2/Gamma/Binomial ≤1e-6, Beta reported honestly at a looser tol since its inner joint is not cleanly convex), both wired into `runtests.jl`. No objective/logLik change — the marginal value/gradient computations are byte-identical; only the inner mode is driven tighter and the f/g body is relocated. | Shannon |
