| slice | date | change | check | result |
|---|---|---|---|---|
| check_drm partial vcov | 2026-08-15 | `src/inference.jl` — `check_drm` no longer calls `isposdef`/`eigvals` on a non-finite covariance. New `vcov_complete` field names the real situation; `vcov_posdef`/`min_eigval`/`cond` degrade to `false`/`NaN`/`Inf` and `ok` is honestly false, with a warning that says the Wald SEs on finite directions are still usable and points at `profile_ci` | `test_check_drm.jl` (26 assertions, incl. a regression test using the reporter's exact reproducer); Rose sweep for the same crash class | **PASSED**. The bug: any fit whose `vcov` held a NaN raised `ArgumentError: matrix contains Infs or NaNs` — and that is the NORMAL state of the sparse phylo route, which computes the β block only. A diagnostic whose purpose is to report trouble was crashing on it. **Sweep result (negative, reported as such):** `location_only.jl:701` already try/catches with a NaN fallback, `gaussian_ranef.jl:397` already guards with `!all(isfinite, …)` first, the `coevolution_q.jl`/`gaussian_bivariate.jl` `isposdef` calls validate USER-SUPPLIED matrices and should throw. All 8 other post-fit accessors (`stderror`, `confint`, `coeftable`, `aic`, `re_sd`, `vc`, `show`) were verified OK on such a fit. Isolated instance, not a class |
