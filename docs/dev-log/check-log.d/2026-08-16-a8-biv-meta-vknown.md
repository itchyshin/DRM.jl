| slice | date | change | check | result |
|---|---|---|---|---|
| A8 bivariate meta V_known | 2026-08-16 | `src/meta_vcov_bivariate.jl` — `meta_vcov_bivariate(v1, v2; cov12/cor12)` (drmTMB validation parity, dense 2n×2n round-trip with off-block refusal) **plus its engine consumer**: per-row known-V extension of `_fit_bivariate_residual` (`S_i = V_i + Σ_het,i`, PD sentinel) behind a new `drm(...; V =)` kwarg. No-V branch kept byte-identical. drmTMB's `meta_V(V = …)`-inside-formula spelling is a keyword arg StatsModels cannot express (the corpair limitation), so the kwarg surface is the documented divergence — irrelevant to the bridge, which ships arrays | `test_meta_vcov_bivariate.jl` (33 assertions, incl. an **independent dense-MVN loglik anchor** ported from drmTMB's own test and a ρ-separation test that fails if V is ignored); `tools/parity_biv_meta.R` vs installed drmTMB 0.7.0 | **PASSED**. Parity **3/3 PASS** (coef ≤2.6e-06, logLik ≤2.7e-09, heterogeneity components ≤2.7e-06 across independence / +0.6 / −0.4 sampling-correlation cells). Unblocks the A4d `meta_vcov_bivariate` claim_boundary — its `DELIBERATELY_NOT_PORTED` ledger entry removed (countdown raw 18→17, still 0 owed). The A2a meta remainder's *bivariate* half now has an engine path; bridge admission stays A2b-gated |
