| 2026-06-07 | **conjugate-EM solver wired as opt-in `algorithm = :em`** (#12) | CI `Pkg.test()` + docs (no local Julia/R here) | ⏳ pending CI (push + PR) — promoted `src/experimental/location_only.jl` → `src/location_only.jl` (EM math identical; dropped the LBFGS/generator/main script bits), `include`d after `gaussian_structured.jl`; added `algorithm` kwarg to the Gaussian `drm` path (`:auto` default = unchanged behaviour, `:gls`/`:lbfgs` aliases, `:em` for the phylo-mean + constant-σ cell, clear `ArgumentError` otherwise); EM DrmFit matches the structured shape with NaN vcov (documented). Added `test/test_conjugate_em.jl` (anchor: `:em` == default β/σ/logLik rtol 1e-3; clean errors on unsupported cells; accessors/show). The 3.1× speed figure is previously-measured (report §4), NOT re-measured. | Shannon |
