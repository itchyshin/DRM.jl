| 2026-09-02 | **LSS tip-identity strict boundary gate: natural-scale oracle** (#563 S7 / codex leaf lss-tip-identity G8) | `test/test_lss_tip_identity.jl` strict + default (410/410 each) + `test_lss_sparse.jl` unchanged | ✅ green; diagnosis: both fits are stationary optima of the same objective (\|Δloglik\| 7e-15) on a Hessian-singular ridge in the 2-parameter `sd_phylo` block, so a 4e-6 raw-θ tolerance was an unfulfillable oracle; gate now compares per-tip σ_phy (measured 1.2e-8 vs 4e-6) and loglik (≤1e-7); raw-θ kept as a non-gating diagnostic; the default suite's 1 Broken is gone honestly | Shannon (Noether, Fisher) |
