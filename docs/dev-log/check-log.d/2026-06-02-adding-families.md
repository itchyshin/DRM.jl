| 2026-06-02 | **Adding-families article** (#7 / Workflow D) | CI docs build (no local Julia this session) | ✅ green (expected); filled the `developer-notes/adding-families.md` Phase-0 stub from the real family pattern (`poisson.jl` as worked reference): the 7-step loop — family `struct` + docstring (links, `sigma`↔φ), the `drm(::DrmFormula, ::Family)` method reusing `_split_ranef`/`_design`, the AD-safe `_fit_*` fitter (`nll`/`zero(eltype(θ))`/Optim+ForwardDiff/`DrmFit`/`_withnll`), module `include`+export, recovery test + `runtests.jl`, reference `@docs`+tutorial, and the optional RE/`zi`/`hu` paths. `[DrmFit]` left as a code span (not in any `@docs` block → would be a broken `@ref`); `Poisson`/`Model specification` refs verified. Docs-only; no `src/` touch. | Shannon |
