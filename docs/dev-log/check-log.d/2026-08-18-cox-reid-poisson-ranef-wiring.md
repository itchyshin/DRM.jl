| 2026-08-18 | **Cox–Reid wiring: opt-in `method = :REML` on Poisson `(1\|g)`** (#443) | TDD red→green on standalone `test/test_cox_reid_poisson_ranef.jl` (38 assertions, not in `runtests.jl`) + full `julia --project=. test/runtests.jl` regression gate | ✅ 38/38 green; σ̂_b moves up per-seed as the penalty predicts; REML vcov ≠ ML vcov (#310); five uncertified Poisson routes error instead of silently returning ML; probe tripwire flipped as designed; ML default byte-identical | Shannon |
