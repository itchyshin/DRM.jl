| 2026-08-31 | **Paired integration CI repair** (#565) | Documenter PASS on fresh head; macOS Julia 1.10.12 profile 102/102 + threaded profile 19/19 + bootstrap refit 15/15 + optimizer neighbour 19/19 + canonical phylogenetic end-to-end 8/8 + structured non-Gaussian 29/29 + joint-missing 63/63; Totoro Julia 1.10.12 same focused counts PASS + bridge labels 819/819; Totoro Julia 1.10.12/1.12.6 bootstrap simulator 100/100 and profile 102/102 + threading 19/19; damaged warm-start, failed-endpoint continuation and raw-route controls PASS; isolated full Totoro run passed through late q4 inference then its final API-freeze gate exposed 21 unclassified prepared-joint exports; deliberate Experimental classification now passes 188/188; the second GitHub run exposed the retained second Gamma bootstrap refit; the certified BFGS/L-BFGS recovery ladder passes that B=2 fixture 15/15 on Totoro Julia 1.10.12 and 1.12.6; the third GitHub run exposed an unidentified zero-boundary raw log-SD comparison on Julia 1.12.7; corrected focused identity file passes 406/407 with one deliberate broken fixture on macOS Julia 1.10.12 and Totoro Julia 1.12.6; the fourth GitHub run kept Documenter green and Julia 1.10 passed in 46 minutes, while Julia 1.12.7 reached a late bare-`Gamma()` namespace ambiguity after 84 minutes; explicit `DRM.Gamma()` plus file-specific helper names pass the adjacent sequence 26/26 on macOS Julia 1.10.12 and Totoro Julia 1.12.6, the expanded affected numerical set passes 99/99 on macOS Julia 1.10.12, and the later heterogeneous sequence passes 26/26 on Totoro Julia 1.12.6; fifth fresh GitHub full-suite check pending | Strict optimizer continuations repair the observed whitened point-fit/profile failures without tolerance, random-seed, estimator, or public-API change; direct test dependencies and `LinearAlgebra.I` are explicit; existing prepared-joint exports are deliberately Experimental; every accepted point/profile continuation must earn the unchanged exact-gradient gate; the Beta boundary failure test is deterministic across Julia RNG implementations; fallback metadata and raw-route boundary are tested; canonical end-to-end coverage exercises the whitened route selected by that frontend; the scalar named-covariance invariant tests likelihood and identified coefficients directly and the zero-boundary phylogenetic SD on its natural scale; right-sized Beta-Binomial recovery retains both slopes, both SD bounds, convergence and certified gradient while its unchanged off-optimum raw FD gate preserves derivative coverage; the namespace repair changes tests only, removes the two observed shared-process helper overwrites, and qualifies all remaining later DRM Gamma-family constructors | Ada / Noether / Rose |
| 2026-09-01 | **CI namespace follow-up** (#565) | The fifth GitHub run kept Documenter and Julia 1.10 green, then found bare `Binomial()` after 88 minutes on Julia 1.12.7. Under an intentional conflicting `Distributions` import, boundary polish passes 50/50 + 4/4 and cross-family formula 18/18 on macOS Julia 1.10.12. Rose's static late-suite audit also found `LogNormal` and `BetaBinomial`; sixth fresh GitHub full-suite check pending. | Test-only qualification now covers all executable later DRM constructors that overlap `Distributions` (`Beta`, `BetaBinomial`, `Binomial`, `Gamma`, `LogNormal`, and `Poisson`); no model, estimator, tolerance, or public API changed. | Ada / Rose |
