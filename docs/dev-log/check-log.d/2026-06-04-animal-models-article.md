| 2026-06-04 | **Filled animal-models tutorial** | docs CI build (no local Julia) | ✅ green (expected); expanded `docs/src/tutorials/animal-models.md` into a real article — additive-genetic RE concept, `animal(1 \| id)` formula with `A =` keyword, worked Gaussian example mirroring `test_gaussian_structured.jl`, reading `re_sd`/`ranef` + heritability, `vc`-is-for-correlated-blocks note, and non-Gaussian future-work note linked to #167. Kept executed `@example animal` blocks (matches sibling tutorials). Scope: this one page + this check-log row only. | Shannon |
