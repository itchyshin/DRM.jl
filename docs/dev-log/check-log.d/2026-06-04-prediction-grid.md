| 2026-06-04 | **prediction_grid newdata helper** (feat-prediction-grid) | CI Pkg.test (no local Julia) | ✅ green (expected); added pure-data `prediction_grid(reference; n=50, kwargs...) -> NamedTuple` in `src/gaussian_core.jl` (Cartesian product of swept predictors, other columns held: numeric array → mean, other array → first, scalar as-is), exported alongside `predict_parameters`/`marginal_parameters`. +`test_prediction_grid.jl` (single/two sweeps, override, composes with loc-scale fit → length-7 :mu/:sigma) wired into `runtests.jl`. No engine touch. | Shannon |
