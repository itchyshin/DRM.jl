| 2026-08-02 | **BetaBinomial phylo + crossed sparse Laplace** (#166) | analytic-vs-ForwardDiff kernel check (machine precision) + analytic-vs-FD gradient gate (≤1e-6) + focused `test_betabinomial_phylo_laplace.jl`/`test_betabinomial_crossed_laplace.jl` (parameter recovery + public-API gradient) + regression run of `test_betabinomial*.jl` + `Pkg.test()` | ✅ green; generalized the verified Beta `:beta_fixed` kernel (`src/beta.jl` / `src/sparse_laplace_glmm.jl`) to beta-binomial's discrete known-trials term (shifted digamma/trigamma/polygamma at `s+a`, `n-s+b`, `n+φ`); added `_fit_betabinomial_phylo_laplace` and `_fit_betabinomial_crossed_laplace`, wired into `BetaBinomial()`'s `drm()` for `phylo(1 \| species)` (with `tree = ...`) and crossed `(1 \| g) + (1 \| h)` random intercepts on the logit mean. Beta-binomial phylo gradient gate achieved 6.6e-8, crossed gate achieved 7.7e-8 (both ≤1e-6 bar). Scope is constant-σ (overdispersion) only, per the issue's own acceptance bar — nonconstant-sigma beta-binomial, q>1 non-Gaussian phylo, and the location–scale extension (#202) are out of scope and rejected with explicit errors. No `src/` q=4 engine files touched. | Shannon |
