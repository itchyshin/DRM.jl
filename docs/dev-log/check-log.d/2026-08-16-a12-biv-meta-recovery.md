| slice | date | change | check | result |
|---|---|---|---|---|
| A12 bivariate meta recovery | 2026-08-16 | `tools/recovery_biv_meta.jl` + evidence note — the ADEMP recovery A8 shipped without. A8 had parity + an MVN anchor; neither asks whether the estimator **recovers** the heterogeneity, or whether `rho12` stays separated from the known sampling correlation | 50 reps × 3 cells (both signs of heterogeneity ρ against an opposite-signed sampling correlation), each replicate ALSO fitted without `V` as a control | **PASSED**. With `V`: `rho12` bias **≤0.4 MCSE in every cell** (−0.0045, +0.0023, −0.0033); variance components carry a 1–2 MCSE downward bias at n=120 that **shrinks by n=300** — expected ML behaviour. **The control is the finding**: omitting `V` biases `rho12` **toward the sampling correlation in both directions** (+0.0909 at sampling +0.6; −0.0635 at sampling −0.4) and the bias **does not shrink with n** (+0.0909 → +0.0901 from n=120 to n=300) — more data makes the wrong answer more precise, with 50/50 convergence and nothing diagnostic to flag it. Converts the meta-analysis guide's "silent failure" warning from a structural claim into a measured magnitude |
