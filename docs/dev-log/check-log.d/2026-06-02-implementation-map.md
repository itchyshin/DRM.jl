| 2026-06-02 | **Implementation-map article** (#7 / Workflow D) | CI docs build (no local Julia this session) | ✅ green (expected); filled the `diagnostics-and-validation/implementation-map.md` Phase-0 stub from `src/` — maps each modelling feature → numerical method → fitter entry point: the verified q=4 engine (`fit_q4_sparse_tmb`), the Gaussian surface (fixed / `(1\|g)` Woodbury / `(1+x\|g)` 2×2 capacitance / crossed whitened-Woodbury / σ-RE GHQ / structured GLS / spatial / meta_V), the 11 non-Gaussian families' fixed/RE/crossed/`zi`/`hu` fitters, and the shared spine (`sparse_laplace_glmm`, `takahashi_selinv`, `inference`). Complements source-map (files) + model-map (status). No cross-page `@ref` (kept build robust). Docs-only; no `src/` touch. | Shannon |
