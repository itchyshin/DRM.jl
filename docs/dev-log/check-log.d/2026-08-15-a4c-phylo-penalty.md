| slice | date | change | check | result |
|---|---|---|---|---|
| A4c phylo penalty | 2026-08-15 | `src/phylo_penalty.jl` — drmTMB's `drm_phylo_penalty()` / `drm_phylo_penalty_sweep()`. PC-prior on each phylo SD + optional N(0, `cor_sd`) on the **unconstrained** correlation, wired into the objective **and analytic gradient** of all four phylo blocks (mean-only sparse, asymmetric, separate, coupled). `penalty=` kwarg on `drm()`; new `:MAP` estimator tag; `phylo_penalty`/`penalty` fields on `DrmFit`; `lrtest`/`anova` refusal; `check_drm(...).penalized_map` | `test_phylo_penalty.jl` (73 assertions); `tools/parity_phylo_penalty.R` vs native drmTMB 0.7.0; full `Pkg.test()` | **PASSED**. Parity **3/3 PARITY_PASS** (ML baseline 9.66e-09, sd_u=0.5 7.46e-07, sd_u=0.25 9.06e-08; tol 1e-4). Penalty reproduces drmTMB's closed form to 1e-9; `penalty=nothing` **bit-identical** to ML; shrinkage measured over 10 seeds (ratio mean 0.958, sd 0.0079). Correlation gradient verified against FD to 1e-9. **Two findings**: DRM.jl's phylo SD is on the raw branch-length scale vs drmTMB's `vcv(corr=TRUE)`, so `sd_u` differs by `sqrt(tree height)` across implementations (documented + warned); and drmTMB reports `phylo_penalty`/`logLik` **off-optimum** via `obj$report()` at `last.par` — an upstream defect, filed not patched |
