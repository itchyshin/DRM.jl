| 2026-06-25 | **Poisson random-intercept recovery RNG stabilization** (DRM.jl#298 CI follow-up) | `julia --project=. test/test_poisson_re.jl`; `julia --project=. test/test_bridge_q2_direct_export.jl`; `julia --project=. test/test_bridge_q4_direct_export.jl`; `git diff --check` | ✅ local focused gates green after replacing the version-sensitive global RNG draw in `test/test_poisson_re.jl` with an explicit `MersenneTwister` draw and centering the simulated group effects. This responds to workflow-dispatch CI run `28201026220`, where both Julia test jobs failed the same brittle intercept-recovery assertion while `scaling-sweep` passed; Documenter run `28201026240` passed on the pre-fix head. No q2/q4 support claim, REML claim, interval claim, or bridge claim was widened. | Codex |
