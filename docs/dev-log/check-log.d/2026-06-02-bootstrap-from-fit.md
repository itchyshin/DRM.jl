| 2026-06-02 | **fit-based bootstrap entry points** (#131) | focused bootstrap tests + `Pkg.test()` + threaded timing fixture | ✅ green; added `bootstrap_result(fit; data, ...)`, `bootstrap_summary(fit; data, ...)`, and `bootstrap_ci(fit; data, ...)` so point-estimate-plus-bootstrap workflows avoid refitting the base model before the simulated refits. Existing formula/family methods unchanged. Bootstrap result now records `julia_threads`, `blas_threads`, and replicate-phase `elapsed`. Deterministic Poisson `(1\|g)` workflow, B=12, threads=4, BLAS=1: formula workflow median 0.074035s vs fit workflow 0.068708s (1.078x), 12/12 used in both, point estimates and summary estimates identical. | Codex |
