| 2026-06-21 | **relmat/animal/spatial covariate dispersion `sigma ~ x`** (#164 follow-on) | `test_relmat_counts_nb2.jl` + `test_relmat_counts_beta.jl` (recovery + FD ≤1e-6 + reduction) + #164 phylo/crossed regression + `test_nonconst_sigma_re.jl` + sparse-Laplace batch | ✅ green; extracted a Q-generic `_fit_general_mean_laplace_hetero` core from the phylo hetero fitter (phylo hetero now a thin tree→Q wrapper, byte-identical) and branched `_fit_{nb2,gamma,beta}_relmat_laplace` — constant-σ → the unchanged nuisance core, covariate-σ → the hetero core with the relmat-derived Q — dropping the three `sigma ~ 1`-only guards. NB2/Gamma/Beta `sigma ~ x` recovery 7/7 each, **σ-slope bands EXCLUDE 0** (recovered −0.28 / −0.19 / −0.30; a constant-σ fit would fail); FD-vs-exact ≤ 1e-6 (margin 4.9e-8) + 1-col-Xσ reduction EXACT (0.0 / 0.0) on a general-covariance Q. Core extraction byte-identical (#164 phylo + crossed gates all green). Constant-σ + broader sparse-Laplace batch (crossed 46/46, nonconst 21/21, phylo nuisance) no regression. De-staled family docstrings + get-started + model-map. Rose (claim-boundary) + Fisher (inference) GO after σ-slope band-tightening + doc fixes. | Claude (Ada) |
