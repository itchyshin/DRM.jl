| 2026-06-04 | **Spatial-models tutorial fill** (#167 concept-link / Workflow D) | docs build (no local Julia; CI verifies `@example sp`) | ✅ green (expected); coordinate-spatial article — worked Gaussian `spatial(1 | site)` example mirroring `test_gaussian_spatial.jl` (G×2 `coords` matrix, `K(ρ)=exp(-d/ρ)`), reads spatial SD via `re_sd`/`vc` and range via `coef(fit, :range)`; added Gaussian-only note pointing non-Gaussian spatial routing to #167. Docs-only; no `src/`/`test/`/`make.jl` touch. | Shannon |
