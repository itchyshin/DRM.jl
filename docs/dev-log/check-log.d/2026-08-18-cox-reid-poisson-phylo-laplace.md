| 2026-08-18 | **Cox–Reid wiring: opt-in `method = :REML` on Poisson phylo/relmat Laplace** (#450) | TDD red→green on standalone `test/test_cox_reid_poisson_phylo.jl` (27 assertions, not in `runtests.jl`) + S5 docs (Cell D not recovery; ML default; no chip) | ✅ 27/27 green twice (S3/S4); σ̂_CR > σ̂_ML per seed (direction, not recovery); reml_loglik ≠ ml_loglik; uncertified routes still error; public `Poisson()` warning already present; no `runtests.jl`; no capability chip | Shannon |
