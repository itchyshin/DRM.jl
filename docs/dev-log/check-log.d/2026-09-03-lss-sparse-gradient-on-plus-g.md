| 2026-09-03 | **Sparse LSS gradient made O(n+G), not O(G·n)** (#630, refs #627, MERGED `301121287`) | `runtests.jl` full suite (457 testsets) + `test_lss_sparse_gradient_scaling.jl` (new) | ✅ green; 457/457, 0 failures, 0 exceptions. Gradient 17.4×, one-coefficient profile 8.2×, point fit 13.2× (27.16s→2.05s) at 16,384 tips; CI endpoints bit-identical, 0.0 relative movement across 279 compared values across four fixture shapes; evaluation counts unchanged (search provably untouched). PR began from a **wrong diagnosis** — #627 named the endpoint search, citing worst-case bounds as measurements; phase-1 measurement (14 objective calls/coefficient, flat in G, 0 cap hits) overturned it and found the true O(G·n) gather in the sd-block quadratic term instead. Does NOT explain the reported 2h02m at N=10,970 (~236× the pre-fix projection); #627 stays OPEN. | Rose (after-task pass; implementation by an earlier session) |
