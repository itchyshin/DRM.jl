| 2026-06-07 | **Epsilon-method bias correction** (#227 B11) | `Pkg.test()` (CI) — `test_bias_correct.jl` | ✅ CI-only this session; anchors: identity (linear g ⇒ bias≈0, se/ci = plug-in Wald, exact); curvature (g=exp ⇒ corrected=exp(m)(1+v/2), matches exp(m+v/2) within O(v²)); exactness (g=θ²⇒m²+v, g=θ₁θ₂⇒m₁m₂+V₁₂); DrmFit method == manual (θ̂,V) form | Shannon |
