| 2026-08-25 | **Public-docs limb audit + `phylo_count_large_p` Julia gate** (DRM.jl#490, #491) | `tools/check_doc_test_citations.py` (new, 108 citations) + `check_test_deps.py` + standalone gate run + injected-drift verification | ✅ green. Found and fixed TWO opposite defects in `docs/src/capabilities.md`: a shipped capability declared **"Absent"** (cross-family bivariate — `src/mixed_family.jl` + 3 wired test files + a ~450-line methods guide all existed; #490), and a capability propped up by a **test that never runs** (`test_q4_laplace.jl`, deliberately unwired, exercising the `bench/` PoC rather than the `src/` file its row names). Both are the same gap: public documentation is one of design/168's four limbs, and nothing in the DoD gates prose the way it gates code. New guard is prove-or-skip, verified against three injected drifts (prefixed citation, bare `test_foo.jl` escape hatch, commented-out `include()`). Swept the page's other two negative claims — both hold. New `test/test_phylo_count_largep_gate.jl` (23 pass + 1 broken, 7.5s) closes limb (a) of that row's boundary; its load-bearing assertion round-trips `re_sd` across tree heights 0.8/1.6/4.0 and was verified to fire on the injected raw-vs-normalised mistake (2.236× = √(4.0/0.8)) and not on correct behaviour (1.08×). Writing it surfaced **#491**: `converged` is `true` at p=128 and `false` for every p ≥ 192 while estimates improve — a flat acceptance limit compared against a gradient linear in n. Not fixed (owner's call); recorded with `@test_broken` so a repair reports "Unexpectedly Pass". | Shannon |
