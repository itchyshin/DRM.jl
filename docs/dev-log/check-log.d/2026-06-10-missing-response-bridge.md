| 2026-06-10 | **Gaussian response-missing bridge slice** (`engine = "julia"` parity support) | `julia --project=. test/test_missing_response.jl`; `julia --project=. test/test_gaussian_bivariate_phylo.jl`; `julia --project=. test/test_bridge.jl`; `julia --project=. test/test_gaussian_core.jl`; `julia --project=. test/test_gaussian_bivariate.jl`; live R bridge smoke from `/tmp/drmtmb-julia-engine-pr` with `DRM_JL_PATH=/Users/z3437171/Dropbox/Github Local/DRM.jl` | ✅ green: univariate Gaussian fixed-effect location-scale fits now accept `missing`/`NaN` responses by fitting the observed-response likelihood and returning full-length fitted/residual vectors; residual bivariate Gaussian fits now use the observed-cell likelihood (complete rows bivariate normal, one-sided rows marginal univariate normal). `drm_bridge()` accepts both shapes. The q=4 phylogenetic location-scale engine now rejects response-missing rows clearly until the sparse latent kernel gets an observed-cell mask slice. Live R bridge smoke matched native TMB logLik to 4.83e-13 (univariate) and 2.92e-10 (bivariate residual). | Codex |
