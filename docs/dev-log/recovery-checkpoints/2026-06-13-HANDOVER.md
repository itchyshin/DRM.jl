# HANDOVER — DRM.jl + drmTMB two-package push (2026-06-13, live)

> **Cold-pickup note for any thread / Codex.** Overarching goal (user-set):
> **finish the overall plan — two packages (DRM.jl + drmTMB), coordinated launch.**
> Reconstruct from this note + GitHub (PRs/issues) + `git log` on the branches below.
> The in-session task list is NOT visible to a new thread — it is mirrored here.

## Repos / worktrees
- DRM.jl primary: `/Users/z3437171/Dropbox/Github Local/DRM.jl` (`codex/missing-response-bridge`).
- DRM.jl work worktree: `/Users/z3437171/worktrees/DRM-integrate` (`shannon/unblock-locscale-profile-test`).
- drmTMB work worktree: `/Users/z3437171/worktrees/drmTMB-RELEASE` (`shannon/RELEASE-drmtmb`).
- HSquared.jl (sister quant-gen): `/Users/z3437171/Dropbox/Github Local/HSquared.jl`.

## ✅ DONE this session
1. **Ayumi #2 σ-phylo — REPLIED.** Separate/uncorrelated σ-phylo block (Fisher-z, REML, boundary CIs,
   missing-responses): DRM.jl `2020235` merged to `main` `990ee03`; drmTMB gate-relax `da90d8a7` pushed
   as `shannon/RELEASE-drmtmb`. Reply POSTED to Ayumi-495/LS_ecogeographical-rules#2 (comment 4699253063).
   Verified end-to-end (R→Julia REML round-trip; clean-install-verified committed branch).
2. **PR #281** (`shannon/unblock-locscale-profile-test`) — unblock the local suite + fix every failure it
   exposed: `0e351af` gate the locscale profile stall (min→1s); `6a23071` rho12 link P1
   (`_param_response`/`_link_deriv` → guarded RHO_GUARD·tanh; test_predict_parameters 35/35);
   `777855c` bridge profile efficiency (drm_bridge_inference profiles only the SD target; test_bridge 46/46);
   `aac0c95` h-sweep-trough FD gate for non-const-σ RE (the CI `test(1.10)` failure was a single-h FD flake
   at 1.06e-5 vs 1e-5 — Linux noise, not a regression; 21/21). **CI re-running on aac0c95 to confirm green.**
   `docs` job fails separately = task #9.
3. **tmb-vs-julia ≤1e-6 parity** committed on drmTMB (`021f0278`+`500b5254`): Route C (loc-scale,
   |ΔlogLik|=1.5e-10) + Route B (biv residual rho12, |ΔlogLik|=1.2e-9 — **validates review P1 alignment**).
   7 pass / 1 skip. tests/testthat/test-julia-tmb-parity.R.
4. **drmTMB vignette** `julia-engine.Rmd` (`943e073a`) documents the σ-phylo REML route.
5. **HSquared AI-REML** assessed → linear-only; agrees with DRM's Laplace finding (task #7, post-launch).

## PR #281 CI VERDICT (commit 0321201)
- ✅ `test (1.10)` GREEN (the FD-gate 2e-5 fix resolved the single-h flake). ✅ macOS local green.
- ❌ `test (1)` (latest Julia) — **#10**: test_mixed_family.jl:142/146 NB2×Gaussian recovery flake
  (Random.seed! not StableRNGs → different data on latest Julia; recovered 0.257 vs 0.5). Pre-existing,
  audit-predicted, NOT a PR #281 regression. ❌ `docs` — **#9** Documenter KeyError(:id), pre-existing.
- PR #281's actual fixes (unblock + rho12 P1 + bridge + FD gate) are VERIFIED (macOS + Julia 1.10).
  The two red jobs are SEPARATE pre-existing issues (#9, #10). PR is ready to review; leave merge to user.

## TASK LEDGER
- #1 ✅ unblock. #2 ✅ **macOS Pkg.test() GREEN** (222 testsets, EXIT 0 — FINISH gate #1); Linux CI confirming
  the FD fix. #3 ⏳ parity (C+B done; A blocked by #8). #5 ✅ Aqua+compat (#244).
- #4 drmTMB held-files (vignette done; `missing-data.R`/`man/*`/`README` uncommitted-finished; `_pkgdown.yml`/
  `vignettes/how-fitting-works.Rmd` HELD — leave for user). #6 [USER-GATED] launch. #7 [ENH] HSquared AI-REML.
- **#8 [BUG HIGH] PINPOINTED** (engine, not bridge): all-node Gaussian phylo-mean `laplace_ll`
  (src/sparse_aug_plsm.jl:266-275 → `-jn -0.5 logdetH +0.5 logdetP`). On a COALESCENT (rcoal) tree, short
  branches → huge 1/branch precision → ill-conditioned Hobs → `sparse_pd_chol` adds a LARGE ridge λ;
  `logdetH=logdet(Hobs+λI)` has no matching λ on logdetP → **+4e7 garbage logLik + converged=TRUE**.
  = independent review §2B (logdetH ridge-mislabel), UNbounded on real trees. Repro /tmp/issue8_rcoal.jl
  (rcoal→3.98e7) vs /tmp/issue8_repro.jl (balanced→-66, fine). Tests only use random_balanced_tree so it was
  never hit. FIX (engine owner): estep_mode RETURN λ → match ridge on P's logdet OR reject/flag (NaN +
  converged=false) when λ>0 + a coalescent-tree regression test. Tracked-skip as Route A in the parity test.
- **#9 [CI]** DRM.jl Documenter fails KeyError(:id) in ExampleBlocks; `docs/Project.toml` Documenter="1"
  unpinned → pin to last-good (≈1.4–1.7) vs DocumenterVitepress 0.3. Pre-existing; launch must-have.

## Environment / gotchas
- Julia `/Users/z3437171/.juliaup/bin/julia` (1.10; not on non-interactive PATH — `export PATH`). Rscript
  `/usr/local/bin/Rscript`; JuliaCall/callr/ape/pkgload/testthat present. **No `timeout`** (use poll loops).
- Machine CORE-CONTENDED (4 long-running R workers ~PIDs 3506-3511 + Defender) → local Julia/R slow; prefer CI.
- Bridge smoke: `JULIA_HOME=~/.juliaup/bin`, `options(drmTMB.DRM.jl.path=<DRM.jl checkout>)`,
  `pkgload::load_all(<drmTMB worktree>)`, `drmTMB(..., engine="julia")`. biv API:
  `drmTMB(bf(mu1=,mu2=,sigma1=,sigma2=,rho12=), family=biv_gaussian())`. Pure-Julia tree-from-Newick:
  `augmented_phy(read(<nwk>, String))`.
- Widget: `/tmp/drm-dashboard` port 8765 (`preview_start name="drm-dashboard"`); version.txt must == HTML BUILD ('r11').

## HELD — USER-GATED (never autonomous)
git push to `main` beyond what's done (incl. merging PR #281 — it's CI-confirming + ready, leave the merge to
the user); Julia registry; CRAN; coordinated tags; external GitHub posts beyond the (approved) Ayumi reply;
filing GLLVM issues; the drmTMB HELD files; any deep core-engine fix (#8) unattended. Branch commits / PRs OK.
