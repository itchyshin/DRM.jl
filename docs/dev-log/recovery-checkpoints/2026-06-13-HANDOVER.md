# HANDOVER — DRM.jl + drmTMB two-package push (2026-06-13, live)

> **Cold-pickup note for any thread / Codex.** Overarching goal (user-set):
> **finish the overall plan — two packages (DRM.jl + drmTMB), coordinated launch.**
> Reconstruct state from this note + GitHub (PRs/issues) + `git log` on the branches
> below; the in-session task list is NOT visible to a new thread, so it is mirrored here.

## Repos / worktrees
- DRM.jl primary: `/Users/z3437171/Dropbox/Github Local/DRM.jl` (on `codex/missing-response-bridge`).
- DRM.jl work worktree: `/Users/z3437171/worktrees/DRM-integrate` (on `shannon/unblock-locscale-profile-test`).
- drmTMB work worktree: `/Users/z3437171/worktrees/drmTMB-RELEASE` (on `shannon/RELEASE-drmtmb`).
- HSquared.jl (sister, quant-gen): `/Users/z3437171/Dropbox/Github Local/HSquared.jl`.

## ✅ DONE + VERIFIED this session
1. **Ayumi #2 σ-phylo — REPLIED.** Univariate separate/uncorrelated σ-phylo block (Fisher-z
   separation, REML, boundary CIs, missing-responses) published: DRM.jl committed `2020235`,
   merged to `main` (`990ee03`), pushed. drmTMB gate-relax `da90d8a7` pushed as
   `shannon/RELEASE-drmtmb` (her 404 gone). Reply POSTED to
   Ayumi-495/LS_ecogeographical-rules#2 (comment 4699253063) as itchyshin. Verified: R→Julia
   `engine="julia"` REML round-trip converges; species↔tree alignment faithful (mean-phylo SD
   recovers ~0.6); committed drmTMB branch clean-install-verified.
2. **Local-suite unblock + 2 exposed fixes → PR #281** (`shannon/unblock-locscale-profile-test`):
   - `0e351af` gate the `test_locscale_profile.jl` stall behind DRM_SLOW_TESTS (min→1s).
   - `6a23071` fix rho12 link P1: `_param_response`/`_link_deriv` now use guarded
     `RHO_GUARD·tanh` (matches `scales[:rho12]`). `test_predict_parameters` 35/35.
   - `777855c` fix bridge profile efficiency: `drm_bridge_inference` now profiles only the SD
     target (`parm=[:resd_sigma,:resd,:resd_mu]`), not the full vector. `test_bridge` 46/46.
3. **tmb-vs-julia ≤1e-6 parity (the audit's "untested as a number" gap)** — committed
   `tests/testthat/test-julia-tmb-parity.R` on drmTMB (`021f0278`+`500b5254`): Route C
   (Gaussian loc-scale, |ΔlogLik|=1.5e-10) + Route B (biv residual rho12, |ΔlogLik|=1.2e-9,
   **validates P1 rho12 alignment**). 7 pass / 1 skip.
4. **drmTMB vignette** `julia-engine.Rmd` (`943e073a`): documents the σ-phylo REML route
   (was stale "blocked").
5. **HSquared AI-REML assessed**: valid for linear Gaussian animal model only; its own docstring
   says it does NOT transfer to Laplace/non-Gaussian — agrees with DRM's finding. → task #7.

## ⏳ OPEN — the one live loose end
- **DRM.jl PR #281 CI: `test (1.10)` FAILED on Linux; `test (1)` still running.** Cannot see the
  failing test until the run completes (gh won't show logs mid-run). My macOS full `Pkg.test()`
  (with rho12 fix) showed ONLY `test_bridge:131` failing — now fixed + verified individually —
  so this is likely Linux/Julia-1.10-specific OR a pre-existing failure, NOT obviously my
  regression. **NEXT: when CI completes, `gh run view 27475087787 --log-failed` → identify the
  failing testset → triage (my rho12/bridge edit vs Linux env). `docs` job also fails = task #9.**
  A background poller (this session) will print it on completion.

## TASK LEDGER (mirror)
- #1 ✅ locscale stall unblock. #2 ⏳ full Pkg.test() green — local strong, **CI test(1.10) red (triage)**.
- #3 ⏳ parity — Routes C+B done; Route A blocked by #8. #5 ✅ Aqua+compat (already on main, #244).
- #4 drmTMB held-files hygiene (vignette done; `missing-data.R`/`man/*`/`README` uncommitted-but-finished,
  `_pkgdown.yml`/`vignettes/how-fitting-works.Rmd` HELD — leave for user).
- #6 [USER-GATED] launch (registry/CRAN/tags). #7 [ENH] HSquared AI-REML for DRM linear routes (post-launch).
- **#8 [BUG HIGH]** engine="julia" Gaussian phylo-mean → garbage logLik (3e7) + false converged on some
  data (all-node route; R faithfully passes `result$loglik`, julia-bridge.R:999). Repro `/tmp/routeA_diag.R`
  (rcoal(60)+BM). NOT Ayumi's σ-phylo route. Tracked-skip in the parity test (Route A).
- **#9 [CI]** DRM.jl Documenter fails `KeyError(:id)` in ExampleBlocks; `docs/Project.toml` has
  `Documenter="1"` unpinned → pin to last-good (≈1.4–1.7) compatible with DocumenterVitepress 0.3.

## Environment / gotchas
- Julia: `/Users/z3437171/.juliaup/bin/julia` (1.10; juliaup PATH not on non-interactive shell — `export PATH`).
- Rscript `/usr/local/bin/Rscript`; JuliaCall/callr/ape/pkgload/testthat available.
- **No `timeout` on this macOS** — use process-poll loops.
- Machine is CORE-CONTENDED (4 long-running R workers PIDs ~3506-3511 + Defender) → local Julia/R runs
  are slow; prefer CI for full-suite verification.
- Bridge smoke pattern: `JULIA_HOME=~/.juliaup/bin`, `options(drmTMB.DRM.jl.path=<DRM.jl checkout>)`,
  `pkgload::load_all(<drmTMB worktree>)`, then `drmTMB(..., engine="julia")`.
- biv API: `drmTMB(bf(mu1=,mu2=,sigma1=,sigma2=,rho12=), family=biv_gaussian())`.
- Widget dashboard: `/tmp/drm-dashboard` port 8765 (`preview_start name="drm-dashboard"`); version.txt must
  equal HTML BUILD ('r11') or it infinite-reloads.

## HELD — USER-GATED (never autonomous)
git push to `main` of either package beyond what's done; Julia registry; CRAN; coordinated tags;
external GitHub posts beyond the (approved) Ayumi reply; filing GLLVM issues; touching the drmTMB HELD files.
Local edits / branch commits / PRs on the user's own repos are fine.
