# HANDOVER — Ayumi σ-phylo REML + missing values (2026-06-12)

> **Read this first** in a new thread, then `git log --oneline -15` on
> `shannon/land-sigma-phylo`, then the named dev-log notes. The session ran very long;
> ALL state is durable in git + these notes. **Branch `shannon/land-sigma-phylo`
> (worktree `~/worktrees/DRM-integrate`); 14 commits; NOTHING pushed.**

## ✅ DONE + VERIFIED — the Ayumi-priority trio (Julia side)

Gaussian σ-phylo location-scale, all reachable via `drm(...)` AND the R-facing `drm_bridge`,
all with regression tests (`test/test_reml_newton_sigma_phylo.jl` 7 testsets +
`test/test_reml_sigma_phylo.jl` end-to-end 12/12):

1. **REML** — `clean-gradient LBFGS` (`_glsp_reml_refit_clean`) is the PRODUCTION default
   (Patterson–Thompson restricted likelihood). FD-REML (`_glsp_reml_refit`) = stable fallback.
2. **Missing RESPONSES** — observed-mask drop (keeps the FULL tree) at the σ-phylo dispatch
   (`src/gaussian_core.jl`). Agent-stress-verified: random 10–95%, fully-missing species,
   NaN==Missing, order-invariant, drm==bridge 100%. Handles Ayumi's "many missing" trait.
3. **Boundary inference** — σ-phylo SD boundary-aware profile CI via `confint(:profile)` AND
   `drm_bridge_inference` (honest `[0, x]` at the variance boundary).

## ✅ R-side gate relax (drmTMB) — done by an agent, NOT yet committed

`~/worktrees/drmTMB-RELEASE` (branch `shannon/RELEASE-drmtmb`): `R/julia-bridge.R` now allows
the σ-phylo Gaussian REML cell (`reml_supported <- gaussian && (!has_phylo || sigma_phylo)`),
plus a new `tests/testthat/test-julia-sigma-phylo-reml.R`.
**⚠ CAUTION:** that worktree has MANY pre-existing uncommitted changes incl. HELD files
(`_pkgdown.yml`, `vignettes/how-fitting-works.Rmd`). Commit ONLY the gate relax + the test
BY NAME (`git add R/julia-bridge.R tests/testthat/test-julia-sigma-phylo-reml.R`); do NOT
touch the held files.

## KEY FINDINGS (durable — don't re-litigate)
- The literal **average-information AI data-quadratic is INVALID** here (â is the shrunk BLUP) —
  proven by a derivation panel.
- The **observed-info Newton** (`_glsp_reml_newton`, experimental) is fast vs FD but **NOT
  faster than clean-LBFGS**; `_glsp_reml_fit` (Newton+polish) is correct but slower. So
  clean-LBFGS stays the default. (Speed trial deferred — see TODO.)
- Two adversarial verification passes found **12 + 1 bugs** (boundary crash, β-coupling bias,
  1e18 penalty→loglik leak) — all fixed + regression-tested.

## TODO — for the new thread

**Finish Ayumi (mostly plumbing, then GATED steps):**
1. Commit the drmTMB R-gate relax (by name, per the caution above).
2. **[GATED — needs user "yes"]** push `shannon/RELEASE-drmtmb` + the DRM.jl branch / registry
   so the bridge is reachable for her.
3. **[GATED]** Post the Ayumi reply (issue #2) — draft below. She wanted "everything at one go";
   it's all there now. Recommend **100 trees**.

**Deferred / future:**
4. **Speed trial** (user-deferred): proper cross-engine bench Julia vs ASReml/glmmTMB once an
   ASReml environment exists. σ-phylo locscale REML has no native R baseline here. bench/ artifacts ready.
5. **GLLVM team notification** [GATED]: share the methods (boundary-inference toolkit; the
   missing-response observed-mask; the REML penalty; the AI-invalid + Newton-not-faster findings).
   Append to `DRM-RELEASE/report/gllvm-cross-pollination-brief.md`; FILE issues only on go-ahead.
6. **Missing PREDICTORS** (the "build all at some point" FIML track) — NOT Ayumi-blocking.

## HELD — USER-GATED (never autonomous)
`git push`; the Ayumi GitHub reply; filing GLLVM issues; touching drmTMB held files. Local
edits / commits / drafts are fine.

## Widget — bring it up in the NEW thread (the user wants the same dashboard)

The live widget is served from `/tmp/drm-dashboard` on **port 8765**. Durable backup committed
to **`docs/dev-log/dashboard/`** on this branch (index.html, sweep.json, status.json,
version.txt, update.sh).

To bring it up in a new thread:
1. If `/tmp/drm-dashboard/` is gone (e.g. machine restarted), recreate it:
   `mkdir -p /tmp/drm-dashboard && cp docs/dev-log/dashboard/* /tmp/drm-dashboard/`.
2. `preview_start` with name `"drm-dashboard"` (`.claude/launch.json` already serves
   `/tmp/drm-dashboard` on 8765; it reuses a running server or starts one).
3. Then `preview_screenshot` to show it.

**Discipline:** keep `sweep.json` current as work proceeds ("update the widget as you go").
**TRAP:** `version.txt` MUST equal the HTML `BUILD` constant (both `r11`) or it infinite-reloads;
only bump `version.txt` when you change the HTML `BUILD`. `sweep.json`/`status.json` are data —
edit freely, they render within ~8s with no reload.

---

## DRAFT — Ayumi reply (issue #2) — HOLD until push + user go-ahead

> Hi Ayumi — good news: the σ-phylo location-scale model with **REML** now works end-to-end,
> including your case of **a trait with many missing entries**, via `engine = "julia"`:
>
> - **REML** for `sigma ~ phylo(1 | species)` (Patterson–Thompson restricted likelihood) — the
>   variance-boundary case (when the phylogenetic σ-signal is weak) is handled honestly.
> - **Missing responses** are dropped row-wise (observed-rows fit, like glmmTMB's default) while
>   the full phylogeny is kept — so species with some missing reps still contribute, and a
>   species with all reps missing simply stays in the prior. Use as much complete data as you have.
> - **Boundary-aware CI** for the phylogenetic σ-SD (an honest `[0, x]` when the signal is weak),
>   via the profile likelihood.
> - On your 50-vs-100 trees question: I'd recommend **100 trees**.
>
> [install / usage snippet once the branch is pushed + DRM.jl is registry-reachable]
