# Parity programme: reader and execution checkpoint

## 1. Goal

Advance the approved Julia–R parity programme (DRM.jl issue563) while preserving
the unresolved protected-core approval and every required programme gate.

## 2. Implemented

Retained the original LSS obligations and refreshed Mission Control. The R article
now has independently reviewed execution evidence in companion commit f3d872cd9.
Consolidated the two Julia first-fit pages into one tutorial plus a preserved
transition URL. Corrected fixture-version and profile-interval claims.

## 3a. Decisions and Rejected Alternatives

Kept the native R and Julia builds isolated. Did not delete dirty worktrees or
stashes, apply the denied source patch indirectly, or infer general parity from
working examples. The installed Vitepress writer cannot handle Documenter's hide
tuple; an unlisted source page preserves the legacy route under the normal full
build instead. Markdown generation is not a finished theme build.

## 4. Files Touched

The three reader pages, docs/make.jl, documentation inventory/pilot tools and
tests, evidence under docs/dev-log/evidence/julia-r-parity, and LOOP/checkpoint.md.
Mission Control was a separate scoped vault edit, locally committed as c32302e.
No Julia src file changed. Unfinished red S5 test files remain outside this commit.

## 5. Checks Run

Rose independently inspected nine executed canonical examples in the two-page
Vitepress Markdown pilot and verified eighteen version-bearing fixture metadata
files (eleven numeric, seven translation). Eight static inventory tests passed.
Final pilot provenance and retained failure logs are in docs-pilot.md. R article
checks cover five converged fits and seven output/runtime/likelihood groups;
their separate one-gate unlazy leaf was actually rerun and passed. Served Mission
Control status/runtime were verified against their configured source.

## 6. Tests of the Tests

Inventory tests damage fixtures and detect mismatches. The first documentation
attempt exposed a full-source scan and a fatal unsupported navigation tuple;
both failures remain evidence, not successful builds. Example failures are fatal
in the bounded pilot. The article runner rejects missing fits and invalid intervals.

## 7a. Issue Ledger

DRM.jl#563 remains open. G0–G8 remain open. No performance denominator, broad
numerical parity, recovery disposition or full-site completion is claimed here.

## 8. Consistency Audit

Rose approved the bounded documentation slice after a rendered summary exposed
unavailable sigma-block z/p values. Prose now explains those values; the missing
post-fit parity remains an obligation. Package version anchors are distinguished
from exact builds, and old timings retain their historical comparator version.

## 9. What Did Not Go Smoothly

Documenter processes more than the pages list unless pagesonly is requested.
The first failed run could not establish an execution-time estimate for the full
site. Later pilots exercise nine real examples. A final prose edit after the v2
pilot means v2 cannot be represented as a render of that final prose.

## 10. Known Residuals

Full Vitepress theme, desktop/mobile/light/dark review, all examples, links and
live deployment remain open. Protected numerical source changes still require
the explicit approval identified by auto-review. Unique worktree/stash recovery,
all missing capabilities, threading, matched benchmarks and final integration
remain outstanding. No Totoro or DRAC compute has been launched.

## 11. Team Learning

Memory receipt: the existing project instructions, retrieved history and current
repositories guided the scope; no Codex memory was written. Golden Set: not run
for this bounded checkpoint. Inspect actual rendered outputs: a successful fit
does not imply every reported inferential field is available.

## 12. Cross-Product Coverage

This does NOT cover whole-programme correctness or performance. It covers the
specified reader-page repairs, bounded executed examples, historical obligation
mapping and verified dashboard refresh. R and Julia source changes beyond these
scopes remain separate slices with their own evidence.
