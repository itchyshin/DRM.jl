# After-task: #136e public-path VA vs Laplace bias report

Date: 2026-08-09 · related #136 (**stays OPEN**) · lane `feat/136e-va-bias-report`

Perspectives: Shannon · Ada · Curie · Fisher · Pat · Rose · Melissa.
No nested subagents.

## 1. Goal

Scoped 136e: ADEMP smoke of public Gamma `(1 | g)` LA vs VA on shape α = 1/σ².
Keep Experimental. Do not close #136. Owner prior: LA often similar/faster/more
accurate in R — treat Julia agreement as a finding.

## 2. Implemented

- `bench/va_vs_laplace_bias.jl` — public `drm` harness, greppable `VA-vs-LA`.
- `report/va-vs-laplace-bias.md` — ADEMP + measured 3-seed smoke + not-ZINB /
  not-7× / ELBO≠logLik.
- Docs honesty: `marginal-la-vs-va.md` (prefer LA on ordinary Gamma RI);
  `capabilities.md` cites the report.
- LOOP kit + check-log + this after-task + Melissa plan-actual.
- No `src/` edits.

## 3a. Decisions and Rejected Alternatives

- Public-honest 136e, not original ZINB + docs-flip (owner lock).
- Interpret LA≈VA as success-aligned with R/TMB, not a failed VA demo.
- Skip n-ladder / Totoro: no material α gap (7× class).
- Rejected: inventing a two-part DGP to chase 7×; comparing ELBO to LA logLik
  as IC; `closes #136`.

## 4. Files Touched

- `LOOP/GOAL.md`, `LOOP/arcs.md`, `LOOP/checkpoint.md`, `LOOP/ultra-plan.md`
- `bench/va_vs_laplace_bias.jl` (new)
- `report/va-vs-laplace-bias.md` (new)
- `docs/src/model-guides/marginal-la-vs-va.md`
- `docs/src/capabilities.md`
- `docs/dev-log/check-log.d/2026-08-09-136e-va-bias.md`
- `docs/dev-log/after-task/2026-08-09-136e-va-bias.md`
- `docs/dev-log/plan-actual/2026-08-09-136e-va-bias.md`
- `docs/dev-log/coordination-board.md`

## 5. Checks Run

```
julia --project=. --startup-file=no bench/va_vs_laplace_bias.jl 3
```

Julia 1.10.0. Log: `SMOKE_OK`. Three seeds, both marginals `conv=true`,
`marginal` tags correct. Warm LA 0.066–0.119 s; warm VA 1.37–1.84 s.
`|α_VA−α_LA|` ∈ {0.0005, 0.0002, 0.0147}. Full `Pkg.test()` not run (no `src/`;
CI ~40–56 min; subset ≠ CI).

## 6. Tests of the Tests

Harness fails loud if α non-finite or a fit errors (no `SMOKE_OK`). Frontend
Gamma seed `202608082` reused so a silent API break would throw in `drm(...)`.
Unit test `test/test_variational_gamma.jl` already required `α_VA ≈ α_LA`
(atol 0.6) — smoke agrees.

## 7a. Issue Ledger

- #136 remains **OPEN**. PR must not say `closes #136`.
- #136e-as-scoped (public Gamma RI report) landed; ZINB / two-part / phylo VA
  still on the epic.
- #49 still parked.

## 8. Consistency Audit

- Experimental banner still on the guide. No “Implemented everywhere.”
- Capabilities absent-list still names phylo/crossed/ZI; 136e row now points
  at the report instead of “report remains open.”
- “VA is steadier on shape” for ordinary RI removed — contradicted by smoke.
- drmTMB sibling not claimed finished. D-111 OFF. No `.worktrees/` staged.

## 9. What Did Not Go Smoothly

- First `/goal` commit was interrupted once; scaffold still landed @ `f7d79e17`.
- First-seed wall times include JIT — report separates warm vs cold.

## 10. Known Residuals

- `n_sim = 3` — no MCSE. Enough to reject 7×; not enough to rank tiny α gaps.
- Seed 3 VA slightly farther from truth than LA — not interpreted as VA-worse
  in general.
- Two-part / ZINB cells still unmeasured on DRM.jl.

## 11. Team Learning

Owner taste mid-loop (“LA preferred in R: faster + more accurate”) matched the
smoke exactly. Write that prior into ADEMP *before* running next time so the
report cannot accidentally sell a VA win the geometry will not produce.

## 12. Cross-Product Coverage

Covers: DRM.jl public Gamma `(1 | g)` LA vs VA smoke + Experimental docs
honesty. Recalled gllvmTMB Design 21 LA-vs-VA trade-off (brain).

This slice does NOT cover: drmTMB `engine="julia"` / Workflow G; GLLVM
Delta-Gamma 7× cell (not re-run, not vendored); ZINB or `zi`/`hu`×RE; phylo /
crossed VA; REML; missing-data (#49); Julia General (D-111); q=4 engine;
full `Pkg.test()` / CI matrix.

## Rose audit (claim-vs-evidence)

| Check | Verdict |
|---|---|
| Claim = public Gamma RI LA ≈ VA on α | **PASS** — Δα ≤ 0.015 vs 7× would be α̂≈0.57 |
| Claim = LA faster warm | **PASS** — 15–20× in log |
| Claim = not ZINB / not two-part / not 7× | **PASS** — explicit in report |
| Experimental held / #136 stays open | **PASS** |
| No src/ / no q=4 / no GPL / no `.worktrees/` | **PASS** |
| ELBO not ranked vs LA logLik | **PASS** — report forbids it |

**Rose verdict: PASS** — scope honest; owner LA prior recorded.

## Memory receipt

Memory receipt: `route.py DRM.jl` LOAD-FIRST (Totoro/DRAC before campaigns;
D-111 OFF; ML default). Simulation-check: n_sim=3 smoke, conclusions not
over-fit. Recalled brain before sister re-scout.

Golden Set: not in scope (no hub-guard regression this report slice).

*Shannon · Ada · Curie · Fisher · Pat · Rose · Melissa.*
