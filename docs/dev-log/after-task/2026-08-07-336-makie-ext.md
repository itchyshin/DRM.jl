# After-task: #336 DRMMakieExt (HSquared-pattern Makie drawing layer)

Date: 2026-08-07 · closes #336

Perspectives: Shannon · Ada · Florence · Pat · Karpinski · Rose · Grace.
No nested subagents (thin conductor).

## Summary

Shipped `DRMMakieExt`: prepare-data stays in `src/visualization.jl`; drawing
lives in `ext/DRMMakieExt.jl` behind Makie + AlgebraOfGraphics **weakdeps**.
Public API: `drm_figure` + thin `plot_profile` / `plot_parameter_surface` /
`plot_corpairs`. Default CI gates the method-less stub only — Makie OUT of CI.

## What landed

- Branch `feat/336-makie-ext` from tip `6c224a6c`
- Plan: `docs/dev-log/plans/2026-08-07-336-makie-ext-ultra-plan.md` (+ `LOOP/`)
- `Project.toml` `[weakdeps]` / `[extensions] DRMMakieExt = ["AlgebraOfGraphics", "Makie"]`; compat Makie `"0.24"`, AoG `"0.13"` (HSquared-aligned)
- `src/plotting_ext.jl` — method-less `drm_figure` + thin `plot_*`
- `ext/DRMMakieExt.jl` — kinds `:profile` (Confidence Eye), `:parameter_surface`, `:corpairs`
- `test/test_makie_ext_stub.jl` wired in `test/runtests.jl`
- Docs honesty: `reference/visualization.md`, `simulation-plot-grammar.md`, `capabilities.md`
- check-log.d + this after-task

## Verify (log — not exit code)

```
julia +1.12 --project=test -e 'using Test; include("test/test_makie_ext_stub.jl")'
→ Test Summary: drm_figure drawing stub … | Pass 8  Total 8  Time 0.1s
→ STUB_TEST_DONE
```

Confirmations inspected:
- `isempty(methods(drm_figure))` true without Makie/AoG
- MethodError on three preparer-shaped NamedTuples
- `plot_*` aliases present as Functions
- `grep`/Project.toml: Makie **absent** from `[deps]` and `test/Project.toml`

Optional local CairoMakie smoke: **not run / not claimed as CI**.

## Inventory of kinds retained

| kind | preparer | drawing notes |
|---|---|---|
| `:profile` | `profile_curve` | Confidence Eye: pale band under cutoff + darker outline + hollow MLE |
| `:parameter_surface` | `parameter_surface` | heatmap + hollow min-deviance marker |
| `:corpairs` | `corpairs_data` | constant hollow point or observation-wise ρ12 path |

## Not covered / deferred

- AoG faceted polish / richer Eye variants (Rung 1 / separate issue)
- DocumenterVitepress gallery rewrite
- Optional local CairoMakie render-proof PNG (opt-in only)
- #136 VA; #49 FIML; R `engine="julia"`; q=4 engine edits
- PR merge (OPEN GATE — owner only)

## Rose audit (claim-vs-evidence)

| Check | Verdict |
|---|---|
| Claim = DRMMakieExt present; drawing opt-in via Makie+AoG | **PASS** — Project.toml + ext file |
| Claim = CI does **not** draw figures | **PASS** — stub-only test; Makie not in test deps |
| Capabilities row no longer overclaims CairoMakie as CI-tested drawing | **PASS** — split data vs stub-tested drawing |
| Confidence Eye named only for `:profile` drawing path / gallery | **PASS** — no CI render claim |
| No `src/` q=4 engine / GPL / `.worktrees/` staging | **PASS** |
| Fence held (#136/#49/R-bridge not started) | **PASS** |

**Rose verdict: PASS** — no overclaim that CI draws figures.

## Melissa

`RECONCILE: N/A` — Arc 0 executed as G0-approved (drm_figure + plot_*; AoG in
weakdeps; Makie OUT of CI). No material plan-vs-actual drift.

*Shannon · Ada · Florence · Pat · Karpinski · Rose · Grace.*
