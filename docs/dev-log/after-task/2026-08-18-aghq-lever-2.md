# 2026-08-18 — AGHQ lever 2: 1-D Liu–Pierce on Poisson `(1 | g)` (#448)

**Lane:** `claude/lane-aghq-lever-2` @ `~/local-scratch/lanes/DRM.jl-aghq-lever-2`
(branch name is `lane_launch.sh`'s fixed prefix; this session is **Cursor Grok**).
**Base:** `origin/main` @ `d04ba994` plus this lane's S0 kit (`0955569f`).
**Persona:** Shannon (conductor / one writer on `src/`); Noether kernel;
Rose claim fence. Two Grok children drafted docs/PR text only — they did
not edit `src/`.
**Closes #448.** Does **not** close #136 / #443 / #49 / #441. Does **not**
merge (maintainer / Noether gate).

## What landed

DRM-native 1-D Liu–Pierce AGHQ around existing `_gauss_hermite`. Opt-in
`marginal = :AGHQ`, `nAGQ = 5` on Poisson `(1 | g)` only. Default `:LA`
stays today's **non-adaptive GHQ-32**. The capability row stays **missing**.

| Change | File | What it does |
|---|---|---|
| `_aghq_require_1d` / `_aghq_1d_logint` / `_poisson_group_aghq_logint` | `src/aghq_1d.jl` | 1-D Liu–Pierce wrap of `_gauss_hermite`. Fail-loud if dim ≠ 1. k=1 ≡ 1-point Laplace. |
| `include("aghq_1d.jl")` | `src/DRM.jl` | After `gaussian_ranef.jl` (owns `_gauss_hermite`), before `poisson.jl`. |
| `struct AGHQ` + `_marginal_method(:AGHQ)` + `_aghq_reject` | `src/variational.jl` | Public symbol beside `:LA` / `:VA`. No silent fallback. |
| `nAGQ` + `(1\|g)` route + fail-louds | `src/poisson.jl` | `_fit_poisson_ranef_aghq`. Rejects phylo / crossed / relmat / `(1+x\|g)` / FE / `:REML`×`:AGHQ`. **Does not edit** `_fit_poisson_general_laplace`. |
| Family rejects | `binomial.jl` / `gamma.jl` / `beta.jl` / `negbinomial.jl` | `marg isa AGHQ && _aghq_reject` so `:AGHQ` cannot fall through to GHQ-32. |
| `marginal` kwarg | `src/associate_pairs.jl` | QuadGK is not AGHQ — reject `:AGHQ`. |
| TDD | `test/test_aghq_1d.jl` + `test/runtests.jl` | Written first (RED: missing symbols). 46 assertions. |

Nothing was vendored from GLLVM `aghq_grid.jl` or drmTMB `R/aghq-coxreid.R`.

## Verification (LOG, not exit code)

Loaded code **is this worktree**:
`pathof(DRM) = /Users/z3437171/local-scratch/lanes/DRM.jl-aghq-lever-2/src/DRM.jl`
(`LOADED=this-worktree`). `git diff` on `src/sparse_laplace_glmm.jl` and
`src/reml_q4.jl` is empty. Capability TSV cell stays `missing`.

TDD: first run of `test/test_aghq_1d.jl` failed with `UndefVarError` for
`_aghq_1d_logint` / `_aghq_require_1d` / `_poisson_group_aghq_logint` (0 pass,
4 fail, 3 error). After the kernel: **9/9**. After S3 wiring: **37/37**
surface tests.

### Kernel smoke (agreement, not recovery)

Tiny Poisson `(1 | g)`, G=8 × 6, seed 448, nll at the **true** θ:

| integral | nll |
|---|---|
| AGHQ k=5 | **73.05425078766923** |
| GHQ-32 (`:LA` rule) | **73.05027860681251** |
| \|Δ\| | **0.003972180856720797** |

### Fitted-surface smoke (agreement, not recovery)

G=12 × 6, seed 44801:

| fit | loglik | `fit.marginal` |
|---|---|---|
| `marginal = :AGHQ, nAGQ=5` | **−100.80748354012172** | `:AGHQ` |
| default / `marginal = :LA` | **−100.80728676421757** | `:LA` |
| \|Δ\| | **1.9677590415767554e-4** | — |

That Mac draw is **not** CI truth. Julia 1 CI (PR #449) measured
`−105.64377` vs `−105.58690` (`|Δ|≈0.0569`) on the same fixture, so the
public-surface fitted-loglik smoke uses `atol = 0.1` (~1.8× that gap).
The tight check remains the kernel nll at fixed θ (`atol = 5e-3`). Do
not claim tighter than evidence.

`coef` / `loglik` of `marginal = :LA` **equals** omitting the keyword
(default GHQ-32 unchanged).

k=1 matched 1-point Laplace to 1e-14 on a shifted Gaussian integrand
(plumbing identity). Fail-loud on a 2-vector `mode` cites dimension and #448.

## Rose claim-vs-evidence

| Tempting claim | Verdict |
|---|---|
| “DRM.jl now has AGHQ” as a capability | **REJECT.** Row stays `missing`. No ADEMP. Plumbing only. |
| k=1 agreement = quadrature works | **REJECT.** k=1 ≡ Laplace is a change-of-variables identity. |
| Fitted nll close to GHQ-32 = recovery | **REJECT.** Agreement of the integral, not β / σ_b recovery. |
| Cite −7.3 / −5.0 / −0.9 as ours | **REJECT.** Those are **drmTMB** `cumulative_logit` (Hopper fence). |
| Cite GLLVM Λ 1.0021 / bimodal median | **REJECT.** Different estimand. Never headline. |
| Relabel GHQ-32 `:LA` as AGHQ | **REJECT.** Public `:LA` on `(1\|g)` remains prior-scaled GHQ-32. |
| QuadGK / VA 12-node / `(1+x\|g)` 12² are AGHQ | **REJECT.** Fail-loud. |
| Flip default to AGHQ or wire `:REML`×`:AGHQ` | **REJECT.** ML default. Restricted×AGHQ is deferred B. |

## DoD (AGENTS.md)

1. Implementation — wired (`include` + Poisson `(1\|g)` dispatch).
2. Tests — `test/test_aghq_1d.jl` in `runtests.jl`; RED then GREEN.
3. Docstrings — `_aghq_1d_logint`, `AGHQ`, Poisson worked example.
4. Worked example — Poisson docstring (`marginal = :AGHQ, nAGQ = 5`).
5. Check-log — `docs/dev-log/check-log.d/2026-08-18-aghq-lever-2.md`.
6. After-task — this file.
7. Rose audit — table above.

## Out of scope (honoured)

- No edit to `_fit_poisson_general_laplace`.
- No q4 / `reml_q4.jl` / #49.
- No steal of #420 / #406.
- No GLLVM `LOOP/GOAL.md`.
- No capability-chip flip.
- No Totoro / Cell D ADEMP.
