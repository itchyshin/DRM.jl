# Finite-state prepared likelihood and compute reconnection checkpoint

## 1. Goal
Continue approved Julia–R parity programme DRM.jl#563. Build the common ordinal/
categorical missing-predictor likelihood dependency; preserve the complete goal.
All programme G0–G8 remain OPEN.

## 2. Implemented
Prepared Gaussian response with one ordinal/categorical predictor; exact finite
sums over state-expanded mean designs, all4missingness masks, stable log
probabilities, raw cutpoint coordinates, level-major nominal coefficients,
posterior probabilities, state-weighted predictions and explicit uncertainty
statuses. Fit snapshots copy mutable model inputs. Module and test runner wired.
Direct formula/R transport for these routes remain required next.

## 3a. Decisions and Rejected Alternatives
Keep native defaults and4e-6 tolerance. No Gaussian substitution for a different
likelihood. Preserve raw covariance; ordinal imputation SD is conditional score
SD without added parameter uncertainty; nominal metric SE remains unavailable.
Do not substitute modal/expected-score designs for state-weighted predictions.
No GPL implementation source copied into Julia; RDS retained only in R repo.

## 4. Files Touched
Terra/high owned new finite kernel/test; root owned module/test wiring, native
exporter/generated numerical fixtures, independent oracles, checks, documentation
and checkpoint. Luna/low filed completed Totoro evidence. Sol/high Rose performed
bounded mathematical/source/evidence review; Terra/high Melissa reconciled scope.
S5test/include, RZOB changes and denied sparse-engine files remain untouched.

## 5. Checks Run
Targeted prepared-kernel and48native fixed-point checks pass. Independent native
finite sums agree within4e-12 likelihood and1.3e-8 gradient. Current prepared fit
receipt retains actual covariance and imputed SDs; independent central-difference
Hessian times V matches identity within2.75e-8 ordinal/1.83e-7 categorical.
Both native-default comparisons FAIL unchanged4e-6: ordinalprediction7.56e-6 and
imputation5.12e-6; categoricaltheta1.74e-5 andprediction9.58e-6. These stay open.
One edited developer page executes3examples; no visual/deployed claim.
Totoro previous source f67eeb80 Linux pilot passed190assertions in67seconds with
Julia1.10.10, oneJulia and oneBLASthread. DRAC Fir connected; no job submitted.

## 6. Tests of the Tests
Initial missing-API test failed before implementation. Native18 and fit17 damaged
receipts fail normally and under Python-O, including covariance1000I, actualSD,
availability, masks, provenance, denominator and dishonest parity status.
Rose found wrong-error refusal tests using internal codes against textlevels;
now valid labels verify the intended intercept-span rejection. Genuine rawcut
-1000, saturated logits and large positive spacing are tested.1e308 arithmetic
repair is verified byinspection, not a specific runtime assertion.

## 7a. Issue Ledger
DRM.jl#563 stays open. No native obligation row, fullS9 or programme gate closes.
The immutable24row initialaudit is untouched. Current progress map records only
the prepared dependency. Approved plan is docs/dev-log/plans/2026-08-30-julia-r-parity.md;
LOOP/ultra-plan.md is unrelated inherited material per LOOP/GOAL.md.

## 8. Consistency Audit
Golden Set: frozen180row native cases/all4masks/3parameterpoints, independent
finite-sum math, currentsource Julia outputs and damaged-result controls.
Rose approves bounded preparedkernel evidence while preserving both fit losses.
Melissa requires all24obligations, direct/R frontends and all original promises.
MissionControl a9b449d updates3fields, verified through servedJSON; lease released.

## 9. What Did Not Go Smoothly
Exporter initially used nonexistent controlfield and matrixgradient transport;
corrected003 preserves earlier attempts. Repeated new-test fixture expectations
were wrong (modal state, likelihood/prior balance, alpha index); corrected from
math, with no loosened assertions. Rose caught missing names, extreme-spacing
arithmetic, wrong-error tests and weak covariance/SD receipt checks. Sandboxcache
EPERM required scoped supervised runtime. Gatewrapper needed explicit checkout
--cwd; failed first attempts retained. No long campaign was launched.
The report structure validator passed, then its CLI unexpectedly began reverifying
all programme ledgers. That process tree was stopped after about3minutes. It
demoted fresh-output checks when existing output paths were reused; the two local
commands now allocate fresh paths on each run and were reverified. This does not
constitute full programme acceptance; older ledgers may be stale/unmet.

## 10. Known Residuals
Prepared defaultfit parity remains failed and earlier singlepredictor failures
remain open. Frontend/bridge for finite-state, full capability/output manifest,
S2/S3/S7/S8/S10/S11, LSSstampedSE/REML/masks/inference-matrix/10k/finalhead, recovery,
all registered warm wins with1/2/4/8policy calibration, worktree recovery/cleanup,
fullsite visual/deployed evidence and finalRose/Melissa are required. No fullsuite,
release, registration, merge/push or collaborator message. Actualagenthours are
not instrumented; no invented total is reported. Prior timing includes startup
and is not a speed benchmark. Completed remote pilot is not finalsource proof.

## 11. Team Learning
Test the exact failure mechanism, not any earlier ArgumentError. Retain actual
output uncertainty instead of recomputing the quantity and mistaking that for
implementation evidence. Check covariance against independently derived curvature.
A reviewed tiny-spacing fix also needs large-spacing and saturated-tail neighbours.

## 12. Cross-Product Coverage
This slice does NOT cover direct ordinal/categorical formula/R bridge admission,
non-Gaussian responses, structured predictor priors, multiple finite predictors,
REML, weights/offset combinations, profile/bootstrap, recovery/coverage, defaultfit
parity, warm-performance or full-site rendering. These stay programme requirements,
not new exclusions. Existing one/two-predictor kernel/frontend/bridge neighbours
pass on current source. Local acceptance:7met,1failed(defaultfit),0abandoned.
