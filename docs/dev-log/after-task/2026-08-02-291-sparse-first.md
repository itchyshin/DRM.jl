# After-task — #291 Arc 2 sparse-first REML characterization

**Date:** 2026-08-02  
**Branch:** `feat/291-reml-sparse-first`  
**Related:** #291 · stacked on Arc 1 tip (PR #362)

## Delivered

1. Report-only structural accounting (`n_outer_phi=11`, `structural_evals_per_gradient_request=23`).
2. `:warm_bidirectional` harness protocol with `timing_class` / `evidence_class`.
3. Framing note: `docs/dev-log/plans/2026-08-02-291-sparse-first-characterization.md`
   (no invented Szymek quotation; H² estimand mismatch + sparse-first framing).
4. Focused contract test: **23/23** pass.
5. Smoke artifact read at `/tmp/reml-sparse-first.md`:
   `evidence_class=warm_comparable`; all timed fits converged;
   intervals `not_evaluated`.

## Warm fixture observation (not a public headline)

Within this p=8/nrep=3 fixture after warmup discard:

| Method | Warm timed seconds (orders 3–6) |
|---|---|
| REML | ≈ 0.86 s (orders 3 and 6) |
| ML | ≈ 0.055–0.064 s (orders 4 and 5) |

Baseline REML remains the slower warm path here (central-FD outer loop), which
is consistent with Arc 1’s structural 23-eval bound. This is **not** an
AI-REML comparison and **not** a registry/speed claim.

## Rose audit — PASS

- No `src/` edit; no `:natgrad` / AI-REML API.
- No invented Szymek quote; premise labeled owner/planning input.
- No 10k / General / Registrator work.
- Timing claims limited to fixture-local warm_comparable class.

## Melissa reconcile

Planned: accounting + warm/order + framing + small-fixture smoke + DoD docs.  
Actual: same; no Sol/Opus fan-out; no heavy compute. Material deviations: none.
