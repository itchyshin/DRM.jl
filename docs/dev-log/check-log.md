# check-log.md — DRM.jl gate status

Canonical per-slice gating status. One row per slice; cite the issue, the
verification command, and the result. This is a **shared resource** — check PR
overlap before editing. See `AGENTS.md` → *Definition of Done*.

| Date | Slice / Issue | Gate run | Result | By |
|---|---|---|---|---|
| 2026-05-30 | Phase 0 scaffold (#2) | `using DRM` load | ✅ loads ("DRM loaded OK"); found a stray load-time print in `sparse_aug_plsm.jl` (→ Phase 1.0) | Shannon |
| 2026-05-30 | Phase 0 scaffold (#2) | `Pkg.test()` | ✅ 13/13 pass (engine loads + phylo foundation) | Shannon |
| 2026-05-30 | Phase 0 scaffold (#2) | `julia --project=docs docs/make.jl` | ✅ 36 pages render (warnonly; 14 docstrings not yet in `@docs`) | Shannon |
| 2026-05-30 | Phase 0 scaffold (#2) | Workflow A + W0 smoke-run | ✅ script format valid; scaffold present | Shannon |
| 2026-05-30 | Phase 0 scaffold (#2) | headline bench logLik −256.51 | ⏭️ NOT re-run — `bench/run_*.jl` needs the Phase-1.0 path fix (HANDOVER §11); engine unchanged so verified number stands | Shannon |
| 2026-05-30 | Slice 1: Gaussian loc-scale (#18) | `Pkg.test()` + docs build | ✅ 17/17 (13 engine + 4 Gaussian recovery); docs `@example` blocks execute, no error markers | Shannon |
| 2026-05-30 | Slice 2: bivariate Gaussian ρ12 | `Pkg.test()` + docs build | ✅ 23/23 (+6 bivariate recovery incl. ρ12); fixed implicit-intercept (`y ~ x` now ⇒ `1 + x`); docs `@example` clean | Shannon |
| 2026-05-30 | Slice 3: random intercept (1\|g) | `Pkg.test()` + docs build | ✅ 27/27 (+4 RE recovery: β, residual σ, group SD via closed-form marginal); `which-scale` `@example` clean | Shannon |
| 2026-05-30 | Slice 6: Wald inference | `Pkg.test()` + docs build | ✅ `stderror` + `confint` (est ± z·se) on all model types; `model-workflow` `@example` clean. (Profile/bootstrap + predict/simulate deferred.) | Shannon |
| 2026-05-30 | Slice 7a: capability map | docs build | ✅ `model-map.md` rewritten to the live Gaussian surface (Stable / Verified engine / Planned); build clean | Shannon |
| 2026-05-30 | Slice (post-fit): fitted + residuals | `Pkg.test()` + docs build | ✅ all green; `fitted`/`residuals` on univariate/bivariate/RE (stored means+obs on DrmFit); `model-workflow` `@example` clean | Shannon |
| 2026-05-30 | Slice 5: meta_V (meta-analysis) | `Pkg.test()` + docs build | ✅ all green; `meta_V(v)` known sampling variances + heterogeneity τ recovered; `meta-analysis` `@example` clean | Shannon |
| 2026-05-30 | Slice: simulate | `Pkg.test()` + docs build | ✅ all green; `simulate(fit)` parametric replicate (univariate + bivariate); refit-recovers-coefs sanity; `model-workflow` `@example` clean | Shannon |
| 2026-05-30 | Slice: independent random slope (0+x\|g) | `Pkg.test()` | ✅ 9 testsets; weighted diagonal-capacitance marginal; fixed slope correctly targets β₁ + realized mean RE (finite-G); variance components recovered | Shannon |
| 2026-05-30 | Slice 4a: structured RE `relmat(1\|id, K)` | `Pkg.test()` + docs | ✅ 10 testsets; closed-form structured GLS (det-lemma + Woodbury, known K); recovers structured SD + residual σ. Engine for animal/phylo/spatial. `relmat-known-matrices` `@example` clean | Shannon |
| 2026-05-30 | Slice 4b: `animal(1\|id, A)` + `phylo(1\|species, tree)` | `Pkg.test()` + docs | ✅ both reuse the structured-GLS engine; phylo K from the verified `sigma_phy_dense`; recoveries pass; `phylogenetic-models` + `animal-models` `@example` clean | Shannon |
| 2026-05-30 | Slice: `bootstrap_ci` | `Pkg.test()` + docs | ✅ parametric bootstrap (simulate + refit percentiles); CIs bracket point estimates + cover truth; 300 refits ~1s | Shannon |
| 2026-05-30 | Slice 4c: `spatial(1\|site, coords)` | `Pkg.test()` + docs | ✅ exponential K(ρ)=exp(-d/ρ), range estimated; β + residual σ recovered, spatial variance detected. **All 4 structured markers done** (relmat/animal/phylo/spatial). `spatial-models` `@example` clean | Shannon |
| 2026-05-31 | Slice: `predict(fit, newdata)` | `Pkg.test()` + docs | ✅ population-level Xβ̂ on new data (univariate + bivariate); in-sample == fitted; formula stored on the fit via default ctor + `_withformula` (no fitter churn) | Shannon |
| 2026-05-31 | Slice: correlated RE `(1+x\|g)` | `Pkg.test()` | ✅ 2×2 block-diagonal capacitance (O(G), explicit 2×2 inv/det/solve, log-Cholesky Σ_re); recovers residual σ + RE SDs; `vc(fit)` accessor | Shannon |
| 2026-05-31 | Slice: multiple crossed RE `(1\|g)+(1\|h)` (#39) | `Pkg.test()` + docs | ✅ all green (17 files, +4 recovery: residual σ + per-grouping SDs); whitened-Woodbury dense capacitance `M=I+Z̃ᵀD⁻¹Z̃`, robust `check=false` factorization + finite penalty (rounding loses I at extreme σ; crossed cols rank-deficient), balanced variance-split init; `model-map` Stable + `@example` | Shannon |
| 2026-05-31 | Slice: profile-likelihood CIs (#38) | `Pkg.test()` + docs | ✅ all green (18 files, +12: profile brackets est, ≈ Wald on μ, default stays Wald); `confint(fit; method=:profile)` inverts LR (re-opt nuisance per fixed value, bracket+bisection); objective `nll` now stored on the fit (`_withnll`, all 8 fitters); verified generic on fixed/RE/bivariate; `model-workflow` `@example` executes | Shannon |
| 2026-05-31 | Slice: σ random effect `sigma ~ 1+(1\|g)` (#40) | `Pkg.test()` + docs | ✅ all green (19 files, +5 recovery: μ, σ-intercept, σ_b); GHQ marginal (32-node Gauss–Hermite per group, Golub–Welsch nodes, logsumexp) — no Laplace/engine surgery; numerics pre-validated in scratch before slice; `re_sd(fit)[:g]`=scale-RE SD; `model-map` Stable + `location-scale` `@example`. **All Gaussian modelling surfaces now Stable.** | Shannon |
| 2026-05-31 | Docs: reflect completed Gaussian surface | docs build | ✅ landing page capability table rewritten (full Gaussian surface = Stable, honest); fixed stale "planned" tags (which-scale `meta_V`, structural-dependence spatial/animal/relmat, get-started understatement, location-scale LRT note); structural-dependence overview filled (4-marker table + links); build clean | Shannon |
| 2026-05-31 | Docs theme: DocumenterVitepress (#31) | local `make.jl` build | ✅ docs.makie.org-style VitePress backend; spike confirmed Node via NodeJS_20_jll (no system install, no CI step); 37 pages build (npm install + vitepress 1.6.4, 3.4s); `node_modules` gitignored, compat pinned `0.3`. Logo/favicon + CairoMakie figures → #32. | Shannon |
| 2026-05-31 | Docs figures: CairoMakie + Confidence Eye (#32) | local `make.jl` build | ✅ render-proof: 2 PNGs render into `figure-gallery` from real fits (location–scale μ±σ(x) ribbon; **Confidence Eye** = pale lens + dark outline + hollow estimate, drawn from profile `confint`); 0 error markers; CairoMakie 0.15 compat pinned. | Shannon |
| 2026-05-31 | Phase 2 family: **Student-t** `Student()` (#6) | `Pkg.test()` + docs | ✅ all green (20 files, +5 recovery: μ, σ, ν via location-scale t = `logpdf(TDist(ν), z) − log σ`, ForwardDiff-safe, no new pkg dep); μ/σ/ν per-parameter formulas, reuses `bf`; Distributions added to test + docs envs; `robust-student` `@example` (df̂≈5.35); model-map + landing Stable. First non-Gaussian family. | Shannon |
| 2026-05-31 | Phase 2 family: **Poisson** `Poisson()` (#6) | `Pkg.test()` + docs | ✅ all green (21 files, +4 recovery: log-λ intercept+slope, counts on response scale); explicit log-pmf `y·logλ − λ − log y!` (no `Distributions.Poisson` in src → no name clash; constant `log y!` offset); log-link mean, integer-count guard; `count-nbinom2` `@example` (rate ratio ≈1.65); model-map + landing Stable. | Shannon |
| 2026-05-31 | Phase 2 family: **NB2** `NegBinomial2()` (#6) | `Pkg.test()` + docs | ✅ all green (22 files, +5 recovery: log-μ + dispersion θ); `Distributions.NegativeBinomial(θ, θ/(θ+μ))`, FD-safe, no new dep; `sigma` slot = dispersion θ (log link), var=μ+μ²/θ; predictors clamped [−20,20] so p∈(0,1) (constructor rejects p≤0 — fix mirrors σ-RE clamp); MoM θ init; `count-nbinom2` `@example` (θ̂≈2.5). | Shannon |
| 2026-05-31 | Phase 2 family: **Beta** `Beta()` (#6) | `Pkg.test()` + docs | ✅ all green (23 files, +5 recovery: logit-μ + precision φ); `Distributions.Beta(μφ,(1-μ)φ)` qualified (no clash); logit-link mean, `sigma` slot = σ with drmTMB mapping **φ=1/σ²**; predictors clamped so μ∈(0,1)/φ>0; MoM φ init; (0,1) response guard; `proportion-beta-binomial` `@example` (φ̂≈15). | Shannon |
| 2026-05-31 | Phase 2 family: **Gamma** `Gamma()` (#6) | `Pkg.test()` + docs | ✅ all green (24 files, +5 recovery: log-μ + shape α); `Distributions.Gamma(α, μ/α)` qualified, FD-safe, no new dep; log-link mean, `sigma` slot = CV → shape **α=1/σ²**, var=μ²σ²; positive-response guard, MoM α init; `distribution-families` guide filled (α̂≈8). **Caught + fixed a VitePress dead-link** (`[bf](@ref)` unresolved → build fail; `warnonly` covers Documenter not VitePress). | Shannon |
| 2026-05-31 | Phase 2 modifier: **`zi`** zero-inflation (#6) | `Pkg.test()` + docs | ✅ all green (25 files, +8: ZIP + ZINB recovery of μ + π); `zi ~ …` (logit link) detected in `Poisson`/`NegBinomial2` drm → mixture `P(0)=π+(1-π)P_c(0)`, `P(k>0)=(1-π)P_c(k)`; stable `_log_logistic`/`_logaddexp` helpers, λ-from-nonzeros init; `count-nbinom2` `@example` (π̂≈0.40). | Shannon |
| 2026-05-31 | Phase 2 family: **LogNormal** `LogNormal()` (#6) | `Pkg.test()` + docs | ✅ all green (26 files, +5 recovery: log-scale mean + SD of log y); reuses the Gaussian loc-scale objective on `log y` + the −log y Jacobian (no `Distributions` needed); `μ`=mean of log y, `σ`=SD of log y; positive-response guard; `distribution-families` `@example` (≈0.5, 0.4). Also verified the **rendered VitePress site** locally (landing + theme shown to maintainer). | Shannon |
| 2026-05-31 | Phase 2 modifier: **`hu`** hurdle (#6) | `Pkg.test()` + docs | ✅ all green (27 files, +8: hurdle-Poisson + hurdle-NB recovery of μ + hurdle π̂); `hu ~ …` (logit) detected in `Poisson`/`NegBinomial2` drm → two-part `P(0)=π`, `P(k>0)=(1-π)P_c(k)/(1-P_c(0))` [zero-truncated]; new stable `_log1mexp` helper; `zi`+`hu` mutually-exclusive guard; `count-nbinom2` `@example` (π̂≈0.41). | Shannon |
| 2026-05-31 | Phase 2 family: **Truncated NB2** `TruncatedNegBinomial2()` (#6) | `Pkg.test()` + docs | ✅ all green (28 files, +5 recovery: log-μ + dispersion θ from zero-truncated draws); `P(k)=NB(k)/(1−NB(0))` reusing `_log1mexp` + NB machinery; same μ/θ params as NB2, positive-count (≥1) guard; `count-nbinom2` `@example` (θ̂≈3.45). | Shannon |
| 2026-05-31 | Release: **v0.1.0** consolidation | `Pkg.test()` + Rose audit | ✅ 28 files green (re-run on release branch); version `0.1.0-DEV→0.1.0`; NEWS + README status rewritten to the real surface (8 families + zi/hu + full Gaussian + webpage); Rose audit — no family-parity overclaim (recovery-tested + syntax-mirror; #17 gate pending), engine claims HANDOVER-verified, MIT/no-GPL clean; after-task report filed. | Shannon |
| 2026-05-31 | Family: **Beta-binomial** `BetaBinomial()` (#6) | `Pkg.test()` + docs | ✅ all green (29 files, +5 recovery: logit-μ + precision φ); **two-column `cbind(s,f)` response** (drmTMB-exact) via a `cbind` formula marker + a `response2` field on `DrmFormula` (transparent 2-arg ctor; no regression); `Distributions.BetaBinomial(n, μφ, (1-μ)φ)` qualified; `φ=1/σ²`; verified `cbind` LHS parsing in a scratch first; `proportion-beta-binomial` `@example` (φ̂≈11.9). Post-v0.1.0 (version → 0.1.1-DEV). | Shannon |
| 2026-05-31 | Family: **Zero-one-inflated beta** `ZeroOneBeta()` (#6) | `Pkg.test()` + docs | ✅ all green (30 files, +6 recovery: logit-μ + φ + zoi + coi); drmTMB `zero_one_beta` mixture P(0)=zoi(1−coi), P(1)=zoi·coi, (1−zoi)·Beta(μ,φ); params mu/sigma/zoi/coi (verified names+structure vs drmTMB ref); fitted = unconditional mean; `[0,1]` response guard; `proportion-beta-binomial` `@example` (ẑoi≈0.24). Fixed a VitePress dead-link (`[0,1](…)` adjacency → backticked). | Shannon |
| 2026-05-31 | Family: **Tweedie** `Tweedie()` (#6) | `Pkg.test()` + docs | ✅ all green (31 files, +6 recovery: log-μ + φ=σ² + **estimated power p** ∈(1,2)); compound Poisson–Gamma **Dunn–Smyth series** (prototyped + validated first: β̂/φ̂/p̂ spot-on); `SpecialFunctions.loggamma`, detached series-window + Dual sum (FD through `loggamma(-jα(p))`); `nu` logit-(1,2) link, clamped to avoid the boundary singularities; semicontinuous (zeros allowed) guard; `distribution-families` `@example` (p̂≈1.48). +SpecialFunctions dep (re-resolved docs env). | Shannon |
| 2026-05-31 | Family: **Cumulative-logit** `CumulativeLogit()` (#6) | `Pkg.test()` + docs | ✅ all green (32 files, +4 recovery: slope + reconstructed cutpoints); ordinal `Pr(y≤k)=logistic(θ_k−η)`, K−1 cutpoints kept ordered by increment param θ_k=θ_{k-1}+exp(δ_k); intercept dropped (identifiability); `fitted`=expected category score; `distribution-families` `@example` (slope≈0.85). **All 13 drmTMB families now Stable (family parity complete).** | Shannon |
| 2026-05-31 | Release: **v0.1.1** — family parity complete | `Pkg.test()` + Rose audit | ✅ 32 files green; version `0.1.1-DEV→0.1.1`; NEWS v0.1.1 section (beta-binomial, ZOIB, Tweedie, cumulative-logit — all 13 drmTMB families); no overclaim (recovery-tested, #17 parity gate pending). | Shannon |
| 2026-05-31 | RE on non-Gaussian: **Poisson `(1\|g)`** count GLMM (#39-follow) | `Pkg.test()` + docs | ✅ all green (33 files, +5 recovery: β + σ_b); random intercept on log λ integrated out per group by 32-node GHQ (reuses `_gauss_hermite`); `drm(::Poisson)` now splits the mean RHS + routes a single `(1\|g)`; `re_sd(fit)[:g]`; rejects combo with zi/hu; `count-nbinom2` `@example` (σ̂_b≈0.60). First non-Gaussian random effect. | Shannon |
| 2026-05-31 | RE on non-Gaussian: **NB2 `(1\|g)`** count GLMM | `Pkg.test()` | ✅ all green (34 files, +5 recovery: β slope, dispersion θ, σ_b); random intercept on log μ via 32-node GHQ (dispersion θ fixed); `drm(::NegBinomial2)` splits mean RHS + routes `(1\|g)`, rejects RE on non-mean params + combo with zi/hu; `re_sd(fit)[:g]`. | Shannon |
| 2026-05-31 | RE on non-Gaussian: **Beta + Gamma `(1\|g)`** GLMMs | `Pkg.test()` | ✅ all green (36 files, +8 recovery: β + dispersion + σ_b each); random intercept on the logit/log mean via 32-node GHQ (φ resp. α a fixed effect); `drm(::Beta)`/`drm(::Gamma)` split mean RHS + route `(1\|g)`, reject RE on non-mean params; `re_sd(fit)[:g]`. Random-intercept GLMMs now on all 4 main non-Gaussian families. | Shannon |
| 2026-05-31 | Deeper non-Gaussian RE: **Poisson `(1+x\|g)`** correlated slope | `Pkg.test()` | ✅ all green (template validated, +4 recovery: slope + 2×2 Σ via `vc`); **2-D Gauss–Hermite tensor grid** (12² nodes) per group integrates the correlated (b0,b1)~N(0,Σ); log-Cholesky Σ (`:recov` block → `vc(fit)`); factorises across disjoint groups (no full Laplace). Template for the NB2/Beta/Gamma slope fan-out (parallel subagents). | Shannon |
| 2026-05-31 | Deeper non-Gaussian RE: **NB2 + Beta + Gamma `(1+x\|g)`** slopes (parallel) | `Pkg.test()` | ✅ all green (+12: each recovers slope + 2×2 Σ via `vc`); 2-D Gauss–Hermite tensor grid per family (mirroring the Poisson template), `:recov` log-Cholesky block. **Implemented by 3 parallel subagents in isolated worktrees**, cherry-picked + integrated; full suite confirms no regression. Correlated random slopes now on all 4 non-Gaussian families. | Shannon |
| 2026-05-31 | Deeper non-Gaussian RE: **Student-t + LogNormal + Beta-binomial `(1\|g)` & `(1+x\|g)`** (parallel) | `Pkg.test()` | ✅ all green (+30 recovery: each family recovers intercept σ_b + correlated 2×2 Σ via `vc`/`re_sd`); 32-node GHQ intercept + 12²-node 2-D GHQ correlated slope (log-Cholesky `:recov`) on the mean — Student-t (μ identity, σ/ν fixed), LogNormal (Gaussian-on-log-y + −log y Jacobian), Beta-binomial (logit μ, `cbind` response, φ fixed). **Implemented by 3 parallel subagents in isolated worktrees**, cherry-picked + integrated; full suite confirms no regression. RE now on **all 7** non-Gaussian families. Also scoped the `bootstrap_ci` model-map row to Gaussian (Rose: implicit overclaim — `inference.jl:128` dispatches `::Gaussian` only). | Shannon |
| 2026-05-31 | **Binomial** logistic family + `(1\|g)` GLMM, and **`summary`/`coeftable`** post-fit accessor (parallel) | `Pkg.test()` | ✅ all green (+36: Binomial cbind + Bernoulli logit-β + logistic-GLMM σ_b recovery; `summary` show + `coeftable` across all blocks); `Binomial()` is mean-only logit link (no dispersion, like Poisson), `cbind(s,f)` or 0/1 response, 32-node GHQ random intercept (`re_sd`); `Base.show(::DrmFit)` + `coeftable(fit)` per-block Wald table (Estimate/SE/z/p + CI; dependency-free + StatsAPI). **Written by 2 parallel subagents in isolated worktrees; integrated + verified centrally** — the agents stalled at their standalone-test gate, so I pulled their files in, merged the two `src/DRM.jl` include/export edits by hand, wired `runtests.jl`, and ran the full suite. Closes the audit's top family + post-fit parity gaps. | Shannon |
| 2026-05-31 | **bootstrap_ci on non-Gaussian families** (#72) | `Pkg.test()` (worktree) | ✅ green (+7: Poisson bootstrap covers truth, Gamma brackets β, `simulate` draws valid counts); generalised `simulate(fit)` to draw from the fitted distribution for **Poisson · NB2 · Beta · Gamma** (μ from `means[:mu]`; dispersion from the constant `sigma` block via `_sigma_scalar`), and added a family-agnostic `bootstrap_ci(formula, family)` method (no structured kwargs; Gaussian method unchanged). Resolves the Gaussian-only overclaim. Student-t/LogNormal/Binomial(needs trials)/Tweedie/ordinal error clearly → follow-up. | Shannon |
