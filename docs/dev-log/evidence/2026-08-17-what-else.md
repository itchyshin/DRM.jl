# 2026-08-17 — what else while Option A waits (#423+#428 DIRTY)

**Lane:** `docs-what-else-menu` (read-only except this note).
**Author:** Ada (Shannon speaking). **No nested Task subagents.**
**Platform:** Cursor Grok. **Did not claim** `claude/lane-arc1-backlog-after-434`,
`claude/lane-gaussian-phylo-mean`, leftover `#434` worktree, `#423`/`#428`
files, or `#406` coordination-board.
**Preflight:** `FOREIGN LANE ACTIVE (claude direct-to-main)` · 10 live lanes.
**Checkout:** leftover Dropbox `docs/a3c-design` — do not build here.

**Recommend:** **WAIT.** No honest motion that is not waiting, stealing, or a
new named G0.

---

## Live state (this pass)

| Fact | Evidence |
|---|---|
| `origin/main` tip | `2c8d3377` Merge #421 (after #438 `8240bdfa`, #436 `6cd59150`) |
| #438 | **MERGED** 13:45Z — `gaussian_phylo_mean` Route A fixture; claim **partial**; no `runtests.jl` |
| #436 | **MERGED** 12:52Z — Arc 1 backlog refresh after #434; **no** new recommended implement |
| #434 | **MERGED** — `biv_q4_phylo_reml` fixture banked; claim **partial** |
| #425 | **MERGED** — cleared `runtests.jl` ownership |
| #421 | **MERGED** — rosetta corpair docs |
| #423 | **OPEN · DIRTY · CONFLICTING** · `test (1)` **FAIL** · `test (1.10)`+docs PASS |
| #428 | **OPEN · DIRTY · CONFLICTING** · CI green · auto-merge **null** (UNARMED) |
| #429 | OPEN · MERGEABLE · stacked on A8 — skip (MC) |
| #420 | OPEN · DIRTY — `LOOP/checkpoint.md` only |
| #406 | OPEN · DIRTY · `test (1.10)` FAIL — owns coordination-board |
| Option A wait-gate | `#423` + `#428` still own `test/runtests.jl` |
| Campaign G0 | 2026-08-14 admit-what-R-fits. Claim stays **partial**. No TSV `supported`. |
| MC `drmTMB.json` | already says wait #423+#428; surface ladder UNCHANGED; fixtures do **not** retally julia-surface |

Hopper late pick (`gaussian_phylo_mean` Route A) **already shipped** as #438
(`closes #437`). Overnight Ada “no remaining inventory `fixture-gap`” and
Hopper’s artefact-NONE were different taxonomies; both are now closed.

---

## A) Wait / poll only — honest idle

**Yes.** This is the honest default.

Option A = include standalone fixtures in `test/runtests.jl` after the owners
merge. Wait-gate is still `#423` + `#428`. Do not open an include PR. Do not
steal the file. MC `next_safe_action` already: *Wait #423+#428 to merge (both
DIRTY; do not steal runtests.jl). Then open include PR for biv_q4_phylo_reml +
gaussian_phylo_mean.*

Polling is idle, not work.

---

## B) Option A include — NO until 423+428

**NO.** Same lock as overnight handover + #436 + #438 after-task + MC.

`#425` cleared. `#423` and `#428` still own `test/runtests.jl`. Include of
`test/test_parity_biv_q4_phylo_reml.jl` and
`test/test_parity_gaussian_phylo_mean.jl` stays deferred until both merge, or
Shinichi names a G0 that **explicitly** steals that file.

---

## C) Next same-target fixture — **0** honest cells

Name **0**. (Cap was 0–2 that are not engine ports / TSV flips / #49 /
`sigma ~ phylo` / steal #428.)

After #434 + #438 the cheap same-target holes on already-implemented engines
are empty.

| Candidate | Why not |
|---|---|
| `biv_q4_phylo_reml` | #434 banked. Claim stays partial. |
| `gaussian_phylo_mean` Route A | #438 banked. Claim stays partial. Do not widen to `sigma ~ phylo`. |
| `base_gaussian_location_scale` / `biv_gaussian_residual` / `plain_binomial_nonphylo` | Phase 1.5 admitted; Workflow G `expected.toml` **exists**. Promotion = drmTMB TSV. |
| `phylo_gamma_beta_binomial` | Comparator **exists** (A5). `next_action` evidence-complete. Do not invent binomial+`phylo`. |
| `phylo_count_large_p` | Smoke-only. **NONE** large-p same-target, but TSV says keep / do not promote. Not Mac-easy. |
| `general_covariance_structured` | A9 gate-compare **done**. Do not invent beta+`relmat`. |
| `gaussian_response_mask` | Twin YES for `miss_control`, but **#49 PARKED**. |
| `cross_family_latent` | **#428 owns.** Steal. |
| `engine_control_surface` | Design fence. Leave `unsupported`. |

#436 ordered backlog (`origin/main:docs/dev-log/evidence/2026-08-16-arc1-ordered-backlog.md`)
already: *no remaining fixture-gap implement; this refresh does not name a new
recommended implement.* Row 8 still says “no Workflow G phylo `expected.toml`”
— still **true** (#438 lives outside that glob). It does not claim Route A is
NONE. A second “#438 footnote” refresh is optional busywork, not owed.

---

## D) Unpark engine / Arc1′ chips — STOP unless named G0

Two tallies must not be mixed.

**File-literal `missing` in `docs/design/capability-status.md` (4 rows):**

| Row | Status | Why not today |
|---|---|---|
| AGHQ adaptive-quadrature | `missing` | New estimator. See D-list below. |
| Cross-family bivariate | `missing` | **#428 owns.** Merge would move this card. Steal / Option B. |
| Missing-response (native) | `missing` | **#49 PARKED.** Listwise ≠ FIML. |
| Missing-predictor `mi()` | `missing` | **#49 PARKED.** |

Snapshot line “37 implemented, 1 rejected, 1 planned, 4 missing” is **stale
arithmetic**: the table has **2** `rejected` (ordinary-RE REML + `:natgrad`).
Not today’s motion; do not flip chips to “fix the count.”

**MC / user D-list (julia-surface Arc1′ fences).** MC operator tip names
“the 4 missing (ordinary-RE REML, :natgrad, AGHQ, HSquared accessors) and VA
planned.” That label does **not** match the file: REML-RE and `:natgrad` are
`rejected`; heritability accessors are `implemented`. Fixtures #434/#438 and
docs #436 **do not retally** julia-surface cards (MC `do_not_repeat`).

| Chip | Scoreboard B now | Move julia-surface cards? | Cost | Stop |
|---|---|---|---|---|
| **VA / #136** | `planned` (public Experimental `(1\|g)` on 5 families; `_fit_va` still errors for unwired; two-part / ZI×RE deferred) | **NO** unless a new engine+test lands and someone edits `capability-status.md`. Fixtures do not retally. TSV/A: **NO** (no ledger row). | Large. Noether. Remaining scope ≠ “close the issue.” | **STOP** unless named G0. GitHub #136 is **CLOSED** (events: 2026-08-11 and again 2026-08-17 13:45:12Z, same second as #438 merge; #438 body said “#136 stays OPEN”). Campaign + MC + capability-status still treat VA as **not done**. Do not treat the close as a ship. |
| **`:natgrad`** | `rejected` (#13 CLOSED; measured FAIL −259.80 vs −256.51) | **NO** — cannot honestly flip `rejected` → `implemented`. | Already paid. Gate failed. | **STOP.** |
| **AGHQ** | `missing` | **YES** on B *if* built+tested. TSV/A: **NO**. | New marginal estimator. Weeks. Noether + maintainer. | **STOP** unless named G0. Not this campaign. |
| **REML-RE** (ordinary Gaussian mean RE) | `rejected` (guard in `src/gaussian_core.jl`) | **YES** on B *if* the guard is replaced by a real REML path + tests. TSV/A: **NO**. | Engine. Conflicts with “ML is the default” only if someone sells it as the default. | **STOP** unless named G0. |
| **HSquared** | Accessors `heritability` / `repeatability` / `icc` already **`implemented`** (`src/heritability.jl`, `test/test_heritability.jl`). HSquared **AI-REML** is a sister-package claim, forbidden as this twin (Rose next-after-biv fence). | **NO** — accessors already green; AI-REML is not a capability-status row and must not be minted to fill a card. | Wrong campaign. | **STOP.** |

Unparking any of these does **not** clear Option A and does **not** change TSV
`claim_status`.

---

## E) Help #423 `test (1)` FAILURE without stealing?

**Report-only diagnosis is legal. Implementing the fix is a steal. Recommend
don’t** (leave it A8’s) unless Shinichi names a G0 to comment or take the PR.

CI run `31945632707` job `95161040362` (Julia **1.12.7**, 2026-08-16):

- File (PR-only; **not** on `origin/main`): `test/test_meta_vcov_bivariate.jl:142`
- Testset: `bivariate known-V engine (A8)` → `residual rho12 is separated from the sampling correlation`
- Assertion: `abs(ρ̂ - fx2.residual_rho) < 0.15`
- Evaluated: **`0.18291728402039295 < 0.15`**
- Suite: 8 pass / **1 fail** / 0 error
- `test (1.10)` **PASS**; docs **PASS**

This is A8’s own recovery tolerance on Julia 1.12 RNG, not a missing engine and
not a `runtests.jl` merge conflict. A comment that quotes the 0.183 vs 0.15
number does not steal files. A push that loosens the tol, reseeds, or rebases
`feat/a8-biv-meta-vknown` **does** steal. Overnight + MC: *#423 Julia 1.12 fail
is A8’s unless the owner says otherwise.* A comment does **not** clear the
wait-gate until A8 rebases the DIRTY PR and CI goes green.

---

## F) Docs / MC hygiene already done?

**Yes, for the owed slices.**

| Slice | State |
|---|---|
| Arc 1 backlog refresh after #434 | **#436 MERGED** — stale NONE gone; no new recommended implement |
| Hopper Route A fixture | **#438 MERGED** |
| Rosetta corpair | **#421 MERGED** |
| MC `drmTMB.json` | Already: wait #423+#428; surface ladder UNCHANGED; do not treat 39/2/1/4 cards as stale because fixtures merged |
| Overnight docs-only `/goal` | Executed as #436 |
| Coordination-board tip | Stale (`d0fac9d7`) — **#406 owns** that file. Do not steal. |
| capability-status snapshot count | Pre-existing 1-vs-2 `rejected` arithmetic. Not a card flip. |

A third docs PR that only footnotes “#438 banked Route A” would repeat #436’s
shape without changing the wait-gate or julia-surface tally. Not honest motion.

---

## Recommendation

**WAIT.** If Shinichi wants motion that is not waiting: there is **no** honest
implement cell. The only cheap legal extra is a **report-only** #423 comment
(E) — still A8’s until he names that G0, and it does not unlock Option A.

Do not: include `runtests.jl`; re-arm #428; flip TSV; unpark #49 / VA / AGHQ /
REML-RE / `:natgrad` / HSquared AI-REML; invent a 12th ledger row; treat #136
CLOSED as VA shipped.

---

## Sources (do not re-derive)

- Dropbox `docs/dev-log/after-task/2026-08-17-overnight-handover.md`
- `origin/main` #436 after-task + `docs/dev-log/evidence/2026-08-16-arc1-ordered-backlog.md`
- `origin/main` #438 after-task `2026-08-17-gaussian-phylo-mean-fixture.md`
- Dropbox `docs/dev-log/evidence/2026-08-16-next-after-biv-{hopper,rose,shannon}.md`
- `origin/main:docs/design/capability-status.md`
- `~/shinichi-brain/Shinichi/Dashboards/mission-control/live/status/drmTMB.json`
- `gh pr view` #423/#428/#436/#438; `gh run view` 31945632707 job 95161040362
- `lane_preflight.sh` this pass (FOREIGN claude; #421 no longer open)

*Written 2026-08-17 America/Denver. Read-only menu. No `/goal`. No PR.*
