# Arc 1 recon S1 — Phase 1.5 admitted trio (2026-08-16)

**Role:** Hopper (read-only). **Compute:** n/a. No Julia, no Rscript, no `Pkg.test`.
**Lane:** scratch `DRM.jl-catchup` evidence only. Did not touch Dropbox `src/`, `test/`, `LOOP/`, or open-PR files.
**STATE:** `PLATFORM: cursor | ON BRANCH: docs/a3c-design (Dropbox leftover; unused) | LANE: arc1-recon-s1`
**OTHER LANES:** #429 A12 · #428 A11 · #425 A10 · #423 A8 · #421 rosetta · #420 LOOP · #406 board · main-direct

Working class for all three: **TSV-claim** (already Phase 1.5 admitted). Not a missing engine. Promoting any row is a drmTMB TSV / claim decision (STOP GATE), not a DRM.jl export.

`claim_boundary` / `next_action` quoted from `git show origin/main:inst/extdata/julia-capabilities.tsv` (drmTMB **0.7.0** @ `097bed1e2`). Twin verdict from scratch `2026-08-16-arc1-hopper-twin-map.md`. Collision from scratch `2026-08-16-arc1-lane-collisions.md`.

---

## 1. `base_gaussian_location_scale`

| Field | Value |
|---|---|
| status | `partial` |
| claim_boundary | "Phase 1.5 Hopper admitted cell (Route C): offline result-shape + optional live TMB parity; CRAN readers still use TMB  -  vignette keeps Julia deferred/experimental." |
| next_action | "Keep coefficient and likelihood parity tests tied to exact bridge payloads. Coefficient/logLik parity re-measured 2026-08-15 against DRM.jl (coef 4.564e-06, logLik 4.584e-09, tol 1e-4); see DRM.jl docs/dev-log/evidence/parity-fixtures.tsv." |
| fixture | `docs/dev-log/evidence/parity-fixtures.tsv` (row `base_gaussian_location_scale`, PARITY_PASS) · Workflow G `test/parity/fixtures/gaussian-locscale/expected.toml` (drmTMB **0.6.0**, #392 split — campaign anchor is 0.7.0) · live Route C in drmTMB `tests/testthat/test-julia-tmb-parity.R` (skip-guarded) |
| twin | **YES** (`drmTMB()` + `bf(y ~ x, sigma ~ z)` + `gaussian()`; vignettes `location-scale` / `location-scale-scale` / `drmTMB`) |
| collision | None of the open PRs own this ID. Soft: do not edit `#423`'s `tools/parity_ledger.py`. Do not flip `claim_status`. |

---

## 2. `biv_gaussian_residual`

| Field | Value |
|---|---|
| status | `partial` |
| claim_boundary | "Phase 1.5 Hopper admitted cell (Route B): residual rho12 result-shape + optional live logLik parity; not a phylo or cross-family claim." |
| next_action | "Keep residual rho12 result-shape and Route B parity tests; do not promote beyond experimental." |
| fixture | Workflow G `test/parity/fixtures/gaussian-bivariate-rho12/expected.toml` (drmTMB **0.6.0**) · live Route B in drmTMB `tests/testthat/test-julia-tmb-parity.R` · Julia result-shape `test/test_bridge.jl`. **Not** a row in `parity-fixtures.tsv`. |
| twin | **YES** (`biv_gaussian()` + `rho12()` + `bf(mu1=, mu2=, sigma1=, sigma2=, rho12=)`; vignette `bivariate-coscale`) |
| collision | Not `#428` (that owns `cross_family_latent`, not residual rho12). Soft: do not widen this row into phylo / xfam. Do not flip `claim_status`. |

---

## 3. `gaussian_phylo_mean`

| Field | Value |
|---|---|
| status | `partial` |
| claim_boundary | "Phase 1.5 Hopper admitted cell (Route A): first phylo-mean (sigma ~ 1) marshalling/result-shape + optional live TMB parity; not loc-scale phylo or non-Gaussian phylo." |
| next_action | "Keep first phylo-mean result-shape and Route A parity tests; do not widen to sigma-phylo here." |
| fixture | **NONE** as a committed same-target `expected.toml` / `parity-fixtures.tsv` row. Live Route A exists in drmTMB `tests/testthat/test-julia-tmb-parity.R` (skip-guarded); Julia marshalling in `test/test_bridge.jl`. No `test/parity/fixtures/gaussian-phylo*` dir. |
| twin | **YES** (`phylo()`; vignettes `phylogenetic-models` / `structural-dependence`; capability-and-limits lists Gaussian phylo `mu` as inference-ready) |
| collision | None of the open PRs own this ID. Adjacent only: `#425` owns binomial/sparse Laplace, not this Gaussian mean cell. Do not widen to `sigma ~ phylo(...)`. Do not flip `claim_status`. |

---

## S1 summary (for Ada S5)

All three are **TSV-claim / keep-tests**, not first-implement candidates. Fixtures already exist for rows 1–2 (Workflow G + row-1 same-target TSV). Row 3 has live Route A only — committed same-target fixture is **NONE**. Rose fence: cell evidence ≠ TSV flip; COUNTDOWN 0 is export-name honesty, not a public twin claim.

Sources: scratch `2026-08-16-arc1-eleven-rows.md`, `2026-08-16-arc1-rose-fence.md`, `2026-08-16-arc1-hopper-twin-map.md`, `2026-08-16-arc1-lane-collisions.md`; Dropbox `docs/dev-log/after-task/2026-08-16-ultra-plan-arc1.md`; TSV via `git show` (not `parity_ledger.py` countdown — row fields already on disk).
