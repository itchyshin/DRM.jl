# Arc 0 smoke — #370 drm_bridge fixtures

Date: 2026-08-03 · branch `feat/370-bridge-fixture-parity` @ post-rebase `379890a`

## Method

For each cohort fixture: `drm_bridge(; formula, family, data)` from
`expected.toml` / `data.csv`, then name-matched coef + loglik vs
`ParityExpected` (default coef bar).

## Results (pre-harness)

| Cell | Status | Notes |
|---|---|---|
| gaussian-locscale | OK | coef+loglik |
| gaussian-bivariate-rho12 | OK | keyed mu1/mu2/… formula parses |
| robust-student | numeric_fail (pre-fix) | `nu_(Intercept)` Δ≈1.06 — stale fixture transform to `log(ν)` while DRM.jl uses `log(ν−2)` (same as drmTMB). Native `compare_fit` failed identically → **not bridge-specific**. |
| count-nbinom2 | OK | |
| proportion-beta | OK | |
| meta-analysis-V | OK | `meta_V(v)` string parse through bridge OK |

## Risks carried into Arc 1–2

1. **Student fixture scale drift** (highest) — fix by undoing transform; modest
   `[tol]` for residual optimizer soft-diff.
2. meta_V — smoke-cleared.
3. xfam — OUT; skip in native runner so incomplete TOML does not error the suite.

## Compare-path gap confirmed

`compare_fit` requires a DRM.jl fit object; bridge returns a flattened Dict →
need `compare_bridge` (Arc 1).
