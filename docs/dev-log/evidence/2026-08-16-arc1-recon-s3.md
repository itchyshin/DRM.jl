# Arc 1 recon S3 — experimentals minus `#428` (rows 7–9)

**Role:** Hopper (read-only). **Compute:** n/a. No Julia, no Rscript, no `Pkg.test`.
**Lane:** scratch `DRM.jl-catchup` evidence only. Did not touch Dropbox `src/`, `test/`, `LOOP/`, or open-PR files.
**STATE:** `PLATFORM: cursor | ON BRANCH: docs/arc1-inventory (scratch) | LANE: arc1-recon-s3`
**OTHER LANES:** #429 A12 · #428 A11 · #425 A10 · #423 A8 · #421 rosetta · #420 LOOP · #406 board · main-direct

`claim_boundary` / `next_action` quoted from `git show origin/main:inst/extdata/julia-capabilities.tsv` (drmTMB **0.7.0** @ `d9fddfa28`; TSV **unchanged** vs eleven-rows tip `097bed1e2`). Twin verdict from scratch `2026-08-16-arc1-hopper-twin-map.md`. Collision from scratch `2026-08-16-arc1-lane-collisions.md`.

**Skipped:** `cross_family_latent` — **owned** by `#428` A11. Documented in S4. Do not steal.

Class vocabulary used here: `smoke-only` · `TSV-claim`.

---

## 7. `phylo_count_large_p` · class **`smoke-only`**

| Field | Value |
|---|---|
| status | `experimental` |
| route / syntax | `phylo` · `poisson()` / `nbinom2()` with `phylo(1 \| group, tree = tree)` |
| claim_boundary | "Large-p phylogenetic random-intercept route; Workflow G FE count cells also route via live expected.toml parity (#499)." |
| next_action | "Keep phylo count smoke + Workflow G FE parity tests; do not promote beyond experimental." |
| fixture | **NONE** as a committed same-target phylo-count `expected.toml` / `parity-fixtures.tsv` row. Workflow G FE (not phylo): `test/parity/fixtures/count-poisson/` + `count-nbinom2/` (`expected.meta.toml` records drmTMB **0.6.0**; campaign anchor is 0.7.0). Live skip-guarded Poisson phylo julia-vs-TMB at **n_tip = 24**, tol **1e-2**, in drmTMB `tests/testthat/test-julia-phylo-count.R` — moderate tree, not large-p; NB2 phylo has no live twin in that file. Julia recovery/FD: `test/test_poisson_phylo_laplace.jl` (p=32), `test/test_nb2_phylo_laplace.jl`. Bench `bench/R/compare_phylo_{poisson,nb2}.R` is timing, not a coef/logLik fixture. |
| twin | **YES** (`poisson()` / `nbinom2()` + `phylo()`; vignettes `count-nbinom2`, `phylogenetic-models`, `large-data`; capability-and-limits: structured count `mu` is recovery-only, not a coverage twin) |
| collision | None of the open PRs own this ID. Soft: `#425` owns `src/sparse_laplace_glmm.jl` (adjacent path). Do not flip `claim_status`. |

Keep the smoke + Workflow G FE gates. Do not promote. Large-p is the *route* (sparse O(p)), not a measured same-target cell.

---

## 8. `phylo_gamma_beta_binomial` · class **`TSV-claim`**

| Field | Value |
|---|---|
| status | `experimental` |
| route / syntax | `phylo` · `Gamma()` / `beta()` / `stats::binomial()` with `phylo(1 \| group, tree = tree)` |
| claim_boundary | "Finite-and-sane bridge smoke evidence only; no native TMB parity or non-phylo binomial bridge promotion." — **stale on Gamma/beta**: A5 produced a native comparator (2026-08-15). Binomial still has no native twin. |
| next_action | "Add comparator or parity evidence before promoting beyond experimental." — **done as evidence**; promotion remains a drmTMB claim. |
| fixture | **EXISTS** (mixed): `docs/dev-log/evidence/parity-phylo-nongaussian.tsv` via `tools/parity_phylo_nongaussian.R` (A5). `phylo_beta` PARITY_PASS (coef 5.019e-07, ll 3.892e-10, tol 1e-4). `phylo_gamma` PARITY_FAIL by a hair (ll \|d\| = 1.016e-04 vs 1e-4; seed-411; not loosened). `phylo_binomial` **NO_NATIVE_COMPARATOR** (drmTMB refuses structured binomial; drmTMB#1048). A5b (`2026-08-15-a5b-binomial-phylo-recovery.md`) is DRM.jl recovery only — not same-target. Bridge `tests/testthat/test-julia-phylo-nongaussian.R` is still finite-and-sane smoke and still claims native TMB *rejects* Gamma phylo — that comment is stale vs A5. |
| twin | **YES** for Gamma/beta + `phylo()`; **NO** public fitted `binomial()` + `phylo()` route (capability-and-limits: binomial structured "not generally available") |
| collision | None own this ID. Adjacent: `#425` A10 (`src/binomial.jl`, `src/sparse_laplace_glmm.jl`). Do not unpark a binomial-phylo twin. Do not flip `claim_status`. |

Not a missing engine. Comparator exists; do not promote. Do not invent a binomial×phylo Δ.

---

## 9. `general_covariance_structured` · class **`TSV-claim`**

| Field | Value |
|---|---|
| status | `experimental` |
| route / syntax | `structured` · `relmat(1 \| group, K = K)` for supported one-response families |
| claim_boundary | "Requires covariance/relatedness matrix K and sigma ~ 1; beta, precision Q, and sigma predictors stay gated." |
| next_action | "Compare current DRM.jl accepted families with the R gate before widening. DRM.jl-vs-gate comparison now exists and is re-runnable: DRM.jl tools/parity_ledger.py against a pinned drmTMB ref, with docs/dev-log/evidence/2026-08-14-drmtmb-parity-ledger.md." |
| fixture | **NONE** as a committed same-target coef/logLik `expected.toml` / `parity-fixtures.tsv` row. Gate-compare **EXISTS**: `docs/dev-log/evidence/2026-08-16-a9-general-covariance-audit.md` (A9, on `origin/main`). Agree on Gaussian / Poisson / NB2 / Gamma (both fit) and Binomial (both refuse). Asymmetry: **Beta + `relmat`** fits in DRM.jl, refused by drmTMB 0.7.0 — do not invent that twin. TSV `drmjl_status` ("Gaussian, Poisson, NB2, and Gamma") understates Julia by one family (A9 Finding 1). Live bridge `tests/testthat/test-julia-structured.R` is Poisson `relmat` finite-and-sane smoke (no TMB twin). Julia recovery/FD: `test/test_relmat_counts.jl` (+ `_nb2` / `_beta`). |
| twin | **YES** (`relmat()`; vignette `relmat-known-matrices`; man `relmat`). Neighbours `beta` / `Q` / `sigma ~ x` stay gated — documented limits, not fitted twins. |
| collision | None own this ID. Soft: do not edit `#423`'s `tools/parity_ledger.py`. Do not widen beta/`Q`/sigma-predictors. Do not flip `claim_status`. |

Gate-compare is the owed `next_action` and is done. Widening is a claim, not a port.

---

## S3 summary (for Ada S5)

| # | ID | class | same-target | first-implement? |
|---|---|---|---|---|
| 7 | `phylo_count_large_p` | `smoke-only` | **NONE** (FE Workflow G ≠ phylo; live n=24 Poisson at 1e-2 is not large-p) | No — TSV says keep / do not promote |
| 8 | `phylo_gamma_beta_binomial` | `TSV-claim` | **EXISTS** (mixed A5 TSV) | No — comparator added; promotion is a claim. Ada's prior "missing comparator / candidate #2" is **stale** |
| 9 | `general_covariance_structured` | `TSV-claim` | **NONE** numeric; gate-compare **EXISTS** (A9) | No — widening is a claim; beta+`relmat` is an invented Δ if sold as parity |

None of rows 7–9 is a first-implement candidate. Row 8's `next_action` is evidence-complete; do not re-open "add comparator" as new work. Row 10 stays in S4 (`owned`).

Rose fence: cell evidence ≠ TSV flip; COUNTDOWN 0 is export-name honesty, not a public twin claim. STOP GATE: do not merge drmTMB `#1049` / `#1050` from this lane.

Sources: scratch `2026-08-16-arc1-{eleven-rows,hopper-twin-map,lane-collisions,rose-fence}.md`; `origin/main` A5 / A5b / A9 + `parity-phylo-nongaussian.tsv`; drmTMB `git show origin/main:` TSV + `tests/testthat/test-julia-{phylo-count,phylo-nongaussian,structured,workflow-g}.R`; Dropbox `docs/dev-log/after-task/2026-08-16-ultra-plan-arc1.md`.
