# Melissa bounded reconciliation — PASS

The retained package matches its bounded promise. Source bases and live hashes
resolve; animal A, converted-spatial K and Poisson K each pass public
`engine = "julia"` point/profile/B=2 bootstrap checks against direct same-engine
Julia at one/four Julia threads with BLAS one. Every bootstrap records 2/2
successful refits. Identity-provider damage moves every profile by more than
1e-6. The EPERM setup failure and boundary/Hessian warnings remain visible.

Only Poisson carries an explicit serial-versus-threaded numerical comparison;
animal and spatial independently match direct Julia in both arms. The evidence
does not claim the stronger cross-run comparison for those two cells. Native-R
parity, coverage/calibration, performance, Gamma normalization, missing
predictors, original LSS obligations and global G0-G8 remain open.
