This receipt was accidentally launched by passing `--help` to the original positional-only runner. It finished within its 180-second cap: 987 assertions passed in 75.883 seconds with 106 unchanged inputs. The original `--help.log` and `--help.json` files were moved here without changing their contents. The runner now uses argparse so help/invalid arguments exit before any Julia launch. Combined004 verifies the corrected runner.
