Rose / Sol high / read-only review: APPROVE bounded diagnostic. Gaussian ML/log-SD gradients and QR coefficient-order recovery are sound; hashes and default reproduction match. PASS means oracle agreement, not tight optimizer or parity success. Three tight fits singular; restarts gradients up to3.54e-5. Label negative runner hash as unmodified base and retain mutation recipe. Factor6.26097e-6>4e-6 stays FAIL. No fits or edits by reviewer.
