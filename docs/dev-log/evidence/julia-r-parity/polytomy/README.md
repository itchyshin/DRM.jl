# Polytomy implementation and verification

The positive-length rooted-polytomy constructor and R serializer restriction is
repaired in this development lane. `public-003.json` records two default Gaussian
ML workflows: a12-tip star and a mixed multifurcating tree, each with60shuffled,
repeated observations. NativeR, directJulia and the realRbridge pass4e-6; source
before/after, nativeDLL fingerprint and actualJulia path/thread budget are retained.
Elapsed21.399seconds includes startup and is not warm performance evidence.

`check_polytomy_public_receipt.py` independently reconstructs shared-path covariance,
observed Gaussian likelihoods and conditional fitted values in original row order.
Sixteen deliberate corruptions reject normally and underPython-O. The new
constructor/kernel/height checks pass55+22+14=91 assertions; Rserialization and
existing bridge guards pass33+122=155. Two edited docs pages execute one example.

Rose approved mathematics and source; her required two test-runner includes are
wired. Melissa reconciled the bounded claim with all original obligations. q2 has
an independent exact Gaussian likelihood check. q4 has dimension, fixed-state
Hessian/joint-density and normalization checks, not a mode-convergence/fit proof.
Height traversal uses an advancing queue cursor; no speed ratio is claimed.

001public run lacked liveJulia opt-in and stopped before bridge execution;002passed
but lacked finalDLL/runtime provenance;003is the qualified retained run. Rgreen001
had six oracle-only scalar-name mismatches; green002passes. ConstructorRED and all
setup failures remain retained. NativeR implementation and RDS fit objects stay in
the Rrepo. Prior admission-001.json remains an immutable pre-repair refusal record.

Foreign Rbridge ZOB bytes were preserved exactly: reverting only our two serializer
lines reconstructs the before-file. No competing activelease or overlapping hunk
was found; parentowns the new hunk only. The exact foreign writer was not inferred
from unrelated Git authorship. Receipts are not clean final-integration proof.

Zero-length edges, unary/single-tip/general-label parity and all-family/inference
qualification remain required work. DirectJulia retains raw Brownian covariance;
scalar height normalization matches correlation scale only for ultrametric trees.
The Rbridge keeps its ultrametric restriction. Ayumi's unspecified profile/bootstrap
report remains open pending her model; it is not evidence of a joint-model defect.
AllG0–G8 and prior strict missing-predictor losses remain OPEN.

## Historical reproduction before repair

# Polytomous tree admission

Shinichi relayed Ayumi-san’s report on 2026-08-30. The three-tip ultrametric
star `(a:1,b:1,c:1);` reproduces the restriction without fitting: native R’s
validator accepts it, the R-to-Julia serializer refuses it as non-binary, and
direct Julia rejects its four nodes because it expects five. The two raw logs
and `admission-001.json` retain the results and source hashes.

This is a required parity gap, not a permitted exclusion. The fix must preserve
branch lengths, root conditioning and implied covariance, with tests for star
and mixed multifurcating/binary trees. Arbitrary positive-length binary
resolution changes the model and is not an acceptable workaround. Downstream
node-count assumptions must be audited before removing the admission checks.

Ayumi-san’s separate profile/bootstrap report concerns `engine = "julia"`;
this no-fit tree probe does not diagnose those inference workflows. Her issue
is forthcoming. No collaborator message or source change was made here.
