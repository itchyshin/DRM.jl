# Bounded paired profile review

Rose, independent Sol/high child, reviewed the combined source hashes listed
in manifest.json. No material source blocker, conditional on final tests passing.
The retained final bundles now pass. Value, analytic gradient, information,
profile nuisance and diagnostic consumers use the same selected representation.
Legacy sigma/correlation defaults stay raw and paired. Typed seeds are copied
and recertified. Each coefficient job owns its solver state; shared inputs are
read-only and output slots distinct. The joined threaded region is inside the
exception-safe, nested/overlap-aware BLAS guard.

The paired nuisance optimizer disables x/f equality stopping, retaining the
1e-7 gradient criterion, 200-iteration budget and independent acceptance check.
This corrects premature stopping without relaxing the convergence threshold.
The strict-options restart probe alone was not causal proof; the unchanged
end-to-end finite fixture subsequently passed and remains unchanged.

Thread provenance: the first original all-pass receipt requested four threads
but did not assert the runtime. The final finite-profile193110Z script explicitly
asserts four Julia/one BLAS; serial-profile193031Z asserts one/one. Both pass.

Scope: no full package-suite, R numerical parity, bootstrap validity/coverage,
universal boundary convergence or speed verdict. Finite SEs, particularly the
large covariance-boundary SE here, do not establish positive-definite outer
information or calibrated Wald coverage. No GPL source imported.
