# Structured-family continuation

Fifteen unchanged original test files completed on Totoro Julia 1.10.10 with one
Julia and one BLAS thread: 228 passing assertions across 40 testsets, 50 seconds,
exit 0. Source commit: 479f1e06. All 327 source/test hashes matched and remained
unchanged. Every requested include emitted its completion marker.

The group covers structured Poisson, negative-binomial, Gamma, beta, binomial
and beta-binomial fits; gradient checks; routing; and selected-inverse entries.
These are package regressions, not a full-suite pass, calibrated coverage,
native-R parity evidence or matched warm-workflow performance measurements.

Mission Control's queued BLAS update was applied after its dashboard lease
cleared. The served status was verified at vault commit 7e41d05. An earlier
commit guard observed foreign staged memory files and stopped. The final commit
used --only for the status file; by then other staging was empty and remained
unchanged. Those staging and served-state observations are receipt-attested;
the commit independently proves the one-file scope. Protected intake was not
read. See mission-control-applied.json.

The following location-scale group is recorded separately in
../locscale-continuation-20260831/. It timed out, and its slow profile checks were
skipped. A completed include is not proof that substantive testsets ran.
