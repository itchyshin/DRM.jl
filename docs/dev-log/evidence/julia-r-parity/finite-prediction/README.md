# Finite prediction and uncertainty follow-up

Direct finite formula fits now retain mean/sigma schemas for known-state new-data
prediction. Point predictions do not condition on a supplied new response. SEs
require finite, symmetric, positive-definite observed-information covariance;
missing/unknown predictor states and absent legacy schemas refuse explicitly.

`finite-prediction-green-003.log` records 51 prediction, 86 frontend and 109 factor
assertions (246). `finite-prediction-unlazy-002.log` re-executes both commands from
the correct repository directory: all three leaf gates met, including Rose review.
001 used the ledger directory and failed to load DRM; it is retained as a harness
configuration failure. Earlier REDs preserve missing methods, test setup mistakes,
covariance review failures and the tiny-negative-covariance regression. Sandbox
cache failures occurred before tests and are not scientific failures.

The edited documentation page executes five examples in 20.890 seconds; generated
Markdown is in the fresh build path printed in the log. This does not verify a
whole-site render, accessibility, deployment or complete navigation.

`joint-public-006.json` is a fresh real R-to-Julia receipt for Gaussian/Bernoulli
predictor workflows, paired with `joint-frontend-fit-wald-001.toml`. Both adapters
pass the independent oracle and all 21 deliberate corruptions reject with normal
Python and Python -O, including a return to old delta-Wald SD endpoints. The native
R repair is commit 6ca8f9377 in drmTMB. Both strict native 4e-6 comparisons still
FAIL; `joint-public-native-006.log` is the retained failing gate. Gaussian training
mean differs by 5.307e-6; Bernoulli theta by 1.002e-5. Startup-inclusive elapsed
25.403 seconds is not warm performance. 005 was blocked at Julia metadata-cache
access before fitting and is retained. Finite public006 in ../finite-frontends/
passes both adapters but also retains both native failures and 17 corruption checks.

Rose approved the frozen source and covariance edge tests. Melissa found no dropped
obligation provided the known-state restriction, full accessor parity, strict losses,
profile/bootstrap, polytomy, all original native obligations and G0–G8 stay open.
Native joint missing-predictor bootstrap now refuses incomplete joint simulation/
refits; implementing it remains required. Ayumi's inference report has no supplied
model yet. The independently reproduced polytomy restriction is not fixed.

R fit objects remain exclusively in the GPL R repository. No GPL implementation
source was copied. Receipts describe tested development bytes, including preserved
foreign R bridge work; they do not certify a clean final committed integration.
