# #372 Arc 0 — probe + protocol stub

Date: 2026-08-03 · Arc 0 of Ultra Plan #372  
Perspectives: Shannon · Ada · Curie · Rose. No nested subagents.

## Version matrix (local machine)

| Component | Value |
|---|---|
| Hostname | (see measurement JSON) |
| Julia | 1.10.0 |
| BLAS threads (Julia) | 1 |
| R | 4.6.0 (2026-04-24) |
| Installed drmTMB | **0.6.0** (not the v0.1.3 fixture pin) |
| Twin repo tag available | `v0.1.3` present in local drmTMB checkout |
| OMP / OpenBLAS / MKL | set to 1 for R arm |

Rose: R-arm timings are labeled **drmTMB 0.6.0**. Coefficient fixtures remain
v0.1.3-generated numbers; this slice measures wall-clock, not re-anchors coef
parity. Preferring a temp-lib 0.1.3 R arm is optional follow-up — not required
to close if version is recorded.

## One-cell smoke (gaussian-locscale)

Protocol: 1 warmup discarded + 3 timed reps; same `data.csv`.

| Arm | median_s (3 warm) | ok |
|---|---:|---|
| Julia `drm_bridge` | ~0.00061 | yes |
| local drmTMB 0.6.0 | ~0.016 | yes |

Smoke logLik (R): −215.4625 (matches fixture scale).

## Protocol for Arc 1 (six cells)

1. Threads: Julia `BLAS.set_num_threads(1)`; R `OMP_NUM_THREADS=1`
   `OPENBLAS_NUM_THREADS=1` `MKL_NUM_THREADS=1`.
2. Per cell: 1 warmup discarded + **5** timed reps; report median / min / max.
3. Julia path: `drm_bridge` with fixture formula + family + `data.csv`.
4. R path: public drmTMB API shapes from `test/parity/gen_fixtures.R` recipe
   (no GPL source vendored).
5. Retain JSON under `bench/results/bridge_six_cell_372/` + markdown evidence
   under `docs/dev-log/evidence/`.
6. Risk: if a cell’s R arm errors → mark **R-blocked — no claim** for that
   cell; continue others. Do not invent timings.

## Harness paths

- `bench/bridge_six_cell_timing.jl`
- `bench/R/bridge_six_cell_timing.R`

## Gate

Arc 0 **PASS** — both arms smoke on `gaussian-locscale`. Proceed to Arc 1.
