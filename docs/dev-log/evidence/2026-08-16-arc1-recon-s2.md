# Arc 1 recon S2 — rows 4–6 (2026-08-16)

**Role:** Hopper (read-only). **Compute:** n/a. No Julia, no Rscript, no `Pkg.test`.
**Lane:** scratch `DRM.jl-catchup` evidence only. Did not touch Dropbox `src/`, `test/`, `LOOP/`, or open-PR files.
**STATE:** `PLATFORM: cursor | ON BRANCH: docs/a3c-design (Dropbox leftover; unused) | LANE: arc1-recon-s2`
**OTHER LANES:** #429 A12 · #428 A11 · #425 A10 · #423 A8 · #421 rosetta · #420 LOOP · #406 board · main-direct · sibling recon S1/S3/S4

`claim_boundary` / `next_action` quoted from `git show origin/main:inst/extdata/julia-capabilities.tsv` (drmTMB **0.7.0** @ `097bed1e2`). Twin verdict from scratch `2026-08-16-arc1-hopper-twin-map.md`. Collision from scratch `2026-08-16-arc1-lane-collisions.md` + Rose fence `#49`. Ada prior: **`biv_q4_phylo_reml` is the default recommended later fixture** (same-target CI/status parity on an already-implemented q4 REML path). Confirm in S5; this batch does not override that default.

---

## 4. `gaussian_response_mask`

| Field | Value |
|---|---|
| status | `partial` |
| claim_boundary | "Gaussian-only response masks; missing predictors and non-Gaussian response masks remain gated." |
| next_action | "Keep mask tests Gaussian-only until non-Gaussian observed-data likelihoods are audited." |
| fixture | **NONE** as a committed same-target `expected.toml` / `parity-fixtures.tsv` row. No `test/parity/fixtures/*mask*` dir. Julia-only observed-subset tests exist (`test/test_missing_response.jl`, plus nongaussian / bivariate siblings) — that is Scoreboard B smoke, not native-vs-Julia cell evidence. |
| twin | **YES** (`miss_control(response = "include")`; vignette `missing-data`; man `miss_control`). Predictor `mi()` / `impute` is a **different** surface. |
| collision | **#49 PARKED** — adjacent, do not unpark. This row is Gaussian response-include only. Do not document `impute` / `mi()` as a fitted route. Soft: `#425` owns `src/binomial.jl`, not this Gaussian mask. Do not flip `claim_status`. |

Working class: **fixture-gap** + **parked-adjacent** (`#49`). Not a missing engine.

---

## 5. `biv_q4_phylo_reml`

| Field | Value |
|---|---|
| status | `partial` |
| claim_boundary | "Requires the full four-axis phylogenetic location-scale grammar; native TMB has separate q4 recovery evidence, but this Julia row does not establish same-target bridge parity, interval reliability, or HSquared AI-REML support." |
| next_action | "Bank fit-specific CI/status parity before release language." |
| fixture | **NONE** as a committed same-target `expected.toml` / `parity-fixtures.tsv` row. No `test/parity/fixtures/*q4*` / `*reml*` dir. Julia q4 REML tests exist (`test/test_reml_q4_allaxes.jl` and siblings) — engine is already on Scoreboard B. Gap is **bridge-parity / CI-status**, not a Laplace port. **Default recommended later fixture** (Ada prior; this recon does not cite a cheaper override). |
| twin | **YES** (`drmTMB(..., REML = TRUE)` + four-axis `phylo()` + `biv_gaussian()`; man `drmTMB` REML; vignettes `phylogenetic-models` / `bivariate-coscale`; NEWS 0.7.0 Julia `REML=TRUE` forwarding for one q4 cell). Do not invent AI-REML / interval coverage as the twin. |
| collision | None of the open PRs own this ID. Do **not** treat the orange Scoreboard B chip “REML with ordinary random effects” as this row (that chip is rejected; not an Arc 1 port). Soft: do not edit `#423`'s `tools/parity_ledger.py`. Later fixture work must not touch the verified q=4 core (logLik −256.51 / 2.18×). Do not flip `claim_status`. |

Working class: **fixture-gap** (same-target missing; engine present).

---

## 6. `plain_binomial_nonphylo`

| Field | Value |
|---|---|
| status | `partial` |
| claim_boundary | "Live R Workflow G binomial-trials cell vs DRM.jl expected.toml; still experimental, not a CRAN default." |
| next_action | "Keep Workflow G live R gate green; do not claim CRAN-default Julia. Independent coefficient/logLik parity for FE Poisson/NB2/Gamma(log) measured through engine='julia' on 0.7.0 (1.03e-12 / 6.89e-08 / 5.32e-06); see DRM.jl docs/dev-log/evidence/parity-fixtures.tsv." |
| fixture | Workflow G `test/parity/fixtures/binomial-trials/expected.toml` (drmTMB **0.6.0**, #392 split — campaign anchor is 0.7.0). **Not** a row in `parity-fixtures.tsv` (that TSV's FE Poisson/NB2/Gamma lines are neighbours cited by `next_action`, not this ID). Promotion is a drmTMB claim, not a port. |
| twin | **YES** (`stats::binomial()` via `drmTMB()`; vignette `proportion-beta-binomial`; NEWS 0.7.0 binomial links + Workflow G `engine = "julia"` FE gate, drmTMB `#499`). |
| collision | Adjacent: `#425` owns `src/binomial.jl` / `src/sparse_laplace_glmm.jl` — do not steal those files. This row is the non-phylo FE Workflow G cell, not phylo-binomial. Do not claim CRAN-default Julia. Do not flip `claim_status`. |

Working class: **TSV-claim** (fixture already exists; keep the live R gate).

---

## S2 summary (for Ada S5)

Row 4 is a **fixture-gap** fenced by **#49** (Gaussian `miss_control` only; do not unpark `mi()` / `impute`). Row 5 is the **default recommended later fixture**: bank same-target CI/status parity on the already-implemented q4 REML path — not an engine port. Row 6 already has Workflow G `binomial-trials/expected.toml`; keep that gate green. None of these three is a first-implement engine. Rose fence: cell evidence ≠ TSV flip; COUNTDOWN 0 is export-name honesty, not a public twin claim.

Sources: scratch `2026-08-16-arc1-eleven-rows.md`, `2026-08-16-arc1-rose-fence.md`, `2026-08-16-arc1-hopper-twin-map.md`, `2026-08-16-arc1-lane-collisions.md`; sibling `2026-08-16-arc1-recon-s1.md` (schema); Dropbox `docs/dev-log/after-task/2026-08-16-ultra-plan-arc1.md`; TSV via `git show` (not a ledger countdown re-run).
