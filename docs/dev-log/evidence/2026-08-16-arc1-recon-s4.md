# Arc 1 recon S4 — owned + fence (rows 10–11)

Date: 2026-08-16 · **Shannon / Hopper** · **read-only** · no `src/` · no impl · no TSV flip
Lane: scratch `DRM.jl-catchup` evidence only. drmTMB via `git show origin/main:…` — no checkout.
Class vocabulary: `owned` · `fence` (this batch only).

drmTMB **0.7.0** @ `origin/main` `097bed1e2`. TSV: `inst/extdata/julia-capabilities.tsv`.
Siblings: `2026-08-16-arc1-eleven-rows.md` · `2026-08-16-arc1-hopper-twin-map.md` · `2026-08-16-arc1-lane-collisions.md` · `2026-08-16-arc1-rose-fence.md`.

**This slice documents. It does not implement. Recommended first implement slice is not either of these rows.**

---

## Row 10 — `cross_family_latent` · class **`owned`**

| Field | Value |
|---|---|
| `claim_status` | `experimental` |
| `route` / `syntax` | `cross_family` · `c(gaussian(), poisson())` cross-family latent-rho route |
| `r_bridge_status` | `experimental` |
| `drmjl_status` | latent-rho mixed-family path; API drift is tracked in tests |
| `claim_boundary` | Latent-rho development route; public docs must not present rho12 formulas or release-ready cross-family inference. |
| `next_action` | Resolve the mixed-family API mismatch before any public promotion. |
| `issue` / evidence | drmTMB#544 (CLOSED) · gllvmTMB#488 |
| Twin-map | **UNKNOWN** — staged `associate_pairs()` / `association()` / `latent_normal()` vs a future joint `c(gaussian(), poisson())` fit. Do not invent equivalence. |
| Same-target fixture | **NONE** (not this lane's job to add one) |

**Owner (live, this pass):** [PR #428](https://github.com/itchyshin/DRM.jl/pull/428) A11 · `feat/a11-cross-family-formula` · OPEN · MERGEABLE · **BEHIND** · auto-merge **unarmed**. CI green (`test (1.10)`, `test (1)`, `docs`). +264 / −0.

Owns: `src/mixed_family.jl` · `src/mixed_family_postfit.jl` · `docs/src/cross-family.md` · `test/test_cross_family_formula.jl` · `test/runtests.jl` · `LOOP/checkpoint.md`.

PR thesis (do not restage): formula `drm(bf(mu1=…, mu2=…), (Gaussian(), Poisson()); data)` front end that delegates to unchanged `fit_mixed_family`; **refuses `rho12 ~ …`** (latent scalar, not residual ρ12); no promotion — row stays `experimental`.

Scoreboard B on `origin/main` still lists Cross-family bivariate as `missing`. That census lag is **#428's** merge, not an Arc 1 port.

**SKIP implement.** Inventory only. Do not steal the branch, rebase it, or edit its files. Promoting `claim_status` remains a drmTMB TSV decision (STOP GATE).

---

## Row 11 — `engine_control_surface` · class **`fence`**

| Field | Value |
|---|---|
| `claim_status` | `unsupported` (sole literal-`unsupported` of the 11) |
| `route` / `syntax` | `base` · `engine_control = ...` or non-default Julia optimizer controls |
| `r_bridge_status` | `unsupported` |
| `drmjl_status` | **no R surface by design** |
| `claim_boundary` | Do not document user-selectable Julia optimizer controls until a real R API is designed. |
| `next_action` | Design `engine_control` explicitly before relaxing the gate. |
| `issue` / evidence | drmTMB#544 |
| Twin-map | **NO** public surface to match |
| Same-target fixture | **NONE — not owed** |

**Not a port.** Hopper: no exported `engine_control()` (absent from `NAMESPACE`); `julia-engine` vignette does not document it; `drm_control()` is TMB/`nlminb` only. Gate is reserved in `R/julia-bridge.R` (`"Julia optimizer controls need an explicit engine_control surface."` / `"Design engine_control explicitly before relaxing the gate."`). `LOOP/GOAL.md`: unsupported by design — needs an R API first.

Leave `unsupported`. Do not document user-selectable Julia optimizer controls. Do not invent a twin Δ. Design lives in **drmTMB**, not this repo.

---

## Ada / S5 note

Put both rows **last** on the ordered backlog. Neither is the recommended first implement slice. S7 must reject a recommended slice that is `#428` / `engine_control_surface` / a TSV flip.

---

## Sources (do not re-derive)

- `git show origin/main:inst/extdata/julia-capabilities.tsv` (drmTMB `097bed1e2`)
- `git show origin/main:R/julia-bridge.R` · `NAMESPACE` (no `engine_control` export)
- `gh pr view 428 --repo itchyshin/DRM.jl` (this pass)
- scratch `2026-08-16-arc1-{eleven-rows,hopper-twin-map,lane-collisions,rose-fence}.md`
