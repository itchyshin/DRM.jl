# Ultra-plan — #202 non-Gaussian phylo location–scale CLOSEOUT (STOP at G0)

Phases 0–2 read-only. No Phase 3 until `G0 APPROVED` + fresh `/goal`.
Platform: **Cursor** (this session). Arc-shaped → hand to `/goal` after approval.

---

## 🎯 GOAL

```
PLATFORM: Cursor (workbench) · live Julia verify on Totoro if recovery fixtures are heavy
DELIVERABLE: Close or honestly re-scope open #202 by landing the remaining PUBLIC
  surface + evidence for non-Gaussian phylogenetic location–scale (NB2 primary;
  Gamma already engine-gated), then DoD PR (closes #202 or splits remainder).
HEADLINE: Tip already has src/locscale_*.jl + Gamma phylo recovery via private
  _fit_locscale — this lane is CLOSEOUT / public API / ledger honesty, not a
  greenfield q=2 kernel rebuild.
IN PARALLEL: RECON twin-bar (drmTMB structured-σ vs coupled (1|p|·)); grammar
  probe (issue formula vs coupled tag); capability-status row audit.
DEFER / FENCE: D-111 / Registrator; .worktrees/; GPL vendoring; q=4 verified
  core edits; #49 FIML; #136 VA; tip-idle SHA-churn; inventing new families;
  nbinom2-locscale R fixture until drmTMB supports coupled (1|p|species) OR
  owner names a structured-σ (non-coupled) parity cell.
DISCIPLINE: verify before claiming · ML default · Rose claim-vs-evidence ·
  one issue → one branch → one PR · leave .worktrees/ alone.
```

---

## ARC PROGRAM

From `/arc-creation` (corrected after Phase 0.25):

- **Mode:** size  
- **Recommended:** **6–10 h** closeout programme (was 12–16 h when mistook #202 for greenfield)  
- **Arc 0:** twin-bar + public-API probe (60–90 min)  
- **Rung 1:** public NB2 phylo location–scale path (grammar owner picks) + recovery  
- **Rung 2:** Gamma public route parity with private gate; FD ≤1e-6 if new path  
- **Rung 3:** capability-status + tutorial + stale-comment fix + close/split #202  
- **Closeout:** check-log + after-task + Rose + PR  

Under-run → pull Rung 3 forward; do not pad with tip-idle.

---

## PHASE 0.25 — SWEEP RECEIPT (gate for decompose)

| Surface | Evidence ran | Finding | Call |
|---|---|---|---|
| **repo git** | `git status -sb`; `git log -15`; `git worktree list`; `branch_drift_check.sh` → main 0/0 vs origin | Tip `7bfe609` idle; `M LOOP/checkpoint.md` local; `?? .worktrees/`; many stale worktrees; **no open PRs**; no #202 WIP branch | **build-the-gap on tip** — do not resume cluster2 / locked agent worktrees |
| **twin drmTMB** | README family table + `tests/testthat/test-nbinom2-sigma-structured-recovery.R` + `gen_fixtures.R:444–455` | R has **exact q=1 NB2 structured `sigma`** (recovery-grade). Coupled `(1\|p\|species)` for nbinom2 is **still skipped** in DRM.jl parity generator (“drmTMB does not yet support…”) | **reuse R structured-σ bar for D-94**; coupled Λ may be Julia-ahead → owner gate |
| **brain** | `search_notes` `#202 non-Gaussian… D-94`; `grep DECISIONS.md D-94`; `grep AGENT_LOG` (no 2026-08 #202 ship); `deep-research/README` → dr2 / dr10 LSS | Design decision `docs/dev-log/decisions/2026-06-06-nongaussian-phylo-location-scale.md`; D-94 R-first; #166 plan had #202 RETRACTED pending R | **resume design + close gap**; do not rebuild engine |
| **repo code / after-task** | `gh issue 202` OPEN; PR **#253 MERGED** (Gamma recovery + FD); `src/locscale_*.jl` included; `test_phylo_locscale.jl` private API; `test_locscale_frontend.jl` iid `(1\|p\|g)`; frontend comment still says phylo waits on #209 but **#209 CLOSED** and `_ls_frontend_grouping` already routes `:phylo` | Engine + iid public route + Gamma phylo **private** gate exist. Issue example `phylo(1\|sp)` on **both** axes still hits NB2 “only mean may carry RE”. Capability-status has no “non-Gaussian phylo location–scale” row | **Genuine gap = public grammar + NB2 phylo public evidence + ledger close** |
| **Verdict** | | Genuinely new: public closeout of #202 (grammar + NB2 phylo evidence + docs/capability + issue disposition). **Not new:** q=2 Laplace kernel, Takahashi grad, Gamma private recovery | **reuse engine → build public/closeout gap** |

---

## WHAT THE BRAIN / REPO ALREADY KNOWS

- Tip IDLE after #394; START HERE after-393; next work needs named G0.
- #202 design note (2026-06-06) accepted; #165/#164 CLOSED.
- Tracking #209 claims locscale e2e complete (NB2 & Gamma; iid & phylo) — yet #202 remains OPEN and dual-`phylo(1|g)` public formula still errors on NB2.
- D-94: do not put Julia ahead of R by default; D-111: no General registry.
- Arc Card (prior turn) oversized as greenfield — **corrected here**.

## WHAT SHINICHI TOLD US

- Resume archived tip-idle lane → tip IDLE confirmed.
- Asked for next G0 + `/arc-creation` → recommended scoped #202.
- Asked `/ultra-plan` for approval (this artifact).

## WHAT THE TEAM RAISED

```
TEAM RAISED
  Noether — Engine already in src/locscale_*.jl; do not touch q=4. Gap is routing
            / public evidence. Stale “waits on #209” comment is drift.
            · Rec: closeout lane, not kernel rewrite.
  Hopper  — R has NB2 structured-σ cells; coupled (1|p|·) still not in drmTMB
            parity generator. Dual-phylo vs coupled tag is the twin grammar question.
            · Rec: Arc 0 cite exact R cell before claiming D-94 catch-up.
  Boole   — Issue #202 formula ≠ shipped `(1|p|g)` / `(1|p|phylo(g))` grammar.
            · Rec: owner picks acceptance surface (alias vs document-only).
  Rose    — Closing #202 while capability-status omits the row + issue text
            still shows the dual-phylo example would be claim drift.
            · Rec: capability row + honest close/split in same PR.
  Ada     — Recommend CLOSEOUT programme 6–10 h; G0 blocked on grammar + D-94.
```

## ADA'S RECOMMENDATION

Approve a **#202 closeout** G0 (not greenfield). Default cohort:

1. Arc 0 confirms whether owner wants **(A)** issue-text dual `phylo(1|sp)` alias, **(B)** public `(1|p|phylo(sp))` only, or **(C)** ledger split (close engine / open grammar issue).
2. Land public NB2 phylo location–scale recovery + wire/tests for the chosen grammar.
3. Capability-status + tutorial honesty + Rose; PR `closes #202` (or closes + opens remainder).

Prefer **B** if A risks Boole/drmTMB grammar fights; prefer **A** if paste-parity with the issue body matters more.

## DECISIONS LOCKED (pending your G0)

- Platform Cursor; after G0 → `/goal` (not Phase 3 in this chat).
- Reuse `locscale_*`; no q=4 core edits; D-111 OFF; leave `.worktrees/`.
- One branch from `origin/main` @ `7bfe609` (or newer tip).
- Compute: local/Totoro for recovery fixtures (p≈50, m≈25 analogues).

## QUESTIONS STILL OPEN (Phase 0.4 — need your call)

**Q1 — Acceptance grammar**  
**QUESTION:** Which public formula must work to close #202?  
**(A)** Issue text: `bf(y ~ x + phylo(1|sp), sigma ~ 1 + phylo(1|sp))`  
**(B)** Coupler: `bf(y ~ x + (1|p|phylo(sp)), sigma ~ 1 + (1|p|phylo(sp)))`  
**(C)** Split: keep engine CLOSED; open new issue for dual-phylo alias  
**WHY NOW:** Tip already fits B-path plumbing; A currently errors on NB2.  
**RECOMMENDATION:** **B** (+ docs that A is rejected/alias TBD) unless you want paste-parity with the issue body.  
**IF YOU DO NOT MIND:** B.  
**WHAT CONTINUES:** Arc 0 twin-bar citation (reversible).

**Q2 — D-94 / coupled estimand**  
**QUESTION:** drmTMB still skips coupled `(1|p|species)` nbinom2 in our parity generator, but has q=1 structured-σ. Is closing coupled phylo location–scale OK as Julia catch-up/extra, or must we only ship what R already recovers?  
**RECOMMENDATION:** Ship public phylo locscale (B); treat R `nbinom2-locscale` fixture as **follow-on** when R supports coupled; cite structured-σ as D-94 cover for *scale-axis phylo existence*.  
**IF YOU DO NOT MIND:** that recommendation.  
**WHAT CONTINUES:** none irreversible.

**Q3 — NotebookLM?**  
Prior art already in design note + dr2/dr10. **Offer:** skip tier-b search unless you want a fresh LSS sweep. Default: **skip**.

---

## SLICE TABLE

| Slice | Member | Model+effort | Bar | Dispatch | Time | Detail | Dep |
|---|---|---|---|---|---|---|---|
| RECON | Hopper+Noether | Composer/Grok · low | Cursor Models | inline | 60–90m | Twin bar cite; probe A vs B formulas; list files; freeze cohort | — |
| S1 grammar+route | Boole+Noether | Composer 2.5 · medium / escalate Auto | Cursor Models → Other if repair | Agent | 2–3h | Implement chosen grammar; forward `tree=` into NB2 locscale frontend if missing | RECON |
| S2 NB2 recovery | Curie+Noether | Composer / Totoro Julia | Hand off compute | Agent+local | 2–3h | Public `drm()` recovery + FD gate (NB2 phylo locscale); wire `runtests.jl` | S1 |
| S3 Gamma public | Curie | Composer · medium | Cursor Models | Agent | 1–1.5h | Public Gamma path mirrors private `test_phylo_locscale` or document private→public | S1 |
| S4 docs/ledger | Pat+Rose | Auto Cost · medium | Other Models | Agent | 1–1.5h | capability-status row; tutorial; fix stale #209 comment; NEWS; close/split #202 | S2 |
| VERIFY | Rose+Noether | Auto Cost / Claude · high | Other Models / hand off | review | 45–60m | `Pkg.test` focused + Rose claim audit | S2–S4 |
| RECONCILE | Melissa | Auto · low | Other Models | after merge | 20m | `docs/dev-log/plan-actual/2026-08-05-202-locscale-closeout.md` | VERIFY |

**LUNA SUITABILITY:** yes — RECON twin/grammar inventory.  
**ULTRA EFFORT:** no.  
**FAN-OUT BUDGET:** checkpoint=`202-closeout` · children ≤4 · ceiling 0 in Cursor parent.  
**CONTEXT BRAKE:** STOP at G0; execute via `/goal`.  
**D-43 PANEL:** on PR merge claim only.

---

## ESTIMATE

~**6–10 h** wall-clock one Cursor `/goal` lane (+ Totoro if recovery slow).  
Fits one focused day; handoff if grammar fight with Boole/Hopper expands.

**ARC ACTUALS:** complete on Arc Card in after-task / Melissa plan-actual.

## REVIEW (plan)

Rose: sweep receipt present and non-vacuous — **PASS** (this section).  
Noether: no q=4 rebuild — **PASS**.  
Hopper: D-94 called out as Q2 — **PASS**.

## VERIFY (execution)

- Focused: public NB2 (and Gamma if in cohort) phylo locscale recovery + FD ≤1e-6  
- Regression: `test_phylo_locscale.jl`, `test_locscale_frontend.jl`, existing NB2 phylo mean  
- Rose: no speed/General claims; #202 disposition matches code  

## CONSOLIDATE

- PR `closes #202` (or closes + opens grammar/parity follow-on)  
- check-log.d + after-task + LOOP refresh → tip IDLE or next named G0  
- Optional vault MC Julia tip  

## RECONCILE

Melissa at meaningful close → `docs/dev-log/plan-actual/2026-08-05-202-locscale-closeout.md`.

---

## SEARCH

none (tier-b NotebookLM offered; default skip — design note + dr2/dr10 exist).

---

## G0 — APPROVAL GATE

Reply **`G0 APPROVED`** with answers to Q1–Q2 (or “use your judgment”).

Then paste into a **fresh** chat:

```text
/goal

Approved plan: docs/dev-log/plans/2026-08-05-202-locscale-closeout-ultra-plan.md
Tip: origin/main @ 7bfe609 (or newer). Issue #202 closeout — reuse locscale_* engine;
public grammar per G0 answers; NB2 phylo recovery + docs/capability; Rose; PR closes #202.
Fences: no q=4 core; D-111; .worktrees/; no GPL; no #49/#136; no tip-idle padding.
Scaffold LOOP/ from the ultra-plan; run arcs to completion.
```

Do **not** start Phase 3 in this planning chat.

*Shannon · Ada · Noether · Hopper · Boole · Rose · Curie. No nested Task subagents in this plan pass.*
