# 2026-08-19 — #420 / #406 housekeeping (read-only scout)

**Lane:** `docs/420-406-housekeeping` (Cursor / Shannon).
**This file is item 5 of the 2026-08-19 honesty sequence.** Items 1–4: AGHQ
#449 + Cox–Reid #451 on `main`; Cell D ADEMP scope (#454); lever-extension
scout (#452); q4/#49 parked scout (#453). **Item 5 is document + recommend,
not a rebase or merge.**

No `src/`. No new issues. No Totoro. No chip flip. Do not close #420 or #406.
Do not rebase either PR while Shinichi is away.

Cite: GitHub GraphQL/`gh pr view` + REST `mergeable`/`mergeable_state` on
2026-08-19 (this session). Required CI on this repo: `test (1.10)`, `test (1)`,
`docs`. `scaling-sweep` skips on PRs.

---

## Verdict

**LEAVE BOTH OPEN.** Do not rebase or merge now (Shinichi away; conflict
risk; neither is engine). After vacation Shinichi decides: close as
superseded if `LOOP/checkpoint.md` and the auto-merge policy are already
live on `main`, else rebase docs-only.

| PR | mergeable (`gh`) | mergeStateStatus / REST `mergeable_state` | Recommendation |
|---|---|---|---|
| [#420](https://github.com/itchyshin/DRM.jl/pull/420) | `CONFLICTING` (`mergeable=false`) | `DIRTY` | leave OPEN |
| [#406](https://github.com/itchyshin/DRM.jl/pull/406) | `CONFLICTING` (`mergeable=false`) | `DIRTY` | leave OPEN |

First `gh pr view` snapshot this session showed `UNKNOWN`/`UNKNOWN` (GitHub
had not recomputed). REST after refresh: both `mergeable=false`,
`mergeable_state=dirty`. `merge_commit_sha` was null at scout time.

---

## #420 — loop items 1–4

- **Title:** docs(loop): record items 1-4 — tree scale surfaced, binomial x phylo wired upstream
- **State:** OPEN
- **URL:** https://github.com/itchyshin/DRM.jl/pull/420
- **Head:** `docs/loop-items-1-4` @ `55f42ba4`
- **Last updated:** 2026-08-16T11:11:23Z
- **Diff:** +15/−0 in `LOOP/checkpoint.md` only
- **auto_merge:** already enabled (2026-08-16T01:34:29Z, itchyshin, `MERGE`)
- **Base SHA at scout:** `0a4c2dc9` (stale vs `main` `8c6d4f78` at sequence start; `main` later `aa9ce55f` after #452)
- **merge_commit_sha:** null (DIRTY / conflict — not a clean merge)
- **Related:** issue [#136](https://github.com/itchyshin/DRM.jl/issues/136) VA epic (historical loop items; not this slice)

**Why not merge:** `git merge-tree` of `origin/main` vs #420 head conflicts in
`LOOP/checkpoint.md`. `main` already overwrote that pointer through #449/#450/#451
(checkpoint still says “PR #451 awaiting human merge” even after #451 landed —
stale, but that is a later honesty edit, not a reason to force #420). Auto-merge
cannot land a DIRTY PR.

Required checks on the last #420 push (2026-08-16) were green (`test (1.10)`,
`test (1)`, `docs`). Green-on-stale-base does not clear a content conflict.

---

## #406 — GitHub PR auto-merge policy

- **Title:** Record GitHub PR auto-merge policy
- **State:** OPEN
- **URL:** https://github.com/itchyshin/DRM.jl/pull/406
- **Head:** `docs/github-auto-merge` @ `0d1c207a`
- **Last updated:** 2026-08-14T18:03:07Z
- **Diff:** +88/−4 in 3 files
  - `docs/dev-log/check-log.d/2026-08-13-github-auto-merge.md` (ADDED; **not** on `main`)
  - `docs/dev-log/decisions/2026-08-13-github-auto-merge.md` (ADDED; **not** on `main`)
  - `docs/dev-log/coordination-board.md` (MODIFIED)
- **auto_merge:** already enabled (2026-08-14T00:20:34Z, itchyshin, `MERGE`)
- **Base SHA at scout:** `d0fac9d7` (very stale vs `8c6d4f78` / `aa9ce55f`)
- **merge_commit_sha:** null (DIRTY / conflict)

**Comment 2026-08-14 (itchyshin):** `test(1.10)` failure was
`src/variational.jl` Hessian, not this docs-only diff; fixed on `main` by
[#408](https://github.com/itchyshin/DRM.jl/pull/408). Re-running CI on current
`main` should clear that check. Last recorded #406 `test (1.10)` is still
FAILURE on the 2026-08-14 run.

**What is already live on `main`:** coordination-board already states option A
(`allow_auto_merge` ON; agents may `gh pr merge N --auto --merge`) and still
points at OPEN #406 for the durable decision file. The dedicated decision +
check-log files from #406 are **not** on `main`.

**Why not merge:** REST `mergeable_state=dirty` against current `main`.
`coordination-board.md` has moved many times since `d0fac9d7`. Shinichi away;
conflict risk; not engine. Leave OPEN.

---

## After vacation (owner)

1. If `LOOP/checkpoint.md` on `main` already records the loop items #420 wanted
   (or a later checkpoint superseded them) → close #420 as superseded.
2. If the durable auto-merge decision should live as
   `docs/dev-log/decisions/2026-08-13-github-auto-merge.md` → rebase #406
   docs-only (expect `coordination-board.md` conflict) or cherry-pick the two
   new files and close the rest.
3. Do not use `--admin`. Do not mix either rebase with an engine G0.

---

## This slice did not

- Merge #420 or #406
- Rebase either branch
- Touch `src/`
- Open a new issue
- Flip a capability chip
- Reopen #49
