# coordination-board.md — branch / PR overlap

Check this before editing `AGENTS.md` / `CLAUDE.md` / `ROADMAP.md` / `src/`.
Record active branches and which files they touch so two agents don't collide.

| Branch | Owner | Touching | Status |
|---|---|---|---|
| `phase-0-team-workflows` | Shannon (Claude) | scaffold: `AGENTS.md`, `CLAUDE.md`, `ROADMAP.md`, `.claude/workflows/`, `.codex/agents/`, `docs/`, `.github/`, project meta. `src/` only a 1-word docstring fix (`twin`). | scaffold complete + verified (load + docs build); awaiting commit decision |
