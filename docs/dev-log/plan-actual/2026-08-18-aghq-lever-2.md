# Plan vs actual — AGHQ lever 2 (2026-08-18)

**Plan:** `LOOP/ultra-plan.md` / `docs/dev-log/plans/2026-08-18-aghq-lever-2-ultra-plan.md`
**Issue:** #448. **Branch:** `claude/lane-aghq-lever-2`.
**Worktree:** `~/local-scratch/lanes/DRM.jl-aghq-lever-2` (not handover).

| Arc | Plan | Actual |
|---|---|---|
| S0 | Issue + worktree + LOOP kit | Done (`0955569f`). #448. |
| S1 | `src/aghq_1d.jl` wrap `_gauss_hermite` | Done. Fail-loud dim≠1. No GLLVM vendor. |
| S2 | TDD `test/test_aghq_1d.jl` first | Done. RED (`UndefVarError`) then GREEN 9/9. Smoke is nll **agreement** (absdiff 0.00397), not recovery. |
| S3 | `marginal=:AGHQ` + `nAGQ=5` on Poisson `(1\|g)` | Done. `_aghq_reject` on phylo / crossed / relmat / `(1+x\|g)` / FE / other families / associate_pairs. No `:REML`×`:AGHQ`. Default `:LA` unchanged. |
| S4 | Mac-local Pkg.test; read the LOG | New-file suite 46/46. `pathof(DRM)` = this worktree. Numbers in after-task. No Totoro / Cell D. |
| S5 | Docstrings + example; chip stays missing | Poisson worked example. Capability row **still `missing`**. check-log.d + after-task + Rose table. |
| S6 | One PR `closes #448`; do not merge | PR opened; merge is a human gate. |
| S7 | Mechanical verify | Issue exists; worktree ≠ handover; no phylo-Laplace hunk; no q4; no chip flip; no GLLVM LOOP; no #420/#406; tests read from LOG. |
| S8 | Rose + Noether claim audit | In after-task (Other Models skipped). |
| S9 | This file | Meaningful close, not N/A. |

**TDD order vs S1-then-S2 label:** failing test was written before the kernel
(user override). S1 production code followed RED.

**Not done (fences):** Cox–Reid on `_fit_poisson_general_laplace`; tensor AGHQ;
capability flip; ADEMP recovery; merge.
