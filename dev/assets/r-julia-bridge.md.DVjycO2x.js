import{_ as a,o as t,c as s,ao as i}from"./chunks/framework.DGf-9kxS.js";const h=JSON.parse('{"title":"R ↔ Julia bridge","description":"","frontmatter":{},"headers":[],"relativePath":"r-julia-bridge.md","filePath":"r-julia-bridge.md","lastUpdated":null}'),o={name:"r-julia-bridge.md"};function r(n,e,l,d,c,u){return t(),s("div",null,[...e[0]||(e[0]=[i('<h1 id="R-Julia-bridge" tabindex="-1">R ↔ Julia bridge <a class="header-anchor" href="#R-Julia-bridge" aria-label="Permalink to &quot;R ↔ Julia bridge {#R-Julia-bridge}&quot;">​</a></h1><div class="tip custom-block"><p class="custom-block-title">Status — Experimental first slice (Phase 1.5)</p><p>DRM.jl now exposes <code>drm_bridge()</code>, a marshalling-friendly entry point for the R-side <code>drmTMB(formula, ..., engine = &quot;julia&quot;)</code> glue. The companion R glue lives in the <strong>drmTMB R repository</strong> via <a href="https://github.com/JuliaInterop/JuliaCall" target="_blank" rel="noreferrer">JuliaCall</a>. The first tested bridge slice covers Gaussian one-response and two-response fits; broader families wait for coefficient-scale parity tests. For translating R syntax to Julia by hand, see the <a href="./rosetta">Rosetta page</a>.</p></div><h2 id="The-idea" tabindex="-1">The idea <a class="header-anchor" href="#The-idea" aria-label="Permalink to &quot;The idea {#The-idea}&quot;">​</a></h2><p>Two ways to use DRM.jl from R, in increasing integration:</p><ol><li><p><strong>Translate by hand</strong> — rewrite the model in Julia using <code>drm</code> / <code>bf</code>. The <a href="./rosetta">Rosetta</a> phrasebook is the lookup table. Available today.</p></li><li><p><strong><code>engine = &quot;julia&quot;</code></strong> — keep writing ordinary <code>drmTMB(...)</code> R code; drmTMB marshals the formula and data across JuliaCall, calls DRM.jl to fit, and returns a result object shaped like a native drmTMB fit. Experimental for Gaussian one-response and two-response models, plus the first Gaussian <code>phylo(1 | species)</code> mean bridge with constant <code>sigma</code>.</p></li></ol><h2 id="The-DRM.jl-side-contract" tabindex="-1">The DRM.jl-side contract <a class="header-anchor" href="#The-DRM.jl-side-contract" aria-label="Permalink to &quot;The DRM.jl-side contract {#The-DRM.jl-side-contract}&quot;">​</a></h2><p>For the bridge to work, DRM.jl exposes a stable, marshalling-friendly surface:</p><ul><li><p><strong>Formula</strong> — the R <code>bf(mu = y ~ x, sigma = ~ x, ...)</code> is mapped to DRM.jl&#39;s <code>bf(...)</code> (see the <a href="./developer-notes/formula-grammar">Formula grammar</a> and Rosetta pages for the exact spelling map);</p></li><li><p><strong>Data</strong> — an R <code>data.frame</code> crosses as a column table (<code>NamedTuple</code> / <code>DataFrame</code>) keyed by the same column names;</p></li><li><p><strong>Result</strong> — <code>drm_bridge()</code> returns a flat dictionary with coefficient names and values, covariance matrix, likelihood summaries, convergence state, fitted values, residuals, fitted scale, and residual-correlation payloads when present.</p></li></ul><p>For the Gaussian phylogenetic mean cell, the current <code>algorithm = :auto</code> route uses the all-node sparse L-BFGS fitter in <code>src/location_only.jl</code>. That route profiles the mean coefficients by sparse GLS, uses exact Takahashi trace gradients for the residual and phylogenetic standard deviations, and returns a finite mean-coefficient covariance block. Scale and variance-component covariance are still left unset for the R bridge, so profile/bootstrap work remains the next inference slice.</p><h2 id="Open-design-questions" tabindex="-1">Open design questions <a class="header-anchor" href="#Open-design-questions" aria-label="Permalink to &quot;Open design questions {#Open-design-questions}&quot;">​</a></h2><p>Tracked in the issue ledger:</p><ul><li><p><strong>Broader phylo / pedigree / relatedness marshalling</strong> — the first Newick tree slice works for one Gaussian <code>phylo(1 | species)</code> mean term; <code>Ainv</code>, <code>K</code>, multiple structured terms, slopes, and non-Gaussian phylogenetic models still need separate parity tests (issue #19).</p></li><li><p><strong>Result-shape parity</strong> — exact field-by-field equivalence between a native drmTMB fit and the Julia-engine fit (issue #5), guarded by the R-parity suite (Workflow G, issue #17).</p></li><li><p><strong>Round-trip <code>bf()</code> formulas</strong> — an R formula and its Julia translation must describe the same model; the parity tests enforce this once R is available in CI.</p></li></ul><p>Until broader parity ships, use the bridge for Gaussian one-response, two-response, and the admitted Gaussian phylogenetic smoke runs, and use hand-translation via the Rosetta phrasebook or native <code>drmTMB</code> for the remaining families and unsupported formula features.</p>',13)])])}const m=a(o,[["render",r]]);export{h as __pageData,m as default};
