import{_ as a,o as t,c as i,ao as s}from"./chunks/framework.6hAVKwXH.js";const m=JSON.parse('{"title":"Laplace vs variational marginals","description":"","frontmatter":{},"headers":[],"relativePath":"model-guides/marginal-la-vs-va.md","filePath":"model-guides/marginal-la-vs-va.md","lastUpdated":null}'),o={name:"model-guides/marginal-la-vs-va.md"};function n(r,e,l,d,h,p){return t(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="Laplace-vs-variational-marginals" tabindex="-1">Laplace vs variational marginals <a class="header-anchor" href="#Laplace-vs-variational-marginals" aria-label="Permalink to &quot;Laplace vs variational marginals {#Laplace-vs-variational-marginals}&quot;">​</a></h1><div class="tip custom-block"><p class="custom-block-title">Status — Experimental (#136 open)</p><p>A variational (VA / ELBO) marginal is an <strong>opt-in Experimental</strong> alternative for random-intercept <code>(1 | g)</code> models. <strong>Laplace remains the default</strong> — the same family of method drmTMB and TMB use.</p><p><strong>Public path today (does not close #136):</strong> Poisson, Binomial, NegBinomial2, Gamma, and Beta <code>(1 | g)</code> via <code>drm(...; marginal = :VA)</code> (scale families need <code>sigma ~ 1</code>). That <code>loglik</code> is an ELBO, not a Laplace log-likelihood. Mixed LA/VA AIC / LRT errors. Phylo / crossed / correlated slopes / ZI / hurdle / <code>sigma ~ x</code> stay unwired — not “implemented everywhere.” Public Gamma RI LA-vs-VA smoke: <code>report/va-vs-laplace-bias.md</code> (#136e scoped; #136 stays open).</p></div><h2 id="What-&quot;the-marginal&quot;-is,-and-why-it-matters" tabindex="-1">What &quot;the marginal&quot; is, and why it matters <a class="header-anchor" href="#What-&quot;the-marginal&quot;-is,-and-why-it-matters" aria-label="Permalink to &quot;What &quot;the marginal&quot; is, and why it matters {#What-&quot;the-marginal&quot;-is,-and-why-it-matters}&quot;">​</a></h2><p>When a model has random effects <code>z</code>, <code>drm</code> does not maximise the joint likelihood of data and random effects directly. It integrates the random effects out, leaving a <em>marginal</em> likelihood that depends only on the fixed parameters and the variance components:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>L(θ) = ∫ p(y | z, θ) p(z | θ) dz.</span></span></code></pre></div><p>That integral has no closed form for non-Gaussian families, so it must be approximated. The quality of the approximation is not a side detail: it is what the dispersion, shape, and zero-inflation parameters are estimated <em>against</em>. A biased marginal biases exactly those parameters.</p><h3 id="The-Laplace-approximation-today&#39;s-default" tabindex="-1">The Laplace approximation (today&#39;s default) <a class="header-anchor" href="#The-Laplace-approximation-today&#39;s-default" aria-label="Permalink to &quot;The Laplace approximation (today&#39;s default) {#The-Laplace-approximation-today&#39;s-default}&quot;">​</a></h3><p>LA replaces the integrand with a Gaussian centred at the posterior mode of <code>z</code>, matched in curvature (the Hessian) at that mode. It is fast — one inner mode solve per outer step — and for a Gaussian random effect on the mean it is <em>exact</em>, because the integrand really is Gaussian (this is why a mean random intercept in a Gaussian model needs no approximation at all).</p><p>The trouble starts when the integrand is <strong>not</strong> close to Gaussian:</p><ul><li><p><strong>Skewed or heavy-tailed posteriors</strong> — a single mode-plus-curvature match understates the mass in the tail, so the integral, and the variance/shape parameters tied to it, are off.</p></li><li><p><strong>Multimodal posteriors</strong> — LA sees one mode and is blind to the others; which mode it lands on can depend on the optimiser, the OS, or the BLAS.</p></li><li><p><strong>Dispersion / shape / zero-inflation parameters</strong> — these read the <em>shape</em> of the integrand, not just its peak, so they absorb the approximation error first. Mean (location) parameters are comparatively robust.</p></li></ul><h2 id="Concrete-evidence-from-the-sister-project-GLLVM.jl" tabindex="-1">Concrete evidence (from the sister project GLLVM.jl) <a class="header-anchor" href="#Concrete-evidence-from-the-sister-project-GLLVM.jl" aria-label="Permalink to &quot;Concrete evidence (from the sister project GLLVM.jl) {#Concrete-evidence-from-the-sister-project-GLLVM.jl}&quot;">​</a></h2><p>DRM.jl is a sister of GLLVM.jl, which fits the same kind of latent-variable integrals and has measured where LA bites:</p><ul><li><p><strong>Two-part Gamma shape.</strong> In a two-part (hurdle) Gamma model, the Gamma shape parameter <code>α</code> was recovered roughly <strong>7× too low</strong> under Laplace. The mean was fine; the shape — which is read off the curvature of a skewed positive density — was badly biased.</p></li><li><p><strong>ZINB multimodality.</strong> In a zero-inflated negative binomial model the zero-inflation probability <code>π</code> and the low-count-mean intercept <code>βc</code> trade off: a zero can be &quot;structural&quot; (<code>π</code>) or &quot;a Poisson/NB zero from a small mean&quot; (<code>βc</code>). That gives the marginal <strong>two modes</strong>, and the sign of the count intercept was observed to <strong>flip across OS / BLAS</strong> — a hallmark of LA picking different modes on different platforms.</p></li></ul><p>Neither failure is a bug in the optimiser; both are the geometry the Laplace approximation cannot see.</p><h2 id="The-variational-VA-/-ELBO-proposal" tabindex="-1">The variational (VA / ELBO) proposal <a class="header-anchor" href="#The-variational-VA-/-ELBO-proposal" aria-label="Permalink to &quot;The variational (VA / ELBO) proposal {#The-variational-VA-/-ELBO-proposal}&quot;">​</a></h2><p>The variational path replaces &quot;find one mode and match curvature&quot; with &quot;fit a whole approximating distribution.&quot; We choose a factorised Gaussian</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>q(z) = N(m, diag(v)),</span></span></code></pre></div><p>and pick <code>m</code> and <code>v</code> to maximise the <strong>evidence lower bound</strong> (ELBO):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ELBO(θ, m, v) = E_q[ log p(y, z | θ) ] − E_q[ log q(z) ]  ≤  log L(θ).</span></span></code></pre></div><p>The ELBO is a <em>provable lower bound</em> on the true log marginal, which is the property that makes it well-behaved as an objective: optimising it cannot silently chase a spurious peak the way a mode-match can.</p><p>The expectations under a Gaussian <code>q</code> are tractable in the two regimes DRM.jl needs:</p><ul><li><p><strong>Closed form</strong> when the log-density is linear in the linear predictor <code>η</code> and in <code>e^{±η}</code> — this covers <strong>Poisson</strong> and <strong>Gamma</strong>, because the Gaussian expectations of <code>η</code> and of <code>e^{η}</code> (a log-normal moment) are both analytic.</p></li><li><p><strong>One-dimensional Gauss–Hermite quadrature</strong> for everything else — <strong>Binomial</strong>, <strong>negative binomial</strong>, and <strong>Beta</strong> — where the expectation reduces to a single integral over the scalar <code>η</code>, cheaply and accurately evaluated with a handful of GH nodes.</p></li></ul><p>Because <code>q</code> carries a <em>variance</em> <code>v</code>, not just a location, it represents tail and spread directly, so the shape and dispersion parameters are no longer estimated against a curvature match at a single point.</p><h2 id="When-to-use-which" tabindex="-1">When to use which <a class="header-anchor" href="#When-to-use-which" aria-label="Permalink to &quot;When to use which {#When-to-use-which}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Situation</th><th style="text-align:right;">Recommendation</th></tr></thead><tbody><tr><td style="text-align:right;">Fixed-effects-only model</td><td style="text-align:right;">VA adds nothing — there is no latent integral to approximate.</td></tr><tr><td style="text-align:right;">Gaussian random effect on the mean</td><td style="text-align:right;">VA adds nothing — LA is already exact here.</td></tr><tr><td style="text-align:right;">Ordinary Gamma <code>(1|g)</code> shape</td><td style="text-align:right;">LA ≈ VA in the #136e smoke; <strong>prefer LA</strong> (15–20× faster warm).</td></tr><tr><td style="text-align:right;">Two-part / hurdle / ZINB geometry</td><td style="text-align:right;">VA may help (GLLVM evidence) — <strong>not a public DRM path yet</strong>.</td></tr><tr><td style="text-align:right;">Speed-critical fits</td><td style="text-align:right;">LA — default; one inner solve per step.</td></tr></tbody></table><p>In short: <strong>LA is faster and is the default.</strong> On the public Gamma random-intercept cell, Julia matches the R/TMB pattern: the two marginals agree on <code>α</code> and LA wins on time (<code>report/va-vs-laplace-bias.md</code>). VA stays an opt-in for the bias-sensitive <em>two-part / ZI</em> cells — those are still unwired here.</p><h2 id="The-public-API-Experimental" tabindex="-1">The public API (Experimental) <a class="header-anchor" href="#The-public-API-Experimental" aria-label="Permalink to &quot;The public API (Experimental) {#The-public-API-Experimental}&quot;">​</a></h2><p>The marginal is selected with <code>marginal</code> (not Gaussian <code>method = :ML/:REML</code>). LA remains the default:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># default — Laplace, as today</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">drm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; marginal </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :LA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># opt-in variational marginal (Experimental: \`(1 | g)\` on Poisson / Binomial /</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># NegBinomial2 / Gamma / Beta; scale families need sigma ~ 1)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">drm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; marginal </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> :VA</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Everything else about the call — the <code>bf(...)</code> formulas, the family, the data — stays the same; only how the random effects are integrated out changes. <code>method = :VA</code> on non-Gaussian families is rejected with a pointer to <code>marginal</code>.</p><h2 id="How-we-trust-it-anchors-on-tip" tabindex="-1">How we trust it (anchors on tip) <a class="header-anchor" href="#How-we-trust-it-anchors-on-tip" aria-label="Permalink to &quot;How we trust it (anchors on tip) {#How-we-trust-it-anchors-on-tip}&quot;">​</a></h2><p>The Experimental <code>(1 | g)</code> path is gated by deterministic anchors in <code>test/test_variational.jl</code> (and per-family ELBO tests) — checks with a known answer, not just &quot;the numbers look plausible&quot;: 2. <strong>Variance → 0 collapses to independence.</strong> As the random-effect variance is driven to zero there is nothing left to integrate, so the ELBO equals the ordinary independent log-likelihood. This pins the no-RE limit exactly.</p><ol start="3"><li><p><strong>ELBO ≤ dense quadrature.</strong> At low latent dimension the true marginal is computed by dense <em>adaptive</em> Gauss–Hermite. The ELBO, being a lower bound, must sit at or below it — never above. (Non-adaptive engine GHQ centred at 0 can sit below the ELBO; that is not a counterexample.)</p></li><li><p><strong>Family limits.</strong> The negative binomial as its size <code>r → ∞</code> becomes Poisson, so NB2-VA converges to Poisson-VA on a shared fixture.</p></li></ol><p>The scoped <strong>#136e</strong> public-path report is <code>report/va-vs-laplace-bias.md</code>: on Gamma <code>(1 | g)</code>, LA ≈ VA on shape <code>α</code> and LA is much faster. Closing #136 still needs public VA beyond random intercept (phylo / crossed / ZI / hurdle) and any two-part bias cell — those are not claimed here.</p><h2 id="A-place-DRM.jl-can-exceed-drmTMB" tabindex="-1">A place DRM.jl can exceed drmTMB <a class="header-anchor" href="#A-place-DRM.jl-can-exceed-drmTMB" aria-label="Permalink to &quot;A place DRM.jl can exceed drmTMB {#A-place-DRM.jl-can-exceed-drmTMB}&quot;">​</a></h2><p>drmTMB is built on TMB, which is <strong>Laplace-only</strong>. Offering a variational marginal alongside LA is therefore not parity work — it is a capability drmTMB does not have. That option is useful <strong>only</strong> where Laplace is known to fail (two-part shape, ZINB multimodality). On ordinary Gamma <code>(1 | g)</code>, the #136e smoke does <strong>not</strong> show a VA accuracy edge; prefer the default Laplace, as in R.</p><h2 id="See-also" tabindex="-1">See also <a class="header-anchor" href="#See-also" aria-label="Permalink to &quot;See also {#See-also}&quot;">​</a></h2><ul><li><a href="./which-scale">Which scale are you modelling?</a> · <a href="./convergence">Improving convergence</a></li></ul>`,38)])])}const g=a(o,[["render",n]]);export{m as __pageData,g as default};
