import{_ as t,o,c as s,ao as n}from"./chunks/framework.dnO2EFF9.js";const p=JSON.parse('{"title":"Toward an O(p) sparse multi-component LSS engine","description":"","frontmatter":{},"headers":[],"relativePath":"developer-notes/lss-sparse-multi-component.md","filePath":"developer-notes/lss-sparse-multi-component.md","lastUpdated":null}'),a={name:"developer-notes/lss-sparse-multi-component.md"};function i(r,e,d,c,l,g){return o(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="Toward-an-Op-sparse-multi-component-LSS-engine" tabindex="-1">Toward an O(p) sparse multi-component LSS engine <a class="header-anchor" href="#Toward-an-Op-sparse-multi-component-LSS-engine" aria-label="Permalink to &quot;Toward an O(p) sparse multi-component LSS engine {#Toward-an-Op-sparse-multi-component-LSS-engine}&quot;">​</a></h1><p>Developer note for a <strong>future</strong> <code>src/gaussian_sparse_lss_multi.jl</code> (no code exists yet — this is the symbolic-alignment pass required before writing any). Scope: extend <code>#551</code>&#39;s single-phylo-component O(p) sparse engine (<code>src/gaussian_sparse_lss.jl</code>) to <strong>several</strong> <code>sd()</code> submodels in one fit, the way <code>#555</code>&#39;s dense engine (<code>_fit_gaussian_lss_multi</code>, <code>src/gaussian_lss.jl:564</code>) already does. Issue #563, Phase-2 slice S7b.</p><p>Reviewer contract item 6 (<code>docs/dev-log/plans/2026-08-30-julia-r-parity.md</code>, &quot;Reviewer-required contracts&quot;): <em>&quot;Shared component precision uses full cross-component incidence products. Check nested/crossed groups, graph fill and memory. No universal O(p) claim for arbitrary dense/crossed structures.&quot;</em> This note&#39;s job is to measure that fill, not assume it.</p><hr><h2 id="1.-The-model" tabindex="-1">1. The model <a class="header-anchor" href="#1.-The-model" aria-label="Permalink to &quot;1. The model {#1.-The-model}&quot;">​</a></h2><p>Per-observation Gaussian response with a heteroscedastic residual and <code>m</code> independent variance components, each with its own log-SD submodel (<code>src/gaussian_lss.jl:544–552</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>y_i = (Xμ βμ)_i + Σ_{c=1}^m (Z_c a_c)_i + e_i</span></span>
<span class="line"><span>e_i ~ N(0, σ_e,i²),        log σ_e,i = (Xσ βσ)_i</span></span>
<span class="line"><span>a_c ~ N(0, D_c K_c D_c),   log σ_c,g = (Z_c α_c)_g   (g = 1..G_c)</span></span>
<span class="line"><span>D_c = diag(σ_c,·) = diag(exp(Z_c α_c))</span></span>
<span class="line"><span>K_c = I_{G_c}                for an iid component</span></span>
<span class="line"><span>K_c = phylogenetic corr.     for the (at most one) phylogenetic component</span></span></code></pre></div><p>Marginal, as already assembled by the dense route:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>V = Σ_c Z_c D_c K_c D_c Z_cᵀ + diag(σ_e,i²)          (gaussian_lss.jl:548–552, 585–599)</span></span></code></pre></div><p><strong>Log-SD combination is two different operations, and the code draws the line at the component boundary:</strong></p><ul><li><p><em>Within</em> one component, several predictor columns combine <strong>additively on the log scale</strong>, i.e. <strong>multiplicatively on the SD scale</strong>: <code>σ_c,g = exp(Z_c[g,:]·α_c) = Π_k exp(Z_c[g,k]·α_c,k)</code> (<code>gaussian_lss.jl:582</code>, <code>σa = exp.(c.Zg * α)</code>; the single-component sparse engine&#39;s <code>ησa = Zg * α; σa = exp.(ησa)</code> at <code>gaussian_sparse_lss.jl:50–52</code> is the same identity for one component).</p></li><li><p><em>Across</em> components the covariances <strong>add</strong>, not multiply — line <code>Vm[i, j] += Σc[...]</code> runs once per component inside the <code>for (ci, c) in enumerate(comps)</code> loop (<code>gaussian_lss.jl:576, 606</code>). This is the ordinary independent-variance-components model, not a product-of-SDs across components.</p></li></ul><p><strong>IN SCOPE for a sparse extension:</strong> one phylogenetic component (as #551) plus any number of <strong>iid</strong> components, where every iid grouping is <strong>nested</strong> in the sense §2 measures (a clean partition — no observation&#39;s group membership is independent of the other components&#39; membership). This is the regime the augmented-tree-plus-hub construction stays close to O(p) in.</p><p><strong>OUT OF SCOPE, with reason:</strong> two or more components whose group memberships are mutually <strong>crossed at comparable scale</strong> (e.g. two ~200-level iid factors with independent random assignment). §2 measures this filling catastrophically regardless of elimination order — it is a structural property of the graph, not an implementation gap. The existing <strong>dense</strong> multi-component route already fits such models today (fixture B below is a real crossed example) up to its <code>n ≤ 5000</code> cap (<code>gaussian_lss.jl:570</code>); this note is only about whether an O(p) <em>sparse</em> alternative exists, and for that topology it does not.</p><hr><h2 id="2.-The-augmented-precision" tabindex="-1">2. The augmented precision <a class="header-anchor" href="#2.-The-augmented-precision" aria-label="Permalink to &quot;2. The augmented precision {#2.-The-augmented-precision}&quot;">​</a></h2><p>Stack the latent state <code>z = (a_phy; a_iid,1; …; a_iid,m)</code> (tree nodes <code>q = 2p-2</code>, <code>gaussian_sparse_lss.jl:19</code>, plus each iid component&#39;s <code>G_c</code> group levels). The augmented precision generalises <code>H = Qs + Diagonal(diag_ZtWZ)</code> (<code>gaussian_sparse_lss.jl:41, 63</code>) to a block matrix with genuine <strong>off-diagonal cross-component blocks</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>H = blockdiag(Q_phy, D_iid,1⁻¹, …, D_iid,m⁻¹) + Zᵀ W Z</span></span>
<span class="line"><span>Z = [Z_phy | Z_iid,1 | … | Z_iid,m]     (n rows; one leaf/group column-block per component)</span></span></code></pre></div><p><code>Zᵀ W Z</code>&#39;s diagonal blocks are exactly <code>diag_ZtWZ</code> per component (each observation touches exactly one leaf and one group per component, so those blocks stay diagonal — no change from #551). The <strong>cross blocks</strong> <code>(Z_k)ᵀ W Z_l</code>, <code>k ≠ l</code>, are new: entry <code>(t, g)</code> is <code>Σ_i w_i·[obs i has component-k index t AND component-l index g]</code>. Their nonzero pattern is the species↔group <strong>incidence bipartite graph</strong>, and that pattern — not the tree — governs symbolic-Cholesky fill.</p><h3 id="2.1-Measured-fill-CHOLMOD-choleskySymmetricH,-default-AMD-ordering" tabindex="-1">2.1 Measured fill (CHOLMOD <code>cholesky(Symmetric(H))</code>, default AMD ordering) <a class="header-anchor" href="#2.1-Measured-fill-CHOLMOD-choleskySymmetricH,-default-AMD-ordering" aria-label="Permalink to &quot;2.1 Measured fill (CHOLMOD \`cholesky(Symmetric(H))\`, default AMD ordering) {#2.1-Measured-fill-CHOLMOD-choleskySymmetricH,-default-AMD-ordering}&quot;">​</a></h3><p>Probe: <code>probe_s7b_fill.jl</code> in the session scratchpad (log: <code>probe_s7b_fill.log</code>, same directory), run time &lt; 0.2 s total. <code>H</code> built with random positive weights (fill depends only on the <em>pattern</em>, not the values); <code>nnz(L)</code> from <code>nnz(sparse(cholesky(Symmetric(H)).L))</code>.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Case</th><th style="text-align:right;">Topology</th><th style="text-align:right;">dim(H)</th><th style="text-align:right;">nnz(H)</th><th style="text-align:right;">nnz(L)</th><th style="text-align:right;">ratio L/H</th><th style="text-align:right;">nnz(L)/dim</th></tr></thead><tbody><tr><td style="text-align:right;">(a)</td><td style="text-align:right;">phylo(1000 tips) + iid(50), <strong>nested</strong> in species (partition)</td><td style="text-align:right;">2048</td><td style="text-align:right;">8028</td><td style="text-align:right;">6213</td><td style="text-align:right;"><strong>0.774</strong></td><td style="text-align:right;">3.03</td></tr><tr><td style="text-align:right;">(b)</td><td style="text-align:right;">phylo(1000 tips) + iid(50), <strong>crossed</strong> with species</td><td style="text-align:right;">2048</td><td style="text-align:right;">15578</td><td style="text-align:right;">23920</td><td style="text-align:right;"><strong>1.535</strong></td><td style="text-align:right;">11.68</td></tr><tr><td style="text-align:right;">(c)</td><td style="text-align:right;">two iid(200,200), fully <strong>crossed</strong>, n=5000, no phylo</td><td style="text-align:right;">400</td><td style="text-align:right;">9848</td><td style="text-align:right;">56141</td><td style="text-align:right;"><strong>5.701</strong></td><td style="text-align:right;">140.35</td></tr></tbody></table><p><code>(a)</code> vs <code>(b)</code> isolate one variable (nested vs. crossed group membership, same sizes): crossing costs a <strong>~3.85× increase in nnz(L)</strong> (6213 → 23920), but the result is still <code>&lt; nnz(H)^{1.something}</code> and far from dense — because one side of the incidence graph (the 50 groups) is small relative to the tree (1998 nodes), so CHOLMOD can defer group elimination and absorb species fill into their tree ancestors instead of forming a group-species clique.</p><p>Scaling sweep (<code>G2 = 50</code> fixed, <code>n = 5p</code>, <code>p ∈ {1000, 2000, 4000}</code> — same script, log tail):</p><table tabindex="0"><thead><tr><th style="text-align:right;">p</th><th style="text-align:right;">nested nnz(L)/p</th><th style="text-align:right;">crossed nnz(L)/p</th></tr></thead><tbody><tr><td style="text-align:right;">1000</td><td style="text-align:right;">3.03</td><td style="text-align:right;">11.62</td></tr><tr><td style="text-align:right;">2000</td><td style="text-align:right;">3.02</td><td style="text-align:right;">11.46</td></tr><tr><td style="text-align:right;">4000</td><td style="text-align:right;">3.01</td><td style="text-align:right;">11.32</td></tr></tbody></table><p>Both stay <strong>flat as p grows with G2 fixed</strong> — i.e. for <em>this</em> topology (one large tree-structured component, one small fixed-size crossed factor) the measured fill is O(p) empirically over this range, not just for the nested case. <strong>This does not extend to case (c):</strong> <code>nnz(L)/dim = 140</code> on a 400×400 system (70% of the dense 400² entries) shows that when <strong>both</strong> crossed factors are large (comparable size to each other, no small &quot;hub&quot; side), no elimination order rescues sparsity — the classic two-way crossed-random-effects fill result. Contract item 6&#39;s caution is confirmed for that topology specifically, not for &quot;any crossing.&quot;</p><h3 id="2.2-Real-fixture-check" tabindex="-1">2.2 Real fixture check <a class="header-anchor" href="#2.2-Real-fixture-check" aria-label="Permalink to &quot;2.2 Real fixture check {#2.2-Real-fixture-check}&quot;">​</a></h3><p><code>probe_s7b_fixtureB.jl</code> (log: <code>probe_s7b_fixtureB.log</code>, both in the session scratchpad) reruns the same measurement on the <strong>actual committed</strong> <code>#555</code> fixture B design (32-tip tree, 16 studies genuinely crossed with species — verified in §5 of the recon: each study spans ~12 species, each species appears in ~6 studies):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>fixture B (real, crossed):        dim(H)=78  nnz(H)=582  nnz(L)=783  ratio=1.345</span></span>
<span class="line"><span>same sizes, nested comparator:    dim(H)=78  nnz(H)=262  nnz(L)=208  ratio=0.794</span></span></code></pre></div><p>Same qualitative signature as the synthetic sweep (crossed ratio &gt; 1, nested &lt; 1) at fixture scale — this is the &quot;small crossed fixture&quot; §5 uses as an acceptance oracle.</p><h3 id="2.3-When-symbolic-Cholesky-stays-Op-—-summary" tabindex="-1">2.3 When symbolic Cholesky stays O(p) — summary <a class="header-anchor" href="#2.3-When-symbolic-Cholesky-stays-Op-—-summary" aria-label="Permalink to &quot;2.3 When symbolic Cholesky stays O(p) — summary {#2.3-When-symbolic-Cholesky-stays-Op-—-summary}&quot;">​</a></h3><ul><li><p><strong>Nested groups</strong> (partition-compatible with the tree, <code>case (a)</code>): O(p), confirmed flat to p=4000.</p></li><li><p><strong>One phylo component + a small crossed iid factor</strong> (<code>case (b)</code>, <code>G_c ≪ p</code>): empirically near-linear over the measured range, but <strong>not proven asymptotically</strong> — G2 was held fixed at 50 while p grew; the argument for why (a hub-and-tree pattern lets AMD defer the small side) would need to be checked again if <code>G_c</code> also scales with <code>p</code>.</p></li><li><p><strong>Two or more crossed components at comparable scale</strong> (<code>case (c)</code>): <strong>NOT O(p)</strong> — fill is a fixed fraction of the dense matrix even at trivial size. <strong>No universal O(p) claim</strong> for this topology, as the contract requires.</p></li></ul><p><strong>Caveat (added after adversarial re-probing, §8 finding 2 — <code>review_s7b_fill2.jl/.log</code>): the flatness above was measured only under uniform group sizes and a balanced tree.</strong> Two further sweeps, same in-scope nested topology (<code>G2 = 50</code>, balanced or caterpillar tree, <code>n = 5p</code>):</p><ul><li><p><strong>Unbalanced group sizes</strong> (90% of species in one giant group, the remaining 10% spread over the other 49): <code>nnz(L)/p</code> = 6.77 → 7.06 → 7.24 at p = 1000/2000/4000 — a real, if mild, <strong>upward drift</strong> (+7% over a 4× increase in <code>p</code>) that the uniform-size sweep never surfaces, and one that has not visibly asymptoted by p=4000. <strong>What would settle it:</strong> the same sweep carried to p=16,000–32,000 to see whether <code>nnz(L)/p</code> converges or keeps climbing.</p></li><li><p><strong>Caterpillar tree</strong> (<code>random_caterpillar_tree</code>, the worst case for CHOLMOD ordering — see its docstring) instead of <code>random_balanced_tree</code>, nested <code>G2=50</code>: <code>nnz(L)/p</code> stays flat (6.03/6.02/6.01 at p=1000/2000/4000) but at <strong>~2× the balanced-tree constant</strong> (≈3.0) — a level shift, not growth, so still O(p), but &quot;flat to p=4000&quot; was only ever demonstrated on one tree shape. <strong>What would settle it:</strong> repeat across the shape sweep #16 already uses elsewhere in this codebase (balanced, caterpillar, and at least one intermediate topology) before treating the constant as shape-independent.</p></li></ul><p>Neither sweep breaks the O(p) classification for the in-scope regime; both narrow what &quot;confirmed&quot; actually covers, which is why they are recorded here rather than folded into the headline claim above.</p><hr><h2 id="3.-The-exact-gradient-—-alignment-table" tabindex="-1">3. The exact gradient — alignment table <a class="header-anchor" href="#3.-The-exact-gradient-—-alignment-table" aria-label="Permalink to &quot;3. The exact gradient — alignment table {#3.-The-exact-gradient-—-alignment-table}&quot;">​</a></h2><p>Generalising #551&#39;s single-component machinery (<code>gaussian_sparse_lss.jl</code>) to the block <code>H</code> of §2. Objective and logdet structure are unchanged in form — <code>logdetV = logdetD + logdetCprior + logdetH</code> (<code>:85–87</code>) becomes <code>logdetV = logdetD + Σ_c logdetCprior,c + logdetH</code> — but every per-leaf loop that walks <code>1:G</code> (the single tree) must become a loop over <strong>all component nodes</strong> (tree leaves ∪ every <code>Σ G_c</code>), and <code>wts[i]</code> generalises to one scaling factor per component per observation.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Symbol</th><th style="text-align:right;">Meaning</th><th style="text-align:right;">Existing code name (file:line) or TO BUILD</th><th style="text-align:right;">Fixture / oracle that pins it</th><th style="text-align:right;">Tolerance</th></tr></thead><tbody><tr><td style="text-align:right;"><code>Q_c</code></td><td style="text-align:right;">prior precision of component <code>c</code> (<code>Q_phy</code> from the tree; <code>D_iid,c⁻¹ = I</code> for iid, since <code>K_c=I</code>)</td><td style="text-align:right;"><code>Qs</code> (<code>gaussian_sparse_lss.jl:20</code>) for the phylo block; <strong>TO BUILD</strong>: trivial diagonal for each iid block</td><td style="text-align:right;">§5 oracle 1 (dense identity)</td><td style="text-align:right;">atol = 1e-5 (loglik)</td></tr><tr><td style="text-align:right;"><code>H</code></td><td style="text-align:right;">augmented block precision, §2</td><td style="text-align:right;"><code>H_template</code>/<code>H</code> (<code>:41, 63</code>) generalises to the block form of §2 — <strong>TO BUILD</strong>: multi-component assembly loop</td><td style="text-align:right;">§5 oracle 1</td><td style="text-align:right;">rtol = 2e-4, atol = 2e-5 (coef)</td></tr><tr><td style="text-align:right;"><code>wts_g = σ_a,g · inv_sd_g</code></td><td style="text-align:right;">the <code>D_a T</code> row-scaling: component SD × Takahashi-derived conditional SD (phylo) or component SD alone (iid, <code>inv_sd ≡ 1</code>)</td><td style="text-align:right;"><code>wts_g</code> (<code>:53</code>), <code>inv_sd</code> (<code>:27</code>) — generalise per component</td><td style="text-align:right;">§5 oracle 2 (FD grad)</td><td style="text-align:right;">grad ≤ 1e-6</td></tr><tr><td style="text-align:right;"><code>b_c = Z_cᵀ W r</code></td><td style="text-align:right;">per-component RHS for <code>â = H⁻¹b</code></td><td style="text-align:right;"><code>b</code> accumulation loop (<code>:75–79</code>) — <strong>TO BUILD</strong>: one accumulation per component into the shared <code>b</code> vector at the component&#39;s block offset</td><td style="text-align:right;">§5 oracle 1</td><td style="text-align:right;">rtol = 2e-4, atol = 2e-5 (coef)</td></tr><tr><td style="text-align:right;"><code>â = H⁻¹ b</code></td><td style="text-align:right;">joint BLUP over all components</td><td style="text-align:right;"><code>ch \\ b</code> (<code>:81</code>) — unchanged pattern, bigger <code>H</code></td><td style="text-align:right;">§5 oracle 1</td><td style="text-align:right;">rtol = 2e-4, atol = 2e-5 (coef)</td></tr><tr><td style="text-align:right;"><code>quad_sos</code></td><td style="text-align:right;">zero-cancellation GMRF sum of squares</td><td style="text-align:right;"><code>dot(res_lat, w .* res_lat) + dot(â, Qs*â)</code> (<code>:89</code>) generalises to <code>+ Σ_c dot(â_c, Q_c â_c)</code></td><td style="text-align:right;">§5 oracle 1</td><td style="text-align:right;">atol = 1e-5 (loglik)</td></tr><tr><td style="text-align:right;"><code>Hinv</code> (selected inverse, <strong>full pattern</strong>, not just the diagonal)</td><td style="text-align:right;">O(p) exact <code>H⁻¹</code> entries at every pair the sparse pattern of <code>H</code>/<code>L</code> connects, any component — this now includes <strong>cross-component</strong> pairs <code>(g, t)</code>, <code>g</code> in one component&#39;s block and <code>t</code> in another&#39;s</td><td style="text-align:right;"><code>takahashi_selinv(ch)</code> (<code>:96</code>, <code>src/takahashi_selinv.jl:103</code>) — <strong>works unchanged on the bigger <code>H</code></strong>: <code>takahashi_selinv</code> returns the selected inverse at every nonzero of the Cholesky factor&#39;s fill pattern, and every <code>H[g,t]</code> §2 introduces is by construction already inside that pattern (fill only ever <em>adds</em> to a matrix&#39;s own nonzero set, never removes from it) — so the cross entries needed below cost nothing extra to obtain</td><td style="text-align:right;">§5 oracle 2</td><td style="text-align:right;">grad ≤ 1e-6</td></tr><tr><td style="text-align:right;"><code>g_βμ</code></td><td style="text-align:right;">mean-block gradient</td><td style="text-align:right;"><code>-(Xμ&#39; * u)</code> (<code>:99</code>) — <strong>unchanged</strong>, <code>u</code> only depends on the joint residual</td><td style="text-align:right;">§5 oracle 2</td><td style="text-align:right;">grad ≤ 1e-6</td></tr><tr><td style="text-align:right;"><code>g_βσ</code></td><td style="text-align:right;">residual-scale gradient</td><td style="text-align:right;">loop (<code>:101–111</code>) — <strong>unchanged</strong>: <code>s_ii = Hinv[r_node, r_node]</code> still reads off the (bigger) selected inverse at the phylo-leaf node</td><td style="text-align:right;">§5 oracle 2</td><td style="text-align:right;">grad ≤ 1e-6</td></tr><tr><td style="text-align:right;"><code>g_α,c</code></td><td style="text-align:right;">per-component α gradient, log-det trace term</td><td style="text-align:right;"><strong>corrected formula — diagonal alone is WRONG.</strong> <code>∂H[g,g]/∂α_c,g</code> (own-node term, as #551) <strong>plus</strong> <code>∂H[g,t]/∂α_c,g</code> for every node <code>t</code> of every <em>other</em> component sharing an observation with group <code>g</code> (exactly the cross blocks §2 introduces): <code>trace_term_g = Hinv[g,g]·∂H[g,g]/∂α_c,g + 2·Σ_{t≠g} Hinv[g,t]·∂H[g,t]/∂α_c,g</code>. Single-component #551 has no such <code>t</code> (its only cross partner is the tree, already folded into the diagonal via <code>leaf_pos</code>), so <code>:114–126</code>&#39;s bare <code>s_node = Hinv[r_node,r_node]</code> is the <code>m=1</code> special case, not the general one. <code>quad_term</code> (<code>u[i]*Zâ[i]</code> sum) is unaffected — it only ever needed the fitted <code>â</code>, not <code>Hinv</code>. <strong>TO BUILD</strong></td><td style="text-align:right;">§5 oracle 3 (cross-term gradcheck)</td><td style="text-align:right;">grad ≤ 1e-6</td></tr><tr><td style="text-align:right;">cross-block <code>∂H[g,t]/∂α_c,g</code> accumulation</td><td style="text-align:right;">for every pair of components <code>(c, c&#39;)</code> and every observation <code>i</code> whose row touches both a node <code>g</code> of <code>c</code> and a node <code>t</code> of <code>c&#39;</code>, the derivative of the <em>cross</em> entry <code>H[g,t] = Σ_i w_i·wts_c[i]·wts_{c&#39;}[i]</code> w.r.t. <code>α_c,g</code> is <code>w_i·(∂wts_c[i]/∂α_c,g)·wts_{c&#39;}[i]</code> — a new per-observation accumulation loop, one for every unordered component pair actually crossed by shared observations (at most <code>m(m-1)/2</code> loops, each <code>O(n)</code>)</td><td style="text-align:right;"><strong>TO BUILD</strong>: no #551 analogue exists (single component has only one factor in <code>wts</code>, so this derivative never arose)</td><td style="text-align:right;">§5 oracle 3</td><td style="text-align:right;">grad ≤ 1e-6</td></tr><tr><td style="text-align:right;"><code>Zâ_i</code> (multi)</td><td style="text-align:right;">fitted joint random effect at row <code>i</code></td><td style="text-align:right;"><code>Zâ</code> (<code>:82</code>) generalises to <code>Σ_c wts_c[i]·â_c[node(i,c)]</code></td><td style="text-align:right;">§5 oracle 1</td><td style="text-align:right;">rtol = 2e-4, atol = 2e-5 (coef)</td></tr></tbody></table><p>Every row above resolves to an existing #551 line to <strong>adapt</strong> except the block-assembly loops (<code>H</code>, <code>b</code>, cross-block <code>∂H/∂α</code> accumulation), which are genuinely new code — they are marked <strong>TO BUILD</strong> rather than silently left blank. The <code>g_α,c</code> row was caught wrong-as-first-drafted by the adversarial review (§8, finding 4): a 6-node nested toy (<code>review_s7b_gradcheck.jl/.log</code>, diagonal-only <code>wts_iid = [1,2]</code> groups nested in 4 phylo-like nodes) gives FD <code>d(logdet H)/dα_iid[1] = 1.400168</code>; the diagonal-only formula returns <code>2.669901</code> (<strong>91% error</strong>); the corrected formula with the cross term returns <code>1.400168</code> (agrees to <code>3.4e-10</code>). The fix costs nothing extra to fetch (<code>Hinv[g,t]</code> is already inside the Takahashi pattern, argued above), so this is a specification bug, not a feasibility gap — but it is exactly the bug symbolic-alignment exists to catch before code is written.</p><hr><h2 id="4.-The-REML-correction" tabindex="-1">4. The REML correction <a class="header-anchor" href="#4.-The-REML-correction" aria-label="Permalink to &quot;4. The REML correction {#4.-The-REML-correction}&quot;">​</a></h2><p>Both existing routes use the identical closed form — Patterson–Thompson, one term regardless of component count:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>L_REML = L_ML(θ̂) + ½ logdet(Xμᵀ V⁻¹ Xμ) − ½ pμ log 2π</span></span></code></pre></div><p>Dense multi-component (<code>gaussian_lss.jl:634–638</code>): <code>A = Vfac \\ Xμ; XtVinvX = Xμ&#39;*A; chX = cholesky(...); + 0.5*logdet(chX) - const_pμ</code> — one dense <code>n×pμ</code> solve. The single-component sparse route computes the same quantity via <code>pμ</code> sparse Cholesky <strong>column</strong> backsolves (<code>gaussian_sparse_lss.jl:135– 156</code>, <code>eval_reml</code>): for each column <code>k</code> of <code>Xμ</code>, accumulate <code>bX</code> at the component&#39;s node positions and solve <code>ch \\ bX</code>. <strong>For the sparse multi route, this generalises directly</strong>: <code>bX</code> accumulates contributions from <em>every</em> component&#39;s <code>wts</code> at that column, i.e. the same per-column loop but summing <code>wts_c[i]*wXk[i]</code> into <code>bX</code> at each component&#39;s node for <code>i</code>&#39;s row — no new mathematics, just the same block-accumulation pattern as <code>b</code> in §3. Cost stays <code>O(pμ · nnz(L))</code> sparse backsolves, same order as #551.</p><hr><h2 id="5.-Acceptance-oracles" tabindex="-1">5. Acceptance oracles <a class="header-anchor" href="#5.-Acceptance-oracles" aria-label="Permalink to &quot;5. Acceptance oracles {#5.-Acceptance-oracles}&quot;">​</a></h2><ol start="2"><li><p><strong>Dense-route identity, small nested fixture.</strong> Fixture A (<code>test/fixtures/lsss/lsss_A.csv</code>, 32 species, <code>(1|species) + phylo(1|species)</code>, both <code>sd(species) ~ x</code>) — iid and phylo groupings <strong>coincide</strong> exactly (the strictest nested case). Assert the sparse multi-route&#39;s <code>loglik</code> matches <code>_fit_gaussian_lss_multi</code> to <code>atol = 1e-5</code>, both <code>α</code> blocks&#39; coefficients to <code>rtol = 2e-4, atol = 2e-5</code> — the same dense-vs-sparse tolerances #551 itself ships with (<code>test/test_lss_sparse.jl:83–115</code>, <code>atol = 1e-5</code> on loglik/coef; the note&#39;s earlier <code>1e-8</code> draft was tighter than #551&#39;s own precedent with no measured margin to justify it, and was flagged as likely-flaky by the adversarial review, §8 finding 6).</p></li><li><p><strong>FD-vs-exact gradient</strong>, three points: the fixture-A MLE, a random interior point, and a boundary-ish point (one component&#39;s <code>α</code> pushed so <code>σ_c,g → e^{-6}</code>, mirroring #551&#39;s own boundary check). <code>‖g_exact − g_FD‖_∞ ≤ 1e-6</code> at all three — unchanged; this is the same bound #551 uses for its own exact-vs-FD Hessian check (<code>gaussian_sparse_lss.jl:199– 217</code>), not the looser <code>rtol=2e-4,atol=2e-5</code> line 198 uses for its packed <em>stored</em>-gradient regression test.</p></li><li><p><strong>Cross-term gradcheck (finding 4, §3).</strong> The generalisation of <code>review_s7b_gradcheck.jl</code>&#39;s toy into a permanent unit test: a small nested multi-component fixture (phylo-like 4-node block + 2-level iid block, nested), assert the analytic <code>g_α,c</code> (with the cross <code>Hinv[g,t]</code> term) matches central-FD <code>d(logdet H)/dα</code> to <code>≤ 1e-6</code>, <strong>and</strong> assert the diagonal-only formula is rejected by the same test (<code>&gt; 1e-3</code> away from FD) — a regression guard so the 91%-error version this note first drafted can never silently come back.</p></li><li><p><strong>Crossed fixture, fill prediction.</strong> Fixture B (<code>lsss_B.csv</code>, real <code>study</code> crossed with <code>species</code>, §2.2) — assert the measured <code>nnz(L)/nnz(H) ≥ 1.2</code> (crossed) vs. the nested comparator&#39;s <code>&lt; 1.0</code>, i.e. the note&#39;s §2.3 claim is checked against the actual shipped fixture, not only synthetic data.</p></li><li><p><strong>Scaling to p = 10,000</strong> — <strong>only</strong> if §2 continues to hold at that scale for the in-scope topology (one phylo + nested/small-crossed iid). This is a &gt;30-minute class of run (D-139): pilot on Totoro first (≤150 cores, per D-143), report the pilot&#39;s wall time and <code>nnz(L)/p</code> before committing to the full p=10,000 fit. Not run for this note.</p></li></ol><hr><h2 id="6.-Memory-bound" tabindex="-1">6. Memory bound <a class="header-anchor" href="#6.-Memory-bound" aria-label="Permalink to &quot;6. Memory bound {#6.-Memory-bound}&quot;">​</a></h2><p>Bytes as a function of the measured <code>nnz</code>, using a standard CSC double sparse-matrix cost <code>≈ 16·nnz + 8·(dim+1)</code> for <code>H</code>, and the same for the Cholesky factor <code>L</code> (its own nnz):</p><table tabindex="0"><thead><tr><th style="text-align:right;">Case</th><th style="text-align:right;">dim</th><th style="text-align:right;">nnz(H)</th><th style="text-align:right;">bytes(H)</th><th style="text-align:right;">nnz(L)</th><th style="text-align:right;">bytes(L)</th></tr></thead><tbody><tr><td style="text-align:right;">(a) nested, p=1000</td><td style="text-align:right;">2048</td><td style="text-align:right;">8028</td><td style="text-align:right;">≈145 KB</td><td style="text-align:right;">6213</td><td style="text-align:right;">≈116 KB</td></tr><tr><td style="text-align:right;">(b) crossed, p=1000</td><td style="text-align:right;">2048</td><td style="text-align:right;">15578</td><td style="text-align:right;">≈265 KB</td><td style="text-align:right;">23920</td><td style="text-align:right;">≈399 KB</td></tr><tr><td style="text-align:right;">(c) two crossed iid(200,200)</td><td style="text-align:right;">400</td><td style="text-align:right;">9848</td><td style="text-align:right;">≈161 KB</td><td style="text-align:right;">56141</td><td style="text-align:right;">≈901 KB</td></tr></tbody></table><p>For the in-scope regime (§2.3), <code>nnz(L) = O(p)</code> (empirically <code>≈3·p</code> nested, <code>≈11.5·p</code> for one small crossed iid factor with <code>G_c=50</code>), so <strong>memory stays linear in <code>p</code></strong> for one phylo + bounded iid components. It is emphatically <strong>not</strong> linear for two large crossed components — <code>(c)</code>&#39;s 400×400 system already needs <code>56,141</code> factor nonzeros, i.e. <code>nnz(L)/dim² ≈ 0.35</code>: memory there scales like <code>O(G_1·G_2)</code>, the dense bound, well before <code>p</code> enters at all.</p><hr><h2 id="7.-Estimate-and-decision" tabindex="-1">7. Estimate and decision <a class="header-anchor" href="#7.-Estimate-and-decision" aria-label="Permalink to &quot;7. Estimate and decision {#7.-Estimate-and-decision}&quot;">​</a></h2><p>Agent-hours for Phase 2, by sub-slice (nested + one-phylo scope only):</p><table tabindex="0"><thead><tr><th style="text-align:right;">Sub-slice</th><th style="text-align:right;">Work</th><th style="text-align:right;">Est.</th></tr></thead><tbody><tr><td style="text-align:right;">S7b.1</td><td style="text-align:right;">Block <code>H</code>/<code>b</code> assembly (multi-component, §2–3 TO BUILD rows)</td><td style="text-align:right;">4–6h</td></tr><tr><td style="text-align:right;">S7b.2</td><td style="text-align:right;">Per-component <code>g_α,c</code> gradient generalisation, incl. the own-node trace term and its FD check</td><td style="text-align:right;">6–8h (raised from 3–4h — §8 finding 4/7: not a mechanical loop-widening; the diagonal-only formula this note first drafted was wrong (91% error vs FD), so the corrected term needs designing and checking, not just transcribing #551&#39;s loop)</td></tr><tr><td style="text-align:right;">S7b.2b</td><td style="text-align:right;">Cross-block <code>Hinv[g,t]</code>/<code>∂H[g,t]/∂α</code> accumulation — design + a permanent per-component-pair FD oracle (generalising <code>review_s7b_gradcheck.jl</code> into <code>test/</code>, §5 oracle 3)</td><td style="text-align:right;">3–4h (new sub-slice; this logic has no #551 analogue to adapt)</td></tr><tr><td style="text-align:right;">S7b.3</td><td style="text-align:right;">REML multi-component sparse backsolves (§4)</td><td style="text-align:right;">2–3h</td></tr><tr><td style="text-align:right;">S7b.4</td><td style="text-align:right;">Oracles 1, 2, 4 (§5), wired into <code>test/</code></td><td style="text-align:right;">3–4h</td></tr><tr><td style="text-align:right;">S7b.5</td><td style="text-align:right;">p=10,000 pilot (Totoro, D-139) + scaling writeup, incl. the unbalanced/caterpillar follow-ups §2.3 leaves open</td><td style="text-align:right;">2–3h (pilot only; full run separate)</td></tr><tr><td style="text-align:right;"><strong>Total</strong></td><td style="text-align:right;"></td><td style="text-align:right;"><strong>20–28h</strong></td></tr></tbody></table><p><strong>GO/NO-GO:</strong> GO for the sparse multi-component engine restricted to one phylogenetic component plus nested (or small, <code>G_c ≪ p</code>) iid components — §2.1/§2.3 measure <code>nnz(L) = O(p)</code> and flat <code>nnz(L)/p</code> to p=4000 for both sub-cases. NO-GO for a general &quot;any crossed structures&quot; sparse route — file a separate issue if that is ever needed: case (c) measured <code>nnz(L)/dim² ≈ 0.35</code> (near-dense) on a trivially small 400×400 system with two comparable- size crossed factors, and no elimination order changes that. The existing <strong>dense</strong> multi-component route (<code>gaussian_lss.jl:564</code>, <code>n ≤ 5000</code>) already covers genuinely crossed models like fixture B and should stay the recommended route for that topology.</p><hr><h2 id="8.-Adversarial-review" tabindex="-1">8. Adversarial review <a class="header-anchor" href="#8.-Adversarial-review" aria-label="Permalink to &quot;8. Adversarial review {#8.-Adversarial-review}&quot;">​</a></h2><ol start="2"><li><p><strong>Fill claim (§2.1) re-run.</strong> <code>probe_s7b_fill.jl</code>/<code>probe_s7b_fixtureB.jl</code> reproduced exactly: (a) 8028/6213, (b) 15578/23920, (c) 9848/56141; fixture B 582/783 vs 262/208 nested. No drift.</p></li><li><p><strong>Adversarial fill sweeps</strong> (<code>review_s7b_fill2.jl/.log</code>): (i) <code>G_c</code> scaling with <code>p</code> (<code>G_c=p/10</code>, <code>p/2</code> at <code>p=1000,2000</code>) — flat holds, but at a <strong>larger constant</strong> (<code>nnz(L)/p ≈ 6.47</code>/<code>6.48</code> vs the note&#39;s <code>3.03</code> at <code>G_c=50</code>) — untested case, result is reassuring but the note never says the O(p) constant itself depends on <code>G_c</code>. (ii) <strong>Unbalanced groups</strong> (90% of species in one group, <code>G2=50</code>): <code>nnz(L)/p</code> = 6.77 → 7.06 → 7.24 at p=1000/2000/4000 — a real, if mild (+7% over 4×p), <strong>upward drift</strong> the note&#39;s uniform- size measurements never surface; not yet visibly asymptoting at p=4000. (iii) <strong>Caterpillar tree</strong> (<code>random_caterpillar_tree</code>, CHOLMOD ordering stress), nested G2=50: flat and stable (6.03/6.02/6.01) but at <strong>2× the balanced-tree constant</strong> — untested topology, survives but the &quot;flat to p=4000&quot; headline was only ever measured on a balanced tree with uniform group sizes. Caterpillar+crossed also stays flat (~11–12, matching case (b)&#39;s order). None of these break O(p); all narrow what &quot;confirmed&quot; actually covers.</p></li><li><p><strong>Model convention (§1).</strong> <code>σa = exp.(c.Zg * α)</code> at <code>gaussian_lss.jl:582/613</code> confirmed — within-component predictors sum on the log scale as stated. The dense builder (<code>gaussian_lss.jl:690–739</code>) allows a general multi-column <code>Zg</code> per iid component and only ever pushes <strong>one</strong> <code>structured</code>/phylo block — the note&#39;s &quot;(at most one) phylogenetic component, general iid predictor&quot; scope is not a silent narrowing; it matches the builder exactly.</p></li><li><p><strong>Gradient table (§3), <code>g_α,c</code> row — WRONG as stated, load-bearing.</strong> The claim &quot;trace_term reads Hinv at that node&quot; (diagonal only) is the single-component (#551) formula; it does not generalize. In the augmented joint state, <code>∂H/∂α_c,g</code>also perturbs the <strong>cross-component blocks</strong><code>H[g,t]</code>for every other-component node<code>t</code>sharing an observation with group<code>g</code>(exactly the blocks §2 itself introduces) — the trace needs<code>2·Σ_t Hinv[g,t]·∂H[g,t]/∂α_c,g</code>too. Numerically confirmed on a 6-node nested toy (<code>review_s7b_gradcheck.jl/.log</code>): FD = 1.400168; diagonal-only formula = 2.669901 (<strong>91% error</strong>); formula with cross terms = 1.400168 (agrees to 3e-10). <strong>Good news</strong>: the needed<code>Hinv[g,t]</code>entries are always inside the Takahashi/<code>L</code>pattern for <em>any</em> topology (nested or crossed) — they equal exactly the pairs where<code>H</code>itself is already nonzero, and sparse-Cholesky fill always contains the original matrix&#39;s pattern. So this is a spec gap, not a feasibility gap — but the &quot;TO BUILD&quot; note for<code>g_α,c</code> must say so explicitly.</p></li><li><p><strong>REML (§4).</strong> No bug: <code>bX</code> accumulation is linear per-component (no cross term), so <code>ch \\ bX</code> over the full block <code>H</code> already captures cross-component coupling correctly through the solve. Aside: #551&#39;s current REML path uses <code>autodiff = :finite</code> (not the analytic ML gradient), so finding 4 does not currently corrupt REML — but a future analytic REML gradient would inherit it.</p></li><li><p><strong>Oracles (§5).</strong> Tolerances are tighter than #551&#39;s own precedent: <code>test_lss_sparse.jl</code> uses <code>atol=1e-5</code> for dense-vs-sparse loglik/coef and <code>rtol=2e-4, atol=2e-5</code> for FD-vs-exact grad; the note proposes <code>1e-8</code>/<code>1e-6</code> with no stated reason to expect a tighter bound — likely flaky as written. Oracle 3&#39;s fixture-B fill assertion (<code>≥1.2</code> crossed vs <code>&lt;1.0</code> nested) is confirmed with real margin (1.345 vs 0.794). p=10,000 correctly fenced behind D-139 (pilot first, not run).</p></li><li><p><strong>Estimate (§7).</strong> #551 (single component, no cross-block bookkeeping) shipped 249 src lines + 424 test lines in one PR with no itemized hour log, so no clean baseline exists — but given finding 4, S7b.2&#39;s 3–4h for &quot;gradient generalization&quot; undersells the work: it is not a mechanical loop-widening, it is new cross-component accumulation logic through Takahashi that has to be designed, tested per component-pair, and checked against FD (as done here) before it can be trusted.</p></li></ol><p>VERDICT: APPROVE WITH REQUIRED EDITS</p><ul><li><p>Rewrite §3&#39;s <code>g_α,c</code> row to include the cross-component <code>Hinv[g,t]</code> trace terms (finding 4); state the general in-pattern guarantee so implementers don&#39;t think this needs re-deriving.</p></li><li><p>Revise §5 oracle tolerances to match #551 precedent (<code>atol=1e-5</code> / <code>rtol=2e-4,atol=2e-5</code>) or justify tighter ones.</p></li><li><p>Add a caveat to §2.3 that flatness was measured only under uniform group sizes and a balanced tree; cite the unbalanced-size drift and caterpillar-tree constant shift (finding 2) as open follow-ups.</p></li><li><p>Revise S7b.2&#39;s estimate upward and add an explicit cross-block-accumulation design/test subtask.</p></li></ul><p>Edits applied: 2026-09-02, by the author</p>`,63)])])}const u=t(a,[["render",i]]);export{p as __pageData,u as default};
