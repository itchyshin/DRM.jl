# GOAL — DRM.jl Claude handover while tip is IDLE (IMMUTABLE — re-read every arc)
Read this first, every cycle. Auto-compact eats messages, not this file. Unsure after a compaction?
Re-read THIS, then checkpoint.md, then continue.

## Mission
Land one **docs-only** PR from `origin/main` @ `d0fac9d7` (Merge [#405](https://github.com/itchyshin/DRM.jl/pull/405))
that writes the Claude START HERE kit. Then the desk is **IDLE** pending an
owner-named G0. Next pickup is **Claude**. Cursor lane idle/handing off.
Issue **136 stays OPEN**. Do **not** invent a ship G0. Do **not**
start drmTMB from this tree. Do **not** start later #136 two-part/ZI×RE or #49
unless the owner names them.

## Headline
Tip pointer says **IDLE pending owner G0 — Claude pickup** — not “wait for merge #405”.

## Invariants
- One *DRM.jl* docs lane for this run (`docs/claude-handover-idle`). drmTMB = sibling.
- Fence: no `src/`; no q=4 core rewrite; ML default; issue 136 stays OPEN; #49 parked;
  no GPL vendoring; never stage `.worktrees/` or `.codex/agents/shannon-coordinator.toml`;
  D-111 OFF; no inventing ship from ROADMAP; no Experimental → Implemented.
- START HERE: `docs/dev-log/handover/2026-08-14-claude-handover-drm-idle.md`.
- 2026-08-13 Cursor idle note is historical. Active-Lane-Split on
  `docs/dev-log/coordination-board.md`.
- Authoritative WHAT: `LOOP/ultra-plan.md`.
- Auto-merge: `allow_auto_merge` true; `gh pr merge N --auto --merge`. Pause for
  `src/` / formula / version / `AGENTS.md`. Never close/fix/resolve next to #136.

## Definition of done (this hygiene slice)
- Handover + LOOP + coordination-board match tip `d0fac9d7` / #405 MERGED
- Issue 136 confirmed OPEN; PR body has no closer keywords for that issue
- Next Immediate Steps = rehydrate → confirm idle-or-named-work → STOP and ask owner for G0
- PR open; `--auto --merge` allowed (docs-only). Do not invent a ship G0.
